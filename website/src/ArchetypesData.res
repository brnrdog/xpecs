// GENERATED FILE — do not edit by hand.
// Run `npm run registry` (scripts/generate-registry.mjs) to regenerate.

type apiProp = {
  name: string,
  type_: string,
  values: array<string>,
  default: string,
  description: string,
}

type apiSlot = {
  name: string,
  required: bool,
  description: string,
}

type apiContract = {
  props: array<apiProp>,
  slots: array<apiSlot>,
  events: array<string>,
  role: string,
  keyboard: array<string>,
  announces: array<string>,
  states: array<string>,
  tokens: array<string>,
}

type archetype = {
  id: string,
  title: string,
  layer: string,
  status: string,
  version: string,
  summary: string,
  tags: array<string>,
  aliases: array<string>,
  composedOf: array<string>,
  usedBy: array<string>,
  related: array<string>,
  intent: string,
  spec: string,
  api: option<apiContract>,
}

let all: array<archetype> = [
  {
    id: "aspect-ratio",
    title: "Aspect Ratio",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A layout primitive that constrains content to a fixed width-to-height proportion.",
    tags: ["layout", "media", "structure", "responsive"],
    aliases: ["ratio-box", "media-box"],
    composedOf: [],
    usedBy: ["card", "carousel", "avatar", "landing-page"],
    related: ["skeleton", "scroll-area", "separator"],
    intent: "An aspect-ratio box holds its content to a consistent width-to-height proportion (16:9, 1:1, 4:3) regardless of viewport size. It keeps media grids tidy and prevents layout shift by reserving the correct space before content loads.",
    spec: "<h2>Intent</h2>\n<p>An aspect-ratio box holds its content to a consistent width-to-height proportion (16:9, 1:1, 4:3) regardless of viewport size. It keeps media grids tidy and prevents layout shift by reserving the correct space before content loads.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Displaying media (images, video, embeds, maps) that must keep a shape.</li><li>Keeping items in a grid uniformly proportioned.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Content height should follow its natural text flow.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Ratio container</strong> (required) — enforces the proportion.</li><li><strong>Content slot</strong> (required) — media or embed that fills the box.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Loading</strong> — reserves exact space (often paired with a skeleton).</li><li><strong>Loaded</strong> — content fills the box, cropped or fitted as configured.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Common ratios</strong> — 16:9, 4:3, 3:2, 1:1, 21:9.</li><li><strong>Fit modes</strong> — cover (crop to fill) or contain (fit within).</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The box scales with its container's width while preserving the ratio, so height is derived, not fixed. This keeps grids aligned and avoids cumulative layout shift as media loads.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a layout wrapper only; the contained media carries its own text alternatives and roles.</li><li><strong>Content</strong> — ensure cropping (cover) doesn't hide essential parts of an image.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Choose a ratio that suits the content and keep it consistent across a set.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a layout primitive wrapping other content.</p>\n<p><strong>Used by:</strong> card, carousel, avatar, landing-page.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Reserve space to prevent layout shift.</li><li>Pick a fit mode intentionally.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Crop away meaningful image content with cover fit.</li></ul>\n<h2>References</h2>\n<ul><li>Web platform — CSS aspect-ratio and intrinsic sizing.</li></ul>",
    api: None,
  },
  {
    id: "avatar",
    title: "Avatar",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A compact visual representation of a person or entity, with a graceful fallback when no image is available.",
    tags: ["identity", "media", "display", "user"],
    aliases: ["profile-picture", "user-pic", "gravatar"],
    composedOf: [],
    usedBy: ["navbar", "card", "dropdown-menu", "comment", "sidebar"],
    related: ["badge", "skeleton"],
    intent: "An avatar represents a person or entity in a small, recognizable footprint so users can identify who is associated with content or an action at a glance. It must degrade gracefully: when an image is missing or slow, it shows a meaningful fallback rather than a broken graphic.",
    spec: "<h2>Intent</h2>\n<p>An avatar represents a person or entity in a small, recognizable footprint so users can identify <em>who</em> is associated with content or an action at a glance. It must degrade gracefully: when an image is missing or slow, it shows a meaningful fallback rather than a broken graphic.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Attributing content or presence to a person or organization.</li><li>Space is limited and identity must be recognizable at small sizes.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>You need a large, decorative image — use a plain media element.</li><li>Identity is better carried by a full name label alone.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Image</strong> (optional) — the portrait or logo.</li><li><strong>Fallback</strong> (required) — initials or a placeholder shown while loading or when no image exists.</li><li><strong>Status/badge overlay</strong> (optional) — presence dot or count, via the badge element.</li><li><strong>Shape</strong> (required) — circular or rounded-square frame that clips content.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Loading</strong> — shows the fallback (or a skeleton) until the image resolves.</li><li><strong>Loaded</strong> — shows the image.</li><li><strong>Error / empty</strong> — remains on the fallback; never a broken image.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Single</strong> — one image/fallback.</li><li><strong>Group / stack</strong> — overlapping avatars with an overflow count (\"+3\").</li><li><strong>With status</strong> — presence or notification overlay.</li><li><strong>Sizes</strong> — from inline-small to profile-large, on a consistent scale.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Avatars keep a fixed aspect ratio and clip content to their shape. In stacks, they overlap by a consistent offset and cap the visible count with an overflow indicator. Sizes come from a defined scale rather than arbitrary values.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — provide a text alternative naming the person/entity; decorative duplicates of an adjacent name are marked decorative.</li><li><strong>Screen reader</strong> — announces the accessible name, not the file.</li><li><strong>Contrast</strong> — fallback text/initials meet contrast against the background.</li><li><strong>Interactive</strong> — if the avatar is a control (opens a menu), it exposes a button/link role with an accessible name.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Prefer initials from the entity's name for the fallback; cap at one or two.</li><li>Keep alt text to the entity name, not \"avatar of…\".</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive; may host a <strong>badge</strong> overlay.</p>\n<p><strong>Used by:</strong> navbar, card, dropdown-menu, sidebar, and any attribution surface.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Always provide a fallback and a text alternative.</li><li>Use a consistent size scale and shape.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Show a broken image when the source fails.</li><li>Encode identity by color alone.</li></ul>\n<h2>References</h2>\n<ul><li>WAI — text alternatives for images.</li></ul>",
    api: None,
  },
  {
    id: "badge",
    title: "Badge",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A small label that annotates an element with a status, category, or count.",
    tags: ["status", "label", "metadata", "count", "tag"],
    aliases: ["tag", "pill", "chip", "status-badge"],
    composedOf: [],
    usedBy: ["avatar", "card", "navbar", "table", "tabs", "sidebar"],
    related: ["button", "label"],
    intent: "A badge attaches a short, high-signal annotation to another element — a status (\"Active\"), a category (\"Beta\"), or a count (unread items). It is read at a glance and never the primary focus; it enriches the thing it sits on.",
    spec: "<h2>Intent</h2>\n<p>A badge attaches a short, high-signal annotation to another element — a status (\"Active\"), a category (\"Beta\"), or a count (unread items). It is read at a glance and never the primary focus; it enriches the thing it sits on.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Communicating status, category, or a small count inline with content.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The label is interactive and triggers an action — use a <strong>button</strong> or a removable chip.</li><li>The text is long or explanatory — use inline text or an <strong>alert</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Label / value</strong> (required) — short text or number.</li><li><strong>Leading icon or dot</strong> (optional) — reinforces status.</li><li><strong>Container</strong> (required) — compact, high-contrast surface.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static</strong> — most badges are non-interactive.</li><li><strong>Count</strong> — may cap large numbers (\"99+\").</li><li><strong>Dismissible (chip variant)</strong> — includes a remove affordance and announces removal.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Status</strong> — semantic colors mapped to meaning (success, warning, danger, info, neutral) with a non-color cue.</li><li><strong>Count / notification</strong> — numeric or dot indicator.</li><li><strong>Category / tag</strong> — neutral labeling.</li><li><strong>Emphasis</strong> — solid, soft, or outline treatments.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Badges size to content with tight padding and align to the baseline or center of the element they annotate. Notification dots anchor to a corner of their host (e.g. an avatar or icon button). Text truncates rather than wraps.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — status badges convey meaning via text or an accessible label, not color alone; live counts may use a polite live region.</li><li><strong>Screen reader</strong> — announces the value and, for status, its meaning.</li><li><strong>Contrast</strong> — text and background meet contrast requirements.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep to one or two words, or a bounded number.</li><li>Use consistent status vocabulary across the product.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive element.</p>\n<p><strong>Used by:</strong> avatar (overlay), card, navbar, table cells, tabs, sidebar.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Pair color with text or an icon for meaning.</li><li>Keep a consistent status color-to-meaning mapping.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Make a badge the only way to trigger an action.</li><li>Overload a view with competing badges.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Use of Color; Status Messages.</li></ul>",
    api: None,
  },
  {
    id: "button",
    title: "Button",
    layer: "element",
    status: "stable",
    version: "1.1.0",
    summary: "An interactive control that triggers an action or event when activated.",
    tags: ["action", "control", "interactive", "form", "cta"],
    aliases: ["btn", "cta"],
    composedOf: [],
    usedBy: ["input", "navbar", "card", "form", "dialog", "landing-page"],
    related: ["link", "icon-button"],
    intent: "A button lets a person trigger a single, well-defined action — submit a form, open a dialog, confirm a choice. It is the most fundamental unit of intent in an interface: the user says \"do this now.\" A button represents an action, not a destination; navigation to another location is the job of a link.",
    spec: "<h2>Intent</h2>\n<p>A button lets a person trigger a single, well-defined action — submit a form, open a dialog, confirm a choice. It is the most fundamental unit of intent in an interface: the user says \"do this now.\" A button represents an <em>action</em>, not a <em>destination</em>; navigation to another location is the job of a link.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>The user needs to perform an action (submit, save, delete, add, confirm).</li><li>The action changes state, sends data, or opens a transient surface.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>You are navigating to a URL or a new view — use a <strong>link</strong> instead, even if it is styled to look like a button.</li><li>The control toggles between two states and should read as on/off — consider a <strong>switch</strong> or <strong>checkbox</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Label</strong> (required for text buttons) — a verb-led phrase naming the action.</li><li><strong>Container / hit target</strong> (required) — the activatable surface; must meet minimum target size even when the visible box is small.</li><li><strong>Leading icon</strong> (optional) — reinforces meaning; never the sole carrier of it unless the button is an explicit icon-only variant with an accessible name.</li><li><strong>Trailing icon</strong> (optional) — e.g. a chevron indicating a menu will open.</li><li><strong>Loading indicator</strong> (optional) — replaces or accompanies the label while an async action is in flight.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default</strong> — resting, ready to be activated.</li><li><strong>Hover</strong> — pointer over the target; signals affordance.</li><li><strong>Focus (visible)</strong> — reached via keyboard; a clearly visible focus indicator is required.</li><li><strong>Active / pressed</strong> — during activation.</li><li><strong>Disabled</strong> — action not currently available; not focusable by default and communicates why through surrounding context.</li><li><strong>Loading</strong> — the action is in progress; the button prevents duplicate submissions and communicates that work is happening.</li></ul>\n<p>Activation fires on click/tap and on <code>Enter</code>/<code>Space</code> when focused. An async button should return to an appropriate state (default, or a success/error cue owned by the surrounding component) when the action resolves.</p>\n<h2>Variants</h2>\n<ul><li><strong>Primary</strong> — the single most important action in a given context; used sparingly (ideally one per view or section).</li><li><strong>Secondary</strong> — a supporting action of lower emphasis.</li><li><strong>Tertiary / ghost</strong> — minimal emphasis for low-stakes actions.</li><li><strong>Destructive</strong> — actions that remove or irreversibly change data; visually distinct and often paired with confirmation.</li><li><strong>Icon-only</strong> — compact; requires an accessible name and usually a tooltip.</li></ul>\n<p>Emphasis is a spectrum expressed through your design tokens; the archetype only requires that emphasis map to <em>importance of the action</em>.</p>\n<h2>Layout &amp; responsiveness</h2>\n<p>Buttons size to their content plus consistent internal padding. In tight containers they may go full-width to maximize the target. When several buttons sit together, order them by importance and keep the primary action in the position your platform conventions expect (e.g. trailing edge in a dialog). On touch targets, honor the minimum target size regardless of visual size.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — focusable in normal tab order; activates on <code>Enter</code> and <code>Space</code>. Disabled buttons are removed from the tab order.</li><li><strong>Semantics</strong> — exposes a button role. An icon-only button must have an accessible name (visually-hidden label or equivalent).</li><li><strong>Screen reader</strong> — announces name, role, and state (e.g. disabled, pressed for toggle buttons, busy while loading).</li><li><strong>Focus</strong> — a visible, high-contrast focus indicator is required and must not be removed.</li><li><strong>Target size &amp; contrast</strong> — meet minimum interactive target size and text/icon contrast against the button surface.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Lead with a verb and name the outcome: \"Save changes\", \"Delete account\".</li><li>Keep labels short (ideally one to three words) and specific; avoid \"OK\"/\"Submit\" when a precise verb is available.</li><li>Use sentence case unless your brand dictates otherwise, consistently.</li><li>Never rely on color alone to convey a destructive or primary action.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a button is a primitive element.</p>\n<p><strong>Used by:</strong> input (as an inline action), navbar, card, form, dialog, landing-page.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Use one clear primary action per context.</li><li>Keep the accessible name and the visible label in agreement.</li><li>Disable and show progress during async work to prevent double submission.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a button to navigate to a URL.</li><li>Fill a screen with primary buttons — emphasis loses meaning.</li><li>Convey the only meaning through an icon without an accessible name.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Button pattern.</li><li>Nielsen Norman Group — \"Buttons vs. Links\".</li></ul>",
    api: Some({ props: [{ name: "variant", type_: "enum", values: ["primary", "secondary", "ghost", "destructive"], default: "primary", description: "Emphasis of the action." }, { name: "size", type_: "enum", values: ["sm", "md", "lg"], default: "md", description: "Control density." }, { name: "type", type_: "enum", values: ["button", "submit", "reset"], default: "button", description: "Native button behavior in a form." }, { name: "disabled", type_: "boolean", values: [], default: "false", description: "Action unavailable; removed from tab order." }, { name: "loading", type_: "boolean", values: [], default: "false", description: "Async action in flight; blocks re-activation." }], slots: [{ name: "label", required: true, description: "Verb-led text naming the action." }, { name: "leadingIcon", required: false, description: "Reinforces meaning." }, { name: "trailingIcon", required: false, description: "e.g. a chevron opening a menu." }], events: ["onActivate"], role: "button", keyboard: ["Enter", "Space"], announces: ["disabled", "busy"], states: ["default", "hover", "focus-visible", "active", "disabled", "loading"], tokens: ["color.action.*", "radius.md", "space.inline.*", "font.weight.medium"] }),
  },
  {
    id: "checkbox",
    title: "Checkbox",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A control for toggling a single option on or off, or selecting several from a set.",
    tags: ["form", "control", "selection", "boolean"],
    aliases: ["check", "tickbox"],
    composedOf: [],
    usedBy: ["form", "field", "data-table", "dropdown-menu"],
    related: ["switch", "radio-group", "toggle"],
    intent: "A checkbox lets a person turn a single option on or off, or pick any number of options from a set. It represents a discrete boolean choice whose effect is typically applied on form submission rather than immediately.",
    spec: "<h2>Intent</h2>\n<p>A checkbox lets a person turn a single option on or off, or pick any number of options from a set. It represents a discrete boolean choice whose effect is typically applied on form submission rather than immediately.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Selecting zero or more options from a list.</li><li>Accepting a single binary condition (e.g. \"I agree\").</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The choice takes effect instantly with an on/off meaning — use a <strong>switch</strong>.</li><li>Exactly one option must be chosen from several — use a <strong>radio-group</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Control box</strong> (required) — the checkable target.</li><li><strong>Check / indeterminate mark</strong> (required) — indicates state.</li><li><strong>Label</strong> (required) — describes the option and extends the hit target.</li><li><strong>Description / helper</strong> (optional) — extra context.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Unchecked / checked</strong> — the two primary states, toggled on activation.</li><li><strong>Indeterminate</strong> — represents a partial selection of child items (e.g. a \"select all\" parent).</li><li><strong>Disabled</strong> / <strong>read-only</strong> — not editable.</li><li><strong>Invalid</strong> — fails validation (e.g. a required agreement).</li></ul>\n<p>Clicking the label toggles the box. A parent \"select all\" reflects children as checked, unchecked, or indeterminate.</p>\n<h2>Variants</h2>\n<ul><li><strong>Single</strong> — standalone boolean.</li><li><strong>Group</strong> — multiple related checkboxes.</li><li><strong>With description</strong> — richer label block.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The box and label sit on a shared baseline with the label to the trailing side; the whole row is the target. Groups stack vertically with consistent spacing. Long labels wrap without misaligning the control.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — focusable; <code>Space</code> toggles.</li><li><strong>Semantics</strong> — checkbox role with checked/unchecked/mixed state exposed; label programmatically associated.</li><li><strong>Screen reader</strong> — announces label, role, and state (including mixed).</li><li><strong>Focus</strong> — visible focus indicator required.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Label states the positive option; avoid negatives that invert meaning.</li><li>Keep labels parallel in phrasing within a group.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive control (often wrapped by <strong>field</strong> for label/error).</p>\n<p><strong>Used by:</strong> form, field, data-table (row selection), dropdown-menu.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Make the label part of the clickable target.</li><li>Use indeterminate for partial parent selection.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a checkbox for an instantly-applied setting.</li><li>Rely on the box alone without a label.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Checkbox pattern.</li></ul>",
    api: None,
  },
  {
    id: "icon",
    title: "Icon",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A small pictographic symbol that conveys meaning or reinforces a label compactly.",
    tags: ["symbol", "visual", "display", "glyph"],
    aliases: ["glyph", "pictogram", "symbol"],
    composedOf: [],
    usedBy: ["button", "icon-button", "feature-grid", "alert", "badge", "list", "stat"],
    related: ["icon-button", "logo"],
    intent: "An icon is a small symbol that conveys a concept at a glance — a magnifier for search, a trash can for delete — or reinforces a text label. Used well, icons speed recognition and save space; used carelessly, they become decoration that assistive technology and unfamiliar users can't interpret. Meaning, not ornament, is the bar.",
    spec: "<h2>Intent</h2>\n<p>An icon is a small symbol that conveys a concept at a glance — a magnifier for search, a trash can for delete — or reinforces a text label. Used well, icons speed recognition and save space; used carelessly, they become decoration that assistive technology and unfamiliar users can't interpret. Meaning, not ornament, is the bar.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Reinforcing a label, or marking a well-understood action/status compactly.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The concept isn't reliably recognizable as a symbol — use text.</li><li>The icon is the sole carrier of meaning without an accessible name.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Glyph</strong> (required) — the symbol itself, on a consistent grid and weight.</li><li><strong>Bounding box</strong> (required) — a consistent optical size for alignment.</li><li><strong>Accessible name</strong> (conditional) — required when the icon conveys meaning on its own; omitted (and hidden) when purely decorative beside a label.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Decorative</strong> — accompanies text; hidden from assistive tech.</li><li><strong>Meaningful</strong> — conveys standalone meaning; carries a text alternative.</li><li><strong>Inherited styling</strong> — typically takes color and size from surrounding text.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Outline vs. solid</strong> — a consistent style set.</li><li><strong>Sizes</strong> — from an optical size scale.</li><li><strong>Standalone vs. inline</strong> — beside text or on its own.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Icons align optically with adjacent text (baseline and size) and scale from a defined size set. Keep one style family across a product so the visual language stays coherent.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Decorative icons</strong> — hidden from assistive tech so they aren't announced.</li><li><strong>Meaningful icons</strong> — provide a text alternative/accessible name describing the concept, not the picture.</li><li><strong>Contrast</strong> — meaningful icons meet non-text contrast against their background.</li><li><strong>Not sole meaning</strong> — never rely on an icon alone where the meaning must be understood; pair with text or a name.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Use conventional metaphors; keep one icon meaning consistent across the product.</li><li>Name meaningful icons by concept (\"Search\"), not appearance (\"Magnifier\").</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive display element.</p>\n<p><strong>Used by:</strong> button, icon-button, feature-grid, alert, badge, list, stat.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep a single, consistent icon style and grid.</li><li>Give meaningful icons accessible names; hide decorative ones.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Rely on an unfamiliar icon without a label.</li><li>Mix icon styles within a product.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Non-text Contrast; text alternatives.</li></ul>",
    api: None,
  },
  {
    id: "icon-button",
    title: "Icon Button",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A compact, icon-only button that triggers an action where space is tight.",
    tags: ["action", "control", "interactive", "icon", "compact"],
    aliases: ["icon-btn", "square-button"],
    composedOf: [],
    usedBy: ["toolbar", "navbar", "data-table", "dialog", "card"],
    related: ["button", "tooltip", "toggle"],
    intent: "An icon button triggers an action using a glyph alone, trading an explicit text label for compactness. It suits dense contexts — toolbars, table rows, card corners — where a full button would crowd the layout. Because it drops the visible label, it carries a strict obligation: the icon must be recognizable and the control must still expose an accessible name.",
    spec: "<h2>Intent</h2>\n<p>An icon button triggers an action using a glyph alone, trading an explicit text label for compactness. It suits dense contexts — toolbars, table rows, card corners — where a full button would crowd the layout. Because it drops the visible label, it carries a strict obligation: the icon must be recognizable and the control must still expose an accessible name.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Space is constrained and the action's icon is well understood (close, edit, more, search).</li><li>Repeating the same action across many rows or items.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The action is important or ambiguous — a labeled <strong>button</strong> communicates better.</li><li>The icon's meaning isn't obvious without a label.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Icon</strong> (required) — a recognizable glyph conveying the action.</li><li><strong>Hit target</strong> (required) — meets minimum interactive size even when the glyph is small.</li><li><strong>Accessible name</strong> (required) — a visually-hidden label describing the action.</li><li><strong>Tooltip</strong> (recommended) — reveals the name on hover/focus.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default / hover / focus (visible) / active</strong> — as a button.</li><li><strong>Disabled</strong> — not focusable; communicates unavailability in context.</li><li><strong>Toggle</strong> (optional) — some icon buttons are pressed/unpressed toggles.</li></ul>\n<p>Activates on click and <code>Enter</code>/<code>Space</code>.</p>\n<h2>Variants</h2>\n<ul><li><strong>Standalone</strong> — a single compact action.</li><li><strong>In a group</strong> — several within a toolbar or button group.</li><li><strong>Emphasis</strong> — solid, subtle, or ghost, mapped to importance.</li><li><strong>Toggle</strong> — reflects an on/off state.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Icon buttons are typically square with the glyph centered. Keep consistent sizing within a group and honor the minimum touch target regardless of visual size, expanding the hit area beyond the glyph if needed.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Accessible name</strong> — required; provided via a visually-hidden label or equivalent, since there is no visible text.</li><li><strong>Keyboard</strong> — focusable; activates on <code>Enter</code>/<code>Space</code>; disabled removes it from the tab order.</li><li><strong>Screen reader</strong> — announces the action name and, for toggles, pressed state.</li><li><strong>Tooltip</strong> — supplements but never replaces the accessible name.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Choose conventional, unambiguous icons; pair with a tooltip.</li><li>Name the action by its outcome (\"Delete row\", not \"Trash icon\").</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive; a compact form of the <strong>button</strong>.</p>\n<p><strong>Used by:</strong> toolbar, navbar, data-table, dialog (close), card.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Always provide an accessible name and, ideally, a tooltip.</li><li>Keep the target comfortably tappable.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use an icon button for a critical or unfamiliar action without a label.</li><li>Rely on the glyph alone to convey meaning to assistive tech.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Button pattern; WCAG Target Size.</li></ul>",
    api: None,
  },
  {
    id: "kbd",
    title: "Keyboard Key",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "An inline representation of a key or key combination the user can press.",
    tags: ["display", "keyboard", "shortcut", "hint"],
    aliases: ["kbd", "keycap", "shortcut-hint", "hotkey"],
    composedOf: [],
    usedBy: ["command", "tooltip", "dropdown-menu", "menubar"],
    related: ["badge", "tooltip"],
    intent: "A keyboard-key element renders a physical key or shortcut combination inline (\"⌘K\", \"Ctrl+S\") so users can discover and recall keyboard shortcuts. It is display-only — it documents an interaction rather than performing one.",
    spec: "<h2>Intent</h2>\n<p>A keyboard-key element renders a physical key or shortcut combination inline (\"⌘K\", \"Ctrl+S\") so users can discover and recall keyboard shortcuts. It is display-only — it documents an interaction rather than performing one.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Showing a shortcut next to a command, in a menu, tooltip, or help text.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The element should trigger an action — that's a <strong>button</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Keycap(s)</strong> (required) — one per key, styled like a physical key.</li><li><strong>Combinator</strong> (optional) — separators between keys in a chord.</li></ul>\n<h2>States &amp; behavior</h2>\n<p>Static. Combinations render as ordered keys joined by a separator or spacing. Platform-specific glyphs (⌘, ⌥, ⇧) may substitute for names by platform.</p>\n<h2>Variants</h2>\n<ul><li><strong>Single key</strong> — one keycap.</li><li><strong>Combination</strong> — multiple keys forming a chord.</li><li><strong>Platform-aware</strong> — swaps modifiers per operating system.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Keycaps sit inline with surrounding text, aligned to the baseline, and stay compact. In menus they typically align to the trailing edge of the row.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — mark up as keyboard input so it's conveyed as such, not as code.</li><li><strong>Screen reader</strong> — announces the keys understandably (e.g. \"Command K\"), not raw glyphs that read poorly.</li><li><strong>Contrast</strong> — legible against its surface.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Match the platform's real modifier names/glyphs.</li><li>Keep chords short and conventional.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive display element.</p>\n<p><strong>Used by:</strong> command, tooltip, dropdown-menu, menubar.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Reflect the user's actual platform modifiers.</li><li>Keep it display-only.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use it as a clickable control.</li></ul>\n<h2>References</h2>\n<ul><li>HTML — the <code>kbd</code> element semantics.</li></ul>",
    api: None,
  },
  {
    id: "label",
    title: "Label",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A short caption that names a form control and is programmatically bound to it.",
    tags: ["form", "text", "accessibility", "field"],
    aliases: ["caption", "field-label"],
    composedOf: [],
    usedBy: ["input", "checkbox", "radio-group", "select", "switch", "field", "form"],
    related: ["field", "input"],
    intent: "A label names a form control so people know what value to provide and assistive technology can announce the control correctly. Its defining property is not the text itself but the programmatic association with a control — a label that isn't bound to anything is just text.",
    spec: "<h2>Intent</h2>\n<p>A label names a form control so people know what value to provide and assistive technology can announce the control correctly. Its defining property is not the text itself but the <strong>programmatic association</strong> with a control — a label that isn't bound to anything is just text.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Naming any interactive form control (input, checkbox, select, switch, …).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>You need long-form guidance — use helper text or a description instead.</li><li>The text is a heading for a section, not a control name.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Text</strong> (required) — the control's name.</li><li><strong>Required/optional indicator</strong> (optional) — marks whether input is mandatory.</li><li><strong>Association</strong> (required) — a binding to exactly one control.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default</strong> — static; clicking it focuses or activates the associated control.</li><li><strong>Disabled</strong> — visually reflects a disabled control.</li><li><strong>Error</strong> (optional) — may adopt an error treatment alongside the field's message.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Inline</strong> — beside the control (e.g. checkbox/switch rows).</li><li><strong>Stacked</strong> — above the control (most inputs).</li><li><strong>With requirement marker</strong> — required or optional annotation.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Stacked labels sit directly above their control for reliable reading order and wrapping; inline labels align to the control's baseline. Keep placement consistent across a form.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — bound to its control so name/role/value is complete.</li><li><strong>Screen reader</strong> — read as the control's accessible name.</li><li><strong>Interaction</strong> — activating the label moves focus to the control.</li><li><strong>Requirement</strong> — convey required state in text/semantics, not color alone.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Short noun phrases (\"Email\", \"Card number\"), not sentences or instructions.</li><li>Mark requirement consistently (either mark required or mark optional, not both).</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive; typically orchestrated by <strong>field</strong>.</p>\n<p><strong>Used by:</strong> input, checkbox, radio-group, select, switch, field, form.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Always associate the label with its control.</li><li>Keep labels visible; don't rely on placeholders.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a label as the only guidance for complex formats.</li><li>Bind one label to multiple controls.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Labels or Instructions; Info and Relationships.</li></ul>",
    api: None,
  },
  {
    id: "legend",
    title: "Legend",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A key that maps a chart's colors or symbols to the series they represent.",
    tags: ["chart", "data", "key", "display"],
    aliases: ["chart-key", "key", "series-key"],
    composedOf: [],
    usedBy: ["chart"],
    related: ["chart", "badge"],
    intent: "A legend is the key that tells the reader what a chart's colors, patterns, or symbols mean — which line is revenue, which bar is churn. Without it a multi-series chart is undecipherable; with it, the encoding becomes readable. It often doubles as a control for toggling series on and off.",
    spec: "<h2>Intent</h2>\n<p>A legend is the key that tells the reader what a chart's colors, patterns, or symbols mean — which line is revenue, which bar is churn. Without it a multi-series chart is undecipherable; with it, the encoding becomes readable. It often doubles as a control for toggling series on and off.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A chart uses color/shape to distinguish multiple series or categories.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>There is a single series, or direct labeling on the marks is clearer.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Items</strong> (required) — one per series: a swatch/symbol plus its label.</li><li><strong>Swatch</strong> (required) — the color/pattern/marker matching the chart.</li><li><strong>Label</strong> (required) — the series name.</li><li><strong>Value</strong> (optional) — a current or total value per series.</li><li><strong>Toggle affordance</strong> (optional) — when items show/hide series.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static key</strong> — maps encodings to labels.</li><li><strong>Interactive</strong> (optional) — clicking an item toggles that series; hidden series are visibly dimmed.</li><li><strong>Hover link</strong> (optional) — highlights the corresponding marks.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Horizontal / vertical</strong> placement.</li><li><strong>Static vs. interactive</strong> (toggle series).</li><li><strong>With values</strong> — legend items show numbers.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Legend items sit adjacent to the plot (top, right, or bottom), wrapping as space allows. On small charts the legend may move below the plot or collapse. Swatches align with their labels and match the chart's encoding exactly.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Association</strong> — labels are the accessible source of series meaning, since color must not be the only cue; consider direct labeling too.</li><li><strong>Interactive items</strong> — expose pressed/selected state and are keyboard operable.</li><li><strong>Contrast</strong> — swatches and text meet non-text/text contrast.</li><li><strong>Screen reader</strong> — series names are available in the chart's accessible description or data alternative.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep series names short and consistent with the data.</li><li>Order legend items to match the chart (e.g., stacking order).</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a small display element (swatch + label per item).</p>\n<p><strong>Used by:</strong> chart.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Match swatches exactly to the chart encoding.</li><li>Provide labels so meaning doesn't depend on color alone.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Rely on color-only differences without labels.</li><li>Detach the legend so far it's hard to correlate.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Use of Color; data visualization labeling guidance.</li></ul>",
    api: None,
  },
  {
    id: "link",
    title: "Link",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A navigational control that takes the user to another location or resource.",
    tags: ["navigation", "text", "interactive", "anchor"],
    aliases: ["anchor", "hyperlink", "a"],
    composedOf: [],
    usedBy: ["navbar", "breadcrumb", "pagination", "navigation-menu", "sidebar", "footer", "sign-in"],
    related: ["button", "icon-button"],
    intent: "A link takes the user somewhere — another page, a section of the current page, or an external resource. It represents navigation, not an action: activating a link changes location, and users rightly expect to be able to open it in a new tab, copy its address, and go back. This is the defining distinction from a button, which performs an action in place.",
    spec: "<h2>Intent</h2>\n<p>A link takes the user somewhere — another page, a section of the current page, or an external resource. It represents <em>navigation</em>, not an action: activating a link changes location, and users rightly expect to be able to open it in a new tab, copy its address, and go back. This is the defining distinction from a button, which performs an action in place.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Navigating to a URL, a route, an anchor on the page, or a downloadable file.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The control performs an action (submit, delete, open a dialog) — use a <strong>button</strong>, even if it should look like a link.</li><li>The target is a compact icon-only affordance — consider an <strong>icon-button</strong> or a linked icon with an accessible name.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Label</strong> (required) — descriptive link text that makes sense out of context.</li><li><strong>Destination</strong> (required) — the address the link resolves to.</li><li><strong>Affordance</strong> (required) — a visual cue (color, underline) that marks it as a link and distinguishes it from surrounding text.</li><li><strong>External/indicator</strong> (optional) — a marker for links that open in a new tab or leave the site.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default / hover / focus (visible)</strong> — with a clearly visible focus indicator.</li><li><strong>Visited</strong> (optional) — distinguishes previously followed links.</li><li><strong>Active</strong> — during activation.</li><li><strong>Current</strong> — a link to the current page is marked as current (or made inert).</li></ul>\n<p>Activation follows the destination on click and on <code>Enter</code>. Modifier-click and context-menu behaviors (open in new tab, copy address) are preserved.</p>\n<h2>Variants</h2>\n<ul><li><strong>Inline</strong> — within running text.</li><li><strong>Standalone</strong> — navigation items, calls to read more.</li><li><strong>External</strong> — opens in a new context, indicated and announced.</li><li><strong>Button-styled</strong> — visually a button but still a navigation link.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Inline links flow with text and wrap naturally; standalone links align with their navigation context. Ensure the target area is large enough to tap on touch, and that underline/spacing keeps adjacent links distinguishable.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — exposes a link role with a valid destination; a link without a destination is not a link.</li><li><strong>Keyboard</strong> — focusable in normal order; activates on <code>Enter</code>.</li><li><strong>Screen reader</strong> — the link text is meaningful on its own (avoid \"click here\"); external and new-tab behavior is announced.</li><li><strong>Focus &amp; contrast</strong> — visible focus; link text is distinguishable from body text by more than color alone.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Write descriptive link text that names the destination.</li><li>Reserve underline/link styling for actual links.</li><li>Warn when a link opens a new tab or downloads a file.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive element.</p>\n<p><strong>Used by:</strong> navbar, breadcrumb, pagination, navigation-menu, sidebar, footer, sign-in.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Use links for navigation and buttons for actions.</li><li>Keep link text meaningful out of context.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Style an action button as a link that can't be opened in a new tab.</li><li>Use \"click here\" or bare URLs as link text.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Link pattern.</li><li>Nielsen Norman Group — \"Links vs. Buttons\".</li></ul>",
    api: None,
  },
  {
    id: "logo",
    title: "Logo",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A brand mark that identifies the product or organization and usually links home.",
    tags: ["brand", "identity", "navigation", "display"],
    aliases: ["brand", "brandmark", "wordmark"],
    composedOf: [],
    usedBy: ["navbar", "footer", "sign-in"],
    related: ["icon", "link"],
    intent: "A logo is the brand's mark in the interface — a wordmark, symbol, or lockup that identifies whose product this is. Beyond identity, it plays a functional role: placed in the navbar it is the conventional \"home\" affordance users reach for, and it anchors trust on auth and marketing pages.",
    spec: "<h2>Intent</h2>\n<p>A logo is the brand's mark in the interface — a wordmark, symbol, or lockup that identifies whose product this is. Beyond identity, it plays a functional role: placed in the navbar it is the conventional \"home\" affordance users reach for, and it anchors trust on auth and marketing pages.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Establishing brand identity in a navbar, footer, or auth/marketing header.</li><li>Providing the conventional link back to the home page.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Space is so tight the mark becomes illegible — use a compact symbol-only form.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Mark</strong> (required) — the wordmark, symbol, or combined lockup.</li><li><strong>Clear space</strong> (required) — protective padding around the mark.</li><li><strong>Link target</strong> (optional) — typically home when placed in navigation.</li><li><strong>Responsive forms</strong> (optional) — full lockup vs. symbol-only.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static identity</strong> — in most placements.</li><li><strong>Interactive</strong> — when it links home, it carries link states and an accessible name.</li><li><strong>Theme variants</strong> — light/dark or monochrome forms for different surfaces.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Wordmark</strong> — the name set in the brand type.</li><li><strong>Symbol / icon</strong> — the mark alone, for compact spaces.</li><li><strong>Lockup</strong> — symbol plus wordmark.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The logo sits at the leading edge of headers with protected clear space. On small screens it may reduce to a symbol-only form. Maintain minimum size for legibility and never distort the mark.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Accessible name</strong> — provides the brand/organization name as its text alternative (e.g. \"Acme home\" when it links home).</li><li><strong>Interactive</strong> — when linking, exposes a link role and is keyboard operable.</li><li><strong>Contrast</strong> — legible against its surface; provide theme-appropriate variants.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Use the approved mark and clear space; don't recolor or distort it.</li><li>When it links home, name it accordingly.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a brand asset (a specialized image/icon).</p>\n<p><strong>Used by:</strong> navbar, footer, sign-in.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Preserve clear space and minimum size.</li><li>Give a linked logo an accessible name.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Stretch, recolor, or crowd the mark.</li><li>Rely on the logo alone for navigation without a name.</li></ul>\n<h2>References</h2>\n<ul><li>WAI — text alternatives for images and logos.</li></ul>",
    api: None,
  },
  {
    id: "input-otp",
    title: "One-Time-Code Input",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A segmented field for entering a short verification code one character per slot.",
    tags: ["form", "field", "control", "verification", "security"],
    aliases: ["otp-input", "pin-input", "code-input", "verification-code"],
    composedOf: [],
    usedBy: ["form", "sign-in", "dialog"],
    related: ["input", "field"],
    intent: "A one-time-code input collects a short verification code (from SMS, email, or an authenticator) with one visible slot per character. The segmented layout makes the expected length obvious, eases review, and pairs naturally with auto-advance and paste of the full code.",
    spec: "<h2>Intent</h2>\n<p>A one-time-code input collects a short verification code (from SMS, email, or an authenticator) with one visible slot per character. The segmented layout makes the expected length obvious, eases review, and pairs naturally with auto-advance and paste of the full code.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Entering a fixed-length numeric or alphanumeric verification code.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The value is free-form or variable length — use a plain <strong>input</strong>.</li><li>A long secret is being entered — use a password input.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Slots</strong> (required) — one per character, fixed count.</li><li><strong>Active-slot indicator</strong> (required) — shows where input lands next.</li><li><strong>Separator</strong> (optional) — groups slots (e.g. 3-3).</li><li><strong>Validation message</strong> (conditional).</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Empty / partially filled / complete</strong> — with the active slot highlighted.</li><li><strong>Auto-advance</strong> — typing moves focus forward; backspace moves back.</li><li><strong>Paste</strong> — pasting the full code distributes characters across slots.</li><li><strong>Invalid</strong> — wrong or expired code, with a clear message.</li><li><strong>Disabled / loading</strong> — during verification.</li></ul>\n<p>Completing the last slot may auto-submit.</p>\n<h2>Variants</h2>\n<ul><li><strong>Numeric vs. alphanumeric</strong>.</li><li><strong>Grouped</strong> — visual separators between segments.</li><li><strong>Masked</strong> — obscured characters for sensitive codes.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Slots sit in a single row, evenly spaced, centered in their context. On mobile the numeric variant surfaces a numeric keypad and integrates with OS code auto-fill. Keep slots large enough to tap.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — type, backspace, and arrow between slots; paste supported.</li><li><strong>Semantics</strong> — the control has a single accessible name describing the code; slots are not announced as separate meaningless fields.</li><li><strong>Screen reader</strong> — announces progress and the active position; errors are associated and announced.</li><li><strong>Autofill</strong> — supports platform one-time-code autofill where available.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>State where the code was sent and its length/expiry.</li><li>Provide a resend affordance and clear expiry messaging.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a specialized primitive.</p>\n<p><strong>Used by:</strong> form, sign-in, dialog (verification steps).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Support paste and OS autofill of the whole code.</li><li>Auto-advance and allow easy correction.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Force character-by-character typing without paste.</li><li>Hide why a code was rejected.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Labels or Instructions; platform one-time-code autofill guidance.</li></ul>",
    api: None,
  },
  {
    id: "progress",
    title: "Progress",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A visual indicator of how far a task has advanced toward completion.",
    tags: ["feedback", "status", "loading", "progress"],
    aliases: ["progress-bar", "meter", "loader-bar"],
    composedOf: [],
    usedBy: ["form", "card", "dialog", "data-table", "sidebar"],
    related: ["spinner", "skeleton", "slider"],
    intent: "A progress indicator communicates that a task is underway and, when known, how much remains. It reassures the user that work is happening and sets expectations for how long they must wait.",
    spec: "<h2>Intent</h2>\n<p>A progress indicator communicates that a task is underway and, when known, how much remains. It reassures the user that work is happening and sets expectations for how long they must wait.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Showing determinate progress of a measurable task (upload, import, steps).</li><li>Showing indeterminate activity when duration is unknown.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The wait is tiny and localized to a control — a <strong>spinner</strong> fits better.</li><li>You are showing the shape of not-yet-loaded content — use a <strong>skeleton</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Track</strong> (required) — the full extent.</li><li><strong>Indicator</strong> (required) — the filled portion (determinate) or moving pattern (indeterminate).</li><li><strong>Value label</strong> (optional) — percentage or \"x of y\".</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Determinate</strong> — indicator reflects a known 0–100% value that advances.</li><li><strong>Indeterminate</strong> — continuous motion signals ongoing, unmeasured work.</li><li><strong>Complete</strong> — reaches full and is typically replaced by a result state.</li><li><strong>Error / paused</strong> (optional) — communicates a stall or failure.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Linear bar</strong> — horizontal fill.</li><li><strong>Circular / radial</strong> — ring fill for compact contexts.</li><li><strong>Stepped</strong> — segmented for discrete stages.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Linear bars span the width of their container; circular variants keep a fixed diameter. Pair with a concise value or status label when precise expectations matter. Avoid layout shift when the indicator appears or completes.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — exposes a progressbar role with min/max/now for determinate progress; indeterminate omits the value but conveys busy state.</li><li><strong>Screen reader</strong> — announces meaningful changes without spamming updates.</li><li><strong>Non-color</strong> — completion and error are conveyed by more than color.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Prefer concrete labels (\"Uploading 3 of 10\") over bare percentages when helpful.</li><li>Keep status copy short and reassuring.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive element.</p>\n<p><strong>Used by:</strong> form (submission), card, dialog, data-table, sidebar (usage meters).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Use determinate progress whenever the total is known.</li><li>Announce completion and errors accessibly.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Fake precise percentages you cannot measure.</li><li>Use a full progress bar for a sub-second action.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Meter and progressbar guidance.</li></ul>",
    api: None,
  },
  {
    id: "radio-group",
    title: "Radio Group",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A set of mutually exclusive options from which exactly one can be selected.",
    tags: ["form", "control", "selection", "single-choice"],
    aliases: ["radio", "radio-buttons", "option-group"],
    composedOf: ["label"],
    usedBy: ["form", "field", "settings"],
    related: ["checkbox", "select", "toggle-group", "switch"],
    intent: "A radio group presents a small set of mutually exclusive options and lets the user choose exactly one. Showing all options at once makes the full set of choices visible and comparable, which aids decisions among a few alternatives.",
    spec: "<h2>Intent</h2>\n<p>A radio group presents a small set of mutually exclusive options and lets the user choose exactly one. Showing all options at once makes the full set of choices visible and comparable, which aids decisions among a few alternatives.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Exactly one choice must be made from roughly two to five visible options.</li><li>Seeing all options at once helps the user decide.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Many options would overwhelm the layout — use a <strong>select</strong> or <strong>combobox</strong>.</li><li>Multiple selections are allowed — use <strong>checkboxes</strong>.</li><li>The choice is binary and instantly applied — use a <strong>switch</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Group container</strong> (required) — with a group label describing the decision.</li><li><strong>Options</strong> (required) — each a control plus a label.</li><li><strong>Selected indicator</strong> (required) — marks the current choice.</li><li><strong>Per-option description</strong> (optional).</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Unselected group</strong> — no option chosen (avoid unless intentionally optional).</li><li><strong>Selected</strong> — exactly one option marked; choosing another clears the previous.</li><li><strong>Disabled</strong> — whole group or individual options.</li><li><strong>Invalid</strong> — required group with no selection.</li></ul>\n<p>Arrow keys move selection within the group; the group is a single tab stop.</p>\n<h2>Variants</h2>\n<ul><li><strong>Vertical list</strong> — default, one option per row.</li><li><strong>Horizontal</strong> — for very short option sets.</li><li><strong>Card / segmented</strong> — richer options with descriptions or as a segmented control.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Options stack vertically for scannability; horizontal layouts only suit two or three short labels and should wrap gracefully. Align indicators and labels consistently; the full row is the target.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — one tab stop for the group; arrow keys change selection; <code>Space</code> selects the focused option.</li><li><strong>Semantics</strong> — radiogroup with a group label; each option exposes a radio role and state.</li><li><strong>Screen reader</strong> — announces group name, option label, position, and selection.</li><li><strong>Focus</strong> — visible focus on the active option.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Give the group a clear question or noun as its label.</li><li>Keep option labels parallel and mutually exclusive in meaning.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> label (group and per-option).</p>\n<p><strong>Used by:</strong> form, field, settings.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Provide a group label describing the decision.</li><li>Preselect a safe default when appropriate.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use radios for multi-select.</li><li>Cram a long list into radios.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Radio Group pattern.</li></ul>",
    api: None,
  },
  {
    id: "scroll-area",
    title: "Scroll Area",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A container that scrolls overflowing content with consistent, styleable scrollbars.",
    tags: ["layout", "scroll", "container", "overflow"],
    aliases: ["scroll-container", "scrollbox"],
    composedOf: [],
    usedBy: ["dropdown-menu", "command", "sidebar", "dialog", "data-table"],
    related: ["separator", "resizable", "aspect-ratio"],
    intent: "A scroll area confines overflowing content to a bounded region with predictable, consistently-styled scrollbars across platforms. It keeps long lists, menus, and panels contained without letting the whole page scroll.",
    spec: "<h2>Intent</h2>\n<p>A scroll area confines overflowing content to a bounded region with predictable, consistently-styled scrollbars across platforms. It keeps long lists, menus, and panels contained without letting the whole page scroll.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A region's content can exceed its bounds (long menus, chat logs, side panels).</li><li>You need consistent scrollbar appearance and behavior across platforms.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The main page should scroll naturally — don't trap the primary document.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Viewport</strong> (required) — the clipped, scrollable region.</li><li><strong>Content</strong> (required) — may exceed the viewport in one or both axes.</li><li><strong>Scrollbar(s) &amp; thumb</strong> (required when overflowing) — indicate and control position.</li><li><strong>Edge affordance</strong> (optional) — fades/shadows hinting more content.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>No overflow</strong> — scrollbars hidden.</li><li><strong>Overflowing</strong> — scrollbars appear; thumb reflects position and proportion.</li><li><strong>Scrolling</strong> — content moves; sticky headers may remain pinned.</li></ul>\n<p>Keyboard and wheel/trackpad scrolling both work; focus moving to an off-screen item scrolls it into view.</p>\n<h2>Variants</h2>\n<ul><li><strong>Vertical / horizontal / both</strong>.</li><li><strong>Overlay vs. inset</strong> scrollbars.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The area takes a bounded size from its context and scrolls the remainder. On touch, native momentum scrolling is preserved; scrollbars may be thinner or overlaid. Avoid nested scroll traps that confuse gesture direction.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — the region is reachable and scrollable by keyboard; focusable descendants scroll into view.</li><li><strong>Semantics</strong> — a scrollable region is discoverable; it is labeled when its content warrants.</li><li><strong>Focus</strong> — never hide focused content behind the viewport edge.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Hint at more content with an edge fade when discovery matters.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a layout primitive.</p>\n<p><strong>Used by:</strong> dropdown-menu, command, sidebar, dialog, data-table.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep focused items visible by scrolling them into view.</li><li>Provide affordances that content continues beyond the fold.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Trap the primary page scroll.</li><li>Nest conflicting scroll regions.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Reflow; keyboard operability of scrollable regions.</li></ul>",
    api: None,
  },
  {
    id: "separator",
    title: "Separator",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A thin divider that visually and semantically separates groups of content.",
    tags: ["layout", "divider", "structure"],
    aliases: ["divider", "rule", "hr"],
    composedOf: [],
    usedBy: ["dropdown-menu", "menubar", "card", "sidebar", "navbar", "list"],
    related: ["scroll-area", "aspect-ratio"],
    intent: "A separator divides content into distinct groups, giving structure and rhythm without adding a heavier container. It clarifies where one set of items ends and another begins.",
    spec: "<h2>Intent</h2>\n<p>A separator divides content into distinct groups, giving structure and rhythm without adding a heavier container. It clarifies where one set of items ends and another begins.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Grouping items within a menu, list, toolbar, or card.</li><li>Providing a light visual break between sections.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Whitespace alone already communicates the grouping.</li><li>You need an interactive boundary — use a <strong>resizable</strong> handle.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Line</strong> (required) — horizontal or vertical rule.</li><li><strong>Optional label</strong> (optional) — a caption breaking the line (e.g. \"OR\").</li></ul>\n<h2>States &amp; behavior</h2>\n<p>Separators are static. A labeled separator centers text within the rule. Decorative separators carry no semantics; meaningful ones expose a separator role.</p>\n<h2>Variants</h2>\n<ul><li><strong>Horizontal</strong> — between stacked sections.</li><li><strong>Vertical</strong> — between inline items in a toolbar or nav.</li><li><strong>Labeled</strong> — divider with centered text.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Horizontal separators span their container's content width; vertical separators match the height of adjacent inline items. Orientation may switch when a layout reflows from row to column.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — purely decorative separators are hidden from assistive tech; meaningful ones expose a separator role with orientation.</li><li><strong>Screen reader</strong> — does not announce redundant dividers.</li><li><strong>Contrast</strong> — as non-text, keep it perceivable without being loud.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Use labeled separators sparingly; keep labels to a word or two.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive element.</p>\n<p><strong>Used by:</strong> dropdown-menu, menubar, card, sidebar, navbar, lists.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Prefer spacing first; add a separator when grouping needs reinforcing.</li><li>Mark decorative separators as such.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Stack multiple separators to create heavy borders.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Separator role.</li></ul>",
    api: None,
  },
  {
    id: "skeleton",
    title: "Skeleton",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A placeholder that mirrors the shape of content while it loads.",
    tags: ["loading", "placeholder", "feedback", "perceived-performance"],
    aliases: ["placeholder", "shimmer", "loading-placeholder"],
    composedOf: [],
    usedBy: ["card", "data-table", "list", "avatar", "dashboard"],
    related: ["spinner", "progress"],
    intent: "A skeleton stands in for content that hasn't loaded yet, echoing its eventual layout. By reserving space in the real shape, it reduces perceived wait and prevents layout shift when the content arrives.",
    spec: "<h2>Intent</h2>\n<p>A skeleton stands in for content that hasn't loaded yet, echoing its eventual layout. By reserving space in the real shape, it reduces perceived wait and prevents layout shift when the content arrives.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Content has a predictable layout and takes a noticeable moment to load.</li><li>You want to avoid a jarring jump when data fills in.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The wait is trivial — no indicator is needed.</li><li>Duration matters and is measurable — use <strong>progress</strong>.</li><li>Layout is unknown — a <strong>spinner</strong> is simpler.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Placeholder blocks</strong> (required) — neutral shapes matching text lines, media, and controls of the real content.</li><li><strong>Motion</strong> (optional) — a subtle shimmer or pulse signaling activity.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Loading</strong> — placeholders shown, optionally animated.</li><li><strong>Resolved</strong> — replaced by the real content in the same footprint.</li></ul>\n<p>Match the count and rough proportions of the real content to avoid a visible jump.</p>\n<h2>Variants</h2>\n<ul><li><strong>Text lines</strong> — bars of varying width.</li><li><strong>Media</strong> — rectangles/circles for images and avatars.</li><li><strong>Composite</strong> — a full card or row skeleton.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Skeletons occupy the same box dimensions the real content will, so the layout is stable before and after load. They adapt to the responsive layout exactly as the content does.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — the loading region conveys a busy state; placeholders are not announced as meaningful content.</li><li><strong>Screen reader</strong> — communicate \"loading\" via a status, not a wall of empty elements.</li><li><strong>Motion</strong> — respect reduced-motion preferences for the shimmer.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep placeholders neutral; don't imply data that may not appear.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive element.</p>\n<p><strong>Used by:</strong> card, data-table, list, avatar, dashboard.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Match the real content's shape and count.</li><li>Reserve exact space to prevent layout shift.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Animate aggressively or ignore reduced-motion.</li><li>Use skeletons for very long or unbounded waits without any progress cue.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — skeleton screens and perceived performance.</li></ul>",
    api: None,
  },
  {
    id: "slider",
    title: "Slider",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A control for choosing a value or range by dragging a handle along a track.",
    tags: ["form", "control", "range", "input"],
    aliases: ["range", "range-slider"],
    composedOf: [],
    usedBy: ["form", "field", "settings", "data-table"],
    related: ["input", "progress", "switch"],
    intent: "A slider lets a person pick a value from a continuous or stepped range by moving a handle along a track. It suits choices where the approximate position matters more than exact typing, and where seeing the value's place within the range is useful.",
    spec: "<h2>Intent</h2>\n<p>A slider lets a person pick a value from a continuous or stepped range by moving a handle along a track. It suits choices where the approximate position matters more than exact typing, and where seeing the value's place within the range is useful.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Choosing from a bounded range where relative position is meaningful (volume, brightness, price range).</li><li>A rough, quickly-adjustable value is acceptable.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Precision matters and the range is large — a numeric <strong>input</strong> is better.</li><li>Options are discrete categories — use a <strong>radio-group</strong> or <strong>select</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Track</strong> (required) — the full range.</li><li><strong>Filled range</strong> (required) — from start (or lower thumb) to the value.</li><li><strong>Thumb(s)</strong> (required) — one for a single value, two for a range.</li><li><strong>Value label / tooltip</strong> (optional) — current value on interaction.</li><li><strong>Ticks / marks</strong> (optional) — for stepped ranges.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default / focus / active</strong> — with a visible focus indicator on the thumb.</li><li><strong>Dragging</strong> — value updates live; may snap to steps.</li><li><strong>Disabled</strong> — not interactive.</li><li><strong>Range</strong> — two thumbs define lower and upper bounds that cannot cross.</li></ul>\n<p>Keyboard arrows adjust by step; Home/End jump to bounds.</p>\n<h2>Variants</h2>\n<ul><li><strong>Single value</strong> — one thumb.</li><li><strong>Range</strong> — two thumbs.</li><li><strong>Stepped</strong> — snaps to discrete increments with optional marks.</li><li><strong>Vertical</strong> — for contexts like volume faders.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The track spans its container; the current value is often shown adjacent or on a tooltip during interaction. Ensure the thumb hit area is comfortably large on touch, larger than its visual size if needed.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — focusable thumb; arrows change by step, Home/End to bounds; for ranges each thumb is operable.</li><li><strong>Semantics</strong> — slider role with min/max/now and a text value when the number alone is ambiguous.</li><li><strong>Screen reader</strong> — announces the value (and unit) as it changes.</li><li><strong>Focus &amp; target</strong> — visible focus; generous touch target.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Show units with the value (\"$40\", \"70%\").</li><li>Label what the slider controls.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive control.</p>\n<p><strong>Used by:</strong> form, field, settings, data-table (range filters).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Provide keyboard control and a visible value.</li><li>Use steps and marks when discrete values are expected.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a slider when exact input is essential.</li><li>Make thumbs too small to grab on touch.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Slider and Slider (Multi-Thumb) patterns.</li></ul>",
    api: None,
  },
  {
    id: "spinner",
    title: "Spinner",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A compact, indeterminate indicator that signals a short, ongoing wait.",
    tags: ["loading", "feedback", "indeterminate", "status"],
    aliases: ["loader", "busy-indicator", "activity-indicator"],
    composedOf: [],
    usedBy: ["button", "form", "card", "dialog", "data-table"],
    related: ["progress", "skeleton"],
    intent: "A spinner signals that something is happening and the user should wait, without claiming to know how long. It is the lightest-weight loading cue — ideal inline, inside buttons, or over a small region during a brief, unmeasured wait.",
    spec: "<h2>Intent</h2>\n<p>A spinner signals that something is happening and the user should wait, without claiming to know how long. It is the lightest-weight loading cue — ideal inline, inside buttons, or over a small region during a brief, unmeasured wait.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Indicating a short, indeterminate wait for an action or a small region.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Duration/steps are measurable — use <strong>progress</strong>.</li><li>Content has a known layout that's loading — use a <strong>skeleton</strong>.</li><li>The wait is long — provide progress or explanatory context instead.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Animated indicator</strong> (required) — a rotating or pulsing motif.</li><li><strong>Optional label</strong> (optional) — \"Loading…\" for context.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Active</strong> — animating while work is in flight.</li><li><strong>Resolved</strong> — removed and replaced by the result (content, success, or error).</li></ul>\n<p>Prefer a brief delay before showing a spinner to avoid flicker on fast responses.</p>\n<h2>Variants</h2>\n<ul><li><strong>Inline</strong> — within a button or next to text.</li><li><strong>Region overlay</strong> — centered over a loading area.</li><li><strong>Sizes</strong> — from a defined scale.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Spinners keep a fixed size and center within their context. When replacing a button's label, preserve the button's dimensions to avoid layout shift.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — convey a busy/loading status to assistive tech (a live status message), not just a spinning graphic.</li><li><strong>Screen reader</strong> — announces that content is loading and, ideally, when it finishes.</li><li><strong>Motion</strong> — respect reduced-motion; keep the animation calm.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Add a short \"Loading…\" label where context helps.</li><li>Don't pair a spinner with a fake percentage.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive element.</p>\n<p><strong>Used by:</strong> button (loading), form, card, dialog, data-table.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Reserve space so the layout doesn't jump.</li><li>Announce busy/loaded states accessibly.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a spinner for long or measurable waits.</li><li>Show it so briefly it flickers.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA — status/live region guidance; WCAG reduced motion.</li></ul>",
    api: None,
  },
  {
    id: "switch",
    title: "Switch",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A control that toggles a single setting on or off with an immediate effect.",
    tags: ["form", "control", "toggle", "boolean", "setting"],
    aliases: ["toggle-switch", "on-off"],
    composedOf: ["label"],
    usedBy: ["form", "field", "settings", "sidebar"],
    related: ["checkbox", "toggle"],
    intent: "A switch turns a single setting on or off, taking effect immediately like a physical light switch. Its value is the instant, self-evident nature of the change — no separate submit step.",
    spec: "<h2>Intent</h2>\n<p>A switch turns a single setting on or off, taking effect immediately like a physical light switch. Its value is the instant, self-evident nature of the change — no separate submit step.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Toggling a binary setting that applies right away (notifications on/off).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The choice is part of a form applied on submit — use a <strong>checkbox</strong>.</li><li>More than two states are possible — use a <strong>radio-group</strong> or <strong>select</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Track</strong> (required) — the on/off channel.</li><li><strong>Thumb</strong> (required) — slides between positions.</li><li><strong>Label</strong> (required) — names the setting.</li><li><strong>State text</strong> (optional) — explicit on/off wording.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Off / on</strong> — toggled on activation, effect applied immediately.</li><li><strong>Disabled</strong> — not interactive.</li><li><strong>Pending</strong> (optional) — brief in-flight state if the change is async, with rollback on failure.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>With inline label</strong> — label beside the switch.</li><li><strong>With description</strong> — supporting text under the label.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The switch aligns to its label on a shared baseline, commonly with the switch at the trailing edge of a settings row. The whole row can be the target.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — focusable; <code>Space</code>/<code>Enter</code> toggles.</li><li><strong>Semantics</strong> — switch role with on/off state, or a checkbox exposing the same.</li><li><strong>Screen reader</strong> — announces label and on/off state.</li><li><strong>Focus</strong> — visible focus indicator.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Label the setting, not the action (\"Email notifications\", not \"Enable\").</li><li>Avoid relying on the thumb position alone; expose state to assistive tech.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> label.</p>\n<p><strong>Used by:</strong> form, field, settings, sidebar.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Use for instantly-applied binary settings.</li><li>Reflect async success/failure honestly.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a switch where a submit step is expected.</li><li>Depend on color alone to show on/off.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Switch pattern.</li></ul>",
    api: None,
  },
  {
    id: "input",
    title: "Text Input",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A single-line field that lets a person enter and edit a short piece of text.",
    tags: ["form", "field", "control", "interactive", "text"],
    aliases: ["textfield", "text-field", "field"],
    composedOf: [],
    usedBy: ["form", "navbar", "sign-in", "landing-page"],
    related: ["button", "textarea", "select", "checkbox"],
    intent: "A text input captures a short, free-form value from the user — a name, an email, a search query. It is the workhorse of data entry: a labelled field the user can focus, type into, correct, and submit. A good input makes its purpose, constraints, and current validity obvious at every moment.",
    spec: "<h2>Intent</h2>\n<p>A text input captures a short, free-form value from the user — a name, an email, a search query. It is the workhorse of data entry: a labelled field the user can focus, type into, correct, and submit. A good input makes its purpose, constraints, and current validity obvious at every moment.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Collecting a short, single-line value that the user types.</li><li>The set of acceptable values is open-ended or too large to enumerate.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The value is long or multi-line — use a <strong>textarea</strong>.</li><li>There is a small, fixed set of options — use a <strong>select</strong>, radio group, or checkboxes.</li><li>The value is boolean — use a <strong>checkbox</strong> or <strong>switch</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Label</strong> (required) — a persistent, programmatically associated name for the field.</li><li><strong>Field / control</strong> (required) — the editable area that holds the value.</li><li><strong>Placeholder</strong> (optional) — an example format; never a replacement for the label.</li><li><strong>Leading/trailing adornment</strong> (optional) — icon, prefix, unit, or an inline action such as a clear or reveal-password button.</li><li><strong>Helper text</strong> (optional) — guidance about format or constraints.</li><li><strong>Validation message</strong> (conditional) — replaces or accompanies helper text when the value is invalid.</li><li><strong>Character/limit counter</strong> (optional) — when a max length applies.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Empty</strong> — awaiting input; may show a placeholder.</li><li><strong>Focus</strong> — active for typing, with a visible focus indicator.</li><li><strong>Filled</strong> — contains a value.</li><li><strong>Disabled</strong> — not editable and not focusable.</li><li><strong>Read-only</strong> — value visible and selectable but not editable.</li><li><strong>Invalid / error</strong> — fails validation; shows a message describing how to fix it.</li><li><strong>Valid / success</strong> (optional) — confirms a value that required verification.</li></ul>\n<p>Validate at sensible moments (typically on blur or submit, not on every keystroke) to avoid punishing users mid-entry. Preserve the user's input when showing errors — never clear the field.</p>\n<h2>Variants</h2>\n<ul><li><strong>Plain text</strong> — default free text.</li><li><strong>Typed</strong> — email, tel, url, number, and similar, which adjust keyboard, inline validation, and formatting.</li><li><strong>Password</strong> — obscured entry with an optional reveal toggle.</li><li><strong>Search</strong> — often with a leading search icon and a clear affordance.</li><li><strong>With adornment</strong> — currency, units, or inline actions attached to the field.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Inputs typically span the width of their container or form column so the target is easy to hit and the expected value length is legible. The label sits above the field for the most reliable reading order and wrapping behavior. Group related fields and keep a consistent vertical rhythm. On small screens, fields go full-width and typed variants surface the appropriate on-screen keyboard.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — focusable in tab order; standard text-editing keys work.</li><li><strong>Semantics</strong> — the label is programmatically associated with the field; required, invalid, and described-by relationships are exposed.</li><li><strong>Screen reader</strong> — announces label, current value, required state, and any active validation message.</li><li><strong>Focus</strong> — a visible focus indicator is required.</li><li><strong>Errors</strong> — error messages are associated with the field and conveyed by text and/or icon, never color alone.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Labels are short nouns describing the value (\"Email\", \"Full name\"), not instructions.</li><li>Put format guidance in helper text, not the placeholder.</li><li>Write validation messages as specific, actionable fixes: \"Enter a valid email address\", not \"Invalid input\".</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a text input is a primitive element. (Its optional inline actions may reference the <strong>button</strong> element.)</p>\n<p><strong>Used by:</strong> form, navbar (search), sign-in, landing-page (lead capture).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Always provide a visible, associated label.</li><li>Keep the user's input when validation fails and explain the fix.</li><li>Choose the typed variant that matches the data to improve entry.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use the placeholder as the only label.</li><li>Validate aggressively on every keystroke for fields still being typed.</li><li>Signal errors with color alone.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — form and text field guidance.</li><li>WCAG — Labels or Instructions; Error Identification &amp; Suggestion.</li></ul>",
    api: None,
  },
  {
    id: "textarea",
    title: "Textarea",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A multi-line field for entering and editing longer free-form text.",
    tags: ["form", "field", "control", "text", "multiline"],
    aliases: ["multiline-input", "text-area"],
    composedOf: [],
    usedBy: ["form", "field", "dialog", "card"],
    related: ["input", "label", "field"],
    intent: "A textarea captures longer, multi-line text — a comment, description, or message. It gives the user room to write and review several lines at once, unlike the single-line input.",
    spec: "<h2>Intent</h2>\n<p>A textarea captures longer, multi-line text — a comment, description, or message. It gives the user room to write and review several lines at once, unlike the single-line input.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Collecting multi-line or lengthy free text (messages, notes, descriptions).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The value is short and single-line — use an <strong>input</strong>.</li><li>The content is rich/formatted — use a dedicated rich-text editor pattern.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Label</strong> (required) — names the field.</li><li><strong>Editable area</strong> (required) — multi-line region.</li><li><strong>Helper text</strong> (optional) — format or length guidance.</li><li><strong>Character counter</strong> (optional) — when a limit applies.</li><li><strong>Resize handle</strong> (optional) — manual or auto-growing height.</li><li><strong>Validation message</strong> (conditional).</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Empty / focus / filled</strong> — as with inputs, with a visible focus indicator.</li><li><strong>Auto-grow</strong> (optional) — expands with content up to a max, then scrolls.</li><li><strong>Disabled / read-only / invalid</strong> — standard field states.</li></ul>\n<p>Preserve input on validation errors; validate on blur/submit, not each keystroke.</p>\n<h2>Variants</h2>\n<ul><li><strong>Fixed height</strong> — scrolls when overflowing.</li><li><strong>Auto-resizing</strong> — grows to fit content.</li><li><strong>With counter</strong> — enforces or surfaces a max length.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Textareas typically span the form column width with a sensible default number of visible rows. Auto-growing variants prevent the page from jumping while keeping a maximum height. On small screens they remain full-width.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — focusable; standard multi-line editing keys work.</li><li><strong>Semantics</strong> — label associated; required/invalid/described-by exposed.</li><li><strong>Screen reader</strong> — announces label, value, and validation messages.</li><li><strong>Focus</strong> — visible focus indicator.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Set expectations for length in helper text, not the placeholder.</li><li>Write actionable validation messages.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive control (often wrapped by <strong>field</strong>).</p>\n<p><strong>Used by:</strong> form, field, dialog, card.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Give enough default height for the expected input.</li><li>Show a counter when a limit is enforced.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a textarea for single-line values.</li><li>Clear the user's text when showing an error.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Labels or Instructions; Error Identification.</li></ul>",
    api: None,
  },
  {
    id: "toggle",
    title: "Toggle",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A two-state button that can be turned on or off, often used in toolbars.",
    tags: ["control", "button", "state", "toolbar"],
    aliases: ["toggle-button", "pressed-button"],
    composedOf: [],
    usedBy: ["toggle-group", "toolbar", "menubar"],
    related: ["toggle-group", "switch", "button", "checkbox"],
    intent: "A toggle is a button that stays pressed or unpressed, representing an on/off state for a formatting or view option — bold text, grid vs. list, mute. Unlike a switch (a setting) it reads as a momentary tool that happens to remember its state, which is why it lives naturally in toolbars.",
    spec: "<h2>Intent</h2>\n<p>A toggle is a button that stays pressed or unpressed, representing an on/off state for a formatting or view option — bold text, grid vs. list, mute. Unlike a switch (a setting) it reads as a momentary tool that happens to remember its state, which is why it lives naturally in toolbars.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Turning a tool or view option on/off, especially in a toolbar (e.g. bold).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Toggling a persistent setting in a form — use a <strong>switch</strong> or <strong>checkbox</strong>.</li><li>Choosing one of several exclusive options — use a <strong>toggle-group</strong> or radios.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Icon and/or label</strong> (required) — what the toggle controls.</li><li><strong>Container</strong> (required) — the pressable target reflecting pressed state.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Off (unpressed) / On (pressed)</strong> — flips on activation.</li><li><strong>Hover / focus / active</strong> — with a visible focus indicator.</li><li><strong>Disabled</strong> — not interactive.</li></ul>\n<p>Activation flips the state on click and <code>Enter</code>/<code>Space</code>.</p>\n<h2>Variants</h2>\n<ul><li><strong>Icon-only</strong> — compact toolbar tool with an accessible name.</li><li><strong>Icon + label</strong> — clearer intent.</li><li><strong>Sizes / emphasis</strong> — from a defined scale.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Toggles sit inline in toolbars with consistent sizing and spacing, often grouped. Icon-only toggles must keep an adequate target size.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — focusable; <code>Enter</code>/<code>Space</code> toggles.</li><li><strong>Semantics</strong> — button with a pressed state exposed (on/off).</li><li><strong>Screen reader</strong> — announces name and pressed/not-pressed.</li><li><strong>Focus</strong> — visible focus indicator; icon-only toggles have accessible names.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Name the option the toggle controls; keep tooltips consistent with the name.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a primitive; composed into <strong>toggle-group</strong>.</p>\n<p><strong>Used by:</strong> toggle-group, toolbars, menubar.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Expose the pressed state to assistive tech.</li><li>Give icon-only toggles accessible names.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Confuse a toggle (tool state) with a switch (setting).</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Button (toggle) pattern.</li></ul>",
    api: None,
  },
  {
    id: "toggle-group",
    title: "Toggle Group",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "A set of toggles acting as a single control for single- or multi-selection.",
    tags: ["control", "toolbar", "selection", "segmented"],
    aliases: ["segmented-control", "button-group-toggle"],
    composedOf: ["toggle"],
    usedBy: ["toolbar", "menubar", "data-table"],
    related: ["toggle", "radio-group", "tabs", "button-group"],
    intent: "A toggle group binds several toggles into one control so the user can pick one option (single-select, like a segmented control) or several (multi-select, like a formatting toolbar). It communicates that the options belong together and are governed as a set.",
    spec: "<h2>Intent</h2>\n<p>A toggle group binds several toggles into one control so the user can pick one option (single-select, like a segmented control) or several (multi-select, like a formatting toolbar). It communicates that the options belong together and are governed as a set.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Grouping related on/off tools (text alignment, formatting).</li><li>Offering a compact single choice among a few options (segmented control).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Options are navigational views — use <strong>tabs</strong>.</li><li>The exclusive choice is a form field with labels — a <strong>radio-group</strong> reads clearer.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Group container</strong> (required) — with an accessible group name.</li><li><strong>Toggle items</strong> (required) — each a toggle with icon/label.</li><li><strong>Selection mode</strong> (required) — single or multiple.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Single-select</strong> — choosing one clears the others (segmented behavior).</li><li><strong>Multi-select</strong> — items toggle independently.</li><li><strong>Disabled</strong> — group or individual items.</li></ul>\n<p>Arrow keys move focus within the group; the group is a single tab stop.</p>\n<h2>Variants</h2>\n<ul><li><strong>Single (segmented)</strong> — exactly one active.</li><li><strong>Multiple</strong> — any number active.</li><li><strong>Icon / label / mixed</strong> items.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Items sit flush in a row as a unified control with shared borders/rounding at the ends. On narrow widths, allow wrapping or scrolling rather than shrinking targets below usable size.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — single tab stop; arrows move between items; <code>Space</code>/<code>Enter</code> activates.</li><li><strong>Semantics</strong> — a named group; items expose pressed (multi) or selected (single) state appropriately.</li><li><strong>Screen reader</strong> — announces group name, item, and state.</li><li><strong>Focus</strong> — visible focus on the active item.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep item labels short and parallel.</li><li>Provide accessible names for icon-only items.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> toggle.</p>\n<p><strong>Used by:</strong> toolbars, menubar, data-table (view switches).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Make selection mode obvious and consistent.</li><li>Treat the group as one control for keyboard users.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use it for navigation between views (that's tabs).</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Toolbar and toggle button guidance.</li></ul>",
    api: None,
  },
  {
    id: "typography",
    title: "Typography",
    layer: "element",
    status: "stable",
    version: "1.0.0",
    summary: "The system of text styles — headings, body, and inline elements — that gives content hierarchy and rhythm.",
    tags: ["text", "hierarchy", "content", "foundation"],
    aliases: ["text-styles", "prose", "type-scale"],
    composedOf: [],
    usedBy: ["card", "landing-page", "dialog", "form"],
    related: ["label", "separator"],
    intent: "Typography is the shared set of text styles — headings, body copy, lists, quotes, code, and inline emphasis — that gives written content clear hierarchy and comfortable reading rhythm. It is the connective tissue of nearly every screen; consistent type is what makes an interface feel coherent.",
    spec: "<h2>Intent</h2>\n<p>Typography is the shared set of text styles — headings, body copy, lists, quotes, code, and inline emphasis — that gives written content clear hierarchy and comfortable reading rhythm. It is the connective tissue of nearly every screen; consistent type is what makes an interface feel coherent.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Presenting any textual content that needs hierarchy and legibility.</li><li>Rendering long-form or authored prose (\"rich text\") consistently.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>A value is really a control's label or a status — use <strong>label</strong> or <strong>badge</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Headings</strong> (required) — a ranked scale (page title down through subsections).</li><li><strong>Body text</strong> (required) — default reading style with comfortable measure.</li><li><strong>Inline elements</strong> (optional) — emphasis, strong, links, inline code.</li><li><strong>Blocks</strong> (optional) — lists, blockquotes, code blocks, captions.</li><li><strong>Lead / muted</strong> (optional) — intro and secondary text treatments.</li></ul>\n<h2>States &amp; behavior</h2>\n<p>Mostly static. Links carry interactive states; truncated text may reveal full content on interaction. Text reflows with container width.</p>\n<h2>Variants</h2>\n<ul><li><strong>Display / heading scale</strong> — for titles and section headers.</li><li><strong>Body / lead / small / muted</strong> — reading and secondary styles.</li><li><strong>Prose block</strong> — a styled container for authored rich text.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Constrain body text to a readable measure (line length) and maintain a consistent vertical rhythm between elements. Scale headings responsively so hierarchy holds on small and large screens without overwhelming the layout.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — headings follow a logical, non-skipping order and convey real document structure, not just visual size.</li><li><strong>Readability</strong> — sufficient text contrast and a minimum comfortable size; respect user font-size and zoom.</li><li><strong>Semantics</strong> — emphasis, lists, quotes, and code use meaningful markup, not styling alone.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Use one page title; nest subheadings by meaning, not appearance.</li><li>Keep line length readable; avoid overly long measures.</li><li>Reserve emphasis for genuine emphasis.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> Not applicable — a foundational element used throughout.</p>\n<p><strong>Used by:</strong> card, landing-page, dialog, form, and essentially every archetype containing text.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Map visual scale to semantic structure.</li><li>Maintain consistent rhythm and readable measure.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Choose heading levels for size instead of hierarchy.</li><li>Convey meaning through styling alone.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Info and Relationships; Text contrast and resize.</li><li>Practical typography — measure, scale, and rhythm.</li></ul>",
    api: None,
  },
  {
    id: "accordion",
    title: "Accordion",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A stack of headers that expand and collapse to reveal or hide sections of content.",
    tags: ["disclosure", "content", "navigation", "faq"],
    aliases: ["expander", "disclosure-group"],
    composedOf: ["collapsible", "separator"],
    usedBy: ["landing-page", "settings", "sidebar"],
    related: ["collapsible", "tabs"],
    intent: "An accordion organizes related content into collapsible sections so users can scan headers and expand only what they need. It reduces long pages to a manageable overview and is a natural fit for FAQs and grouped settings.",
    spec: "<h2>Intent</h2>\n<p>An accordion organizes related content into collapsible sections so users can scan headers and expand only what they need. It reduces long pages to a manageable overview and is a natural fit for FAQs and grouped settings.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Content divides into labeled sections and users need only some at a time.</li><li>Vertical space is limited and progressive disclosure helps scanning.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Users need to compare sections side by side — use <strong>tabs</strong> or show all.</li><li>There is a single collapsible region — use a <strong>collapsible</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Items</strong> (required) — each a header plus a collapsible panel.</li><li><strong>Header/trigger</strong> (required) — labels the section and toggles it; shows an expanded/collapsed affordance.</li><li><strong>Panel</strong> (required) — the revealed content.</li><li><strong>Dividers</strong> (optional) — separate items.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Collapsed / expanded</strong> — per item, toggled by the header.</li><li><strong>Single vs. multiple open</strong> — either one item at a time or several.</li><li><strong>Disabled</strong> — an item that can't be opened.</li></ul>\n<p>Expanding animates height; the header reflects state.</p>\n<h2>Variants</h2>\n<ul><li><strong>Single-open</strong> — opening one closes the others.</li><li><strong>Multi-open</strong> — any number expanded.</li><li><strong>Bordered / flush</strong> — visual framing options.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Items stack full-width; panels push content below rather than overlaying it. Works well on small screens as a space-saving disclosure. Avoid layout jumps by animating height smoothly.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — headers are focusable and toggle on <code>Enter</code>/<code>Space</code>; arrow keys may move between headers.</li><li><strong>Semantics</strong> — each header is a button controlling its panel, exposing expanded/collapsed and the relationship to the panel.</li><li><strong>Screen reader</strong> — announces the header, state, and that it controls a region.</li><li><strong>Focus</strong> — visible focus on headers.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Write headers as concise, scannable labels or questions.</li><li>Keep panel content self-contained.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> collapsible (per item), separator.</p>\n<p><strong>Used by:</strong> landing-page (FAQ), settings, sidebar.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Let users open multiple sections when comparison helps.</li><li>Keep headers descriptive and consistent.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Hide critical content that everyone needs behind a collapse.</li><li>Nest accordions deeply.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Accordion pattern.</li></ul>",
    api: None,
  },
  {
    id: "alert",
    title: "Alert",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "An inline message that draws attention to important, contextual information.",
    tags: ["feedback", "message", "status", "inline"],
    aliases: ["callout", "banner", "notice", "inline-message"],
    composedOf: ["button", "badge"],
    usedBy: ["form", "card", "dashboard", "settings"],
    related: ["toast", "alert-dialog", "badge"],
    intent: "An alert surfaces an important message in the flow of a page — a warning, a tip, a success confirmation, or an error summary. Unlike a toast, it stays in place and in context; unlike an alert-dialog, it doesn't interrupt or block the user.",
    spec: "<h2>Intent</h2>\n<p>An alert surfaces an important message in the flow of a page — a warning, a tip, a success confirmation, or an error summary. Unlike a toast, it stays in place and in context; unlike an alert-dialog, it doesn't interrupt or block the user.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Communicating persistent, contextual status tied to a region or the page.</li><li>Summarizing form errors or highlighting a condition users should notice.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The message is transient confirmation — use a <strong>toast</strong>.</li><li>The user must decide before continuing — use an <strong>alert-dialog</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Icon</strong> (optional) — reinforces severity.</li><li><strong>Title</strong> (optional) — short headline.</li><li><strong>Message</strong> (required) — the body text.</li><li><strong>Actions</strong> (optional) — links or buttons to resolve or learn more.</li><li><strong>Dismiss</strong> (optional) — close affordance for non-critical alerts.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Severity</strong> — info, success, warning, danger, each with a non-color cue.</li><li><strong>Dismissible</strong> — can be closed; critical alerts may persist.</li><li><strong>Live</strong> — an alert that appears in response to an event announces itself.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Informational / success / warning / destructive</strong>.</li><li><strong>With actions</strong> — remediation controls.</li><li><strong>Banner</strong> — full-width, page-level placement.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Alerts span their container width, with icon, text, and actions aligned in a row that wraps gracefully on small screens. Place them near what they concern (top of a form, above a section).</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — important, time-sensitive alerts use an assertive live role; advisory ones use a polite status. Severity is conveyed by text/icon, not color alone.</li><li><strong>Screen reader</strong> — dynamically shown alerts are announced without moving focus.</li><li><strong>Keyboard</strong> — any actions and the dismiss control are reachable and operable.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Lead with what happened and what to do next.</li><li>Keep severity vocabulary consistent with badges and toasts.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button (actions), badge (optional severity tag).</p>\n<p><strong>Used by:</strong> form, card, dashboard, settings.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Match severity to real importance.</li><li>Offer a clear next step where relevant.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Overuse high-severity styling.</li><li>Rely on color alone for meaning.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Alert and status/live regions.</li></ul>",
    api: None,
  },
  {
    id: "alert-dialog",
    title: "Alert Dialog",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A blocking modal that interrupts to confirm a consequential or irreversible action.",
    tags: ["modal", "confirmation", "overlay", "destructive"],
    aliases: ["confirm-dialog", "confirmation-modal"],
    composedOf: ["dialog", "button"],
    usedBy: ["data-table", "settings", "form"],
    related: ["dialog", "alert", "toast"],
    intent: "An alert dialog interrupts the user to confirm an action that is consequential or hard to undo — deleting data, discarding changes, or canceling a running job. It demands an explicit decision before anything continues, making it distinct from a general-purpose dialog used for tasks and forms.",
    spec: "<h2>Intent</h2>\n<p>An alert dialog interrupts the user to confirm an action that is consequential or hard to undo — deleting data, discarding changes, or canceling a running job. It demands an explicit decision before anything continues, making it distinct from a general-purpose dialog used for tasks and forms.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Confirming a destructive or irreversible action.</li><li>A decision must be made before proceeding.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The task is routine or non-destructive — use a <strong>dialog</strong>.</li><li>The message is informational only — use an <strong>alert</strong> or <strong>toast</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Overlay / scrim</strong> (required) — blocks and dims the background.</li><li><strong>Title</strong> (required) — states the decision plainly.</li><li><strong>Description</strong> (required) — the consequence, especially for destructive acts.</li><li><strong>Confirm action</strong> (required) — clearly labeled, styled to match risk.</li><li><strong>Cancel action</strong> (required) — the safe way out.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Open / closed</strong> — modal while open; background inert.</li><li><strong>Pending</strong> — confirm shows progress during async work.</li><li><strong>Focus trapped</strong> — focus stays within until dismissed.</li></ul>\n<p><code>Escape</code> and the cancel action dismiss without acting. The safe option is the default focus target.</p>\n<h2>Variants</h2>\n<ul><li><strong>Confirm / cancel</strong> — standard two-action.</li><li><strong>Destructive</strong> — confirm emphasized as dangerous.</li><li><strong>Type-to-confirm</strong> — requires typing a phrase for high-risk actions.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A centered, size-constrained panel over a scrim, with actions grouped by convention (safe vs. risky). On small screens it adapts to available width while keeping actions reachable.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — an alert-dialog role with the title and description associated as its name and message.</li><li><strong>Keyboard</strong> — focus moves into the dialog on open, is trapped while open, and returns to the trigger on close; <code>Escape</code> cancels.</li><li><strong>Screen reader</strong> — announces as an alert dialog with its consequence text.</li><li><strong>Focus default</strong> — the non-destructive action receives initial focus.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Title names the action; buttons use specific verbs (\"Delete\", \"Discard\"), not \"Yes/No\".</li><li>Describe the consequence and whether it can be undone.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> dialog (base overlay/modality), button (actions).</p>\n<p><strong>Used by:</strong> data-table (row deletion), settings, form (discard changes).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Default focus to the safe choice.</li><li>Name actions by their outcome.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use blocking confirmation for trivial actions.</li><li>Make the destructive action the easiest to hit by accident.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Alert Dialog pattern.</li></ul>",
    api: None,
  },
  {
    id: "breadcrumb",
    title: "Breadcrumb",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A trail showing the current location within a hierarchy and the path back to the top.",
    tags: ["navigation", "wayfinding", "hierarchy"],
    aliases: ["breadcrumbs", "path-nav"],
    composedOf: ["link", "separator", "dropdown-menu"],
    usedBy: ["dashboard", "settings", "data-table"],
    related: ["navbar", "pagination", "tabs", "sidebar"],
    intent: "A breadcrumb shows where the current page sits within a hierarchy and offers a one-click path back to any ancestor. It answers \"where am I and how do I get back,\" reinforcing structure in deep or nested products.",
    spec: "<h2>Intent</h2>\n<p>A breadcrumb shows where the current page sits within a hierarchy and offers a one-click path back to any ancestor. It answers \"where am I and how do I get back,\" reinforcing structure in deep or nested products.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Content is hierarchical and users navigate several levels deep.</li><li>Users benefit from seeing and jumping to ancestor levels.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The site is flat or the hierarchy is shallow — breadcrumbs add little.</li><li>The list represents peers, not ancestry — use <strong>tabs</strong> or nav.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Items</strong> (required) — ancestor links from root to parent.</li><li><strong>Separators</strong> (required) — between items.</li><li><strong>Current page</strong> (required) — the last item, non-linking.</li><li><strong>Overflow menu</strong> (optional) — collapses middle levels when the trail is long.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default</strong> — ancestors are links; the current item is inert.</li><li><strong>Overflow</strong> — middle items collapse into a menu on narrow widths.</li><li><strong>Hover/focus</strong> — standard link states on ancestors.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Full trail</strong> — all levels shown.</li><li><strong>Collapsed</strong> — root + overflow + current.</li><li><strong>With icons</strong> — small icons per level.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A single horizontal line reading root → current. When it exceeds available width, collapse middle items behind an overflow control rather than wrapping or truncating the current item. Keep the current page always visible.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a navigation landmark labeled as breadcrumb; the current item is marked as current.</li><li><strong>Keyboard</strong> — all links reachable and operable in order.</li><li><strong>Screen reader</strong> — announces the trail and current location.</li><li><strong>Separators</strong> — decorative separators are not announced as content.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Use the same titles as the destinations.</li><li>Keep labels short; the current page matches the page title.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> link, separator, dropdown-menu (overflow).</p>\n<p><strong>Used by:</strong> dashboard, settings, data-table detail views.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Mark the current page and keep it visible.</li><li>Collapse gracefully when the trail is long.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use breadcrumbs for a flat structure.</li><li>Link the current page to itself.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Breadcrumb pattern.</li></ul>",
    api: None,
  },
  {
    id: "button-group",
    title: "Button Group",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A set of related buttons joined into a single visual unit.",
    tags: ["action", "layout", "toolbar", "grouping"],
    aliases: ["btn-group", "segmented-buttons", "split-button"],
    composedOf: ["button", "dropdown-menu", "separator"],
    usedBy: ["toolbar", "data-table", "card", "dialog"],
    related: ["toggle-group", "dropdown-menu", "button"],
    intent: "A button group visually joins related buttons into one unit to signal that they belong together — a set of related actions, or a primary action paired with a menu of alternatives (a split button). It clarifies relationships and conserves space in toolbars and action areas.",
    spec: "<h2>Intent</h2>\n<p>A button group visually joins related buttons into one unit to signal that they belong together — a set of related actions, or a primary action paired with a menu of alternatives (a split button). It clarifies relationships and conserves space in toolbars and action areas.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Presenting a small set of related actions as a cohesive cluster.</li><li>Pairing a default action with secondary options (split button).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The buttons are unrelated — space them separately.</li><li>The group represents selectable state — use a <strong>toggle-group</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Buttons</strong> (required) — two or more related actions with shared end-rounding.</li><li><strong>Split trigger</strong> (optional) — an attached menu button revealing more actions.</li><li><strong>Dividers</strong> (optional) — subtle separation between members.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default</strong> — each button behaves as a normal button.</li><li><strong>Split button</strong> — the main button runs the default action; the attached trigger opens a dropdown of alternatives.</li><li><strong>Disabled</strong> — individual members or the whole group.</li></ul>\n<p>Members are independent controls, not a single selection.</p>\n<h2>Variants</h2>\n<ul><li><strong>Grouped actions</strong> — several peer buttons.</li><li><strong>Split button</strong> — default action + menu.</li><li><strong>Attached vs. spaced</strong> — joined seam or subtle gaps.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Members sit flush with shared borders and rounded outer corners, forming one shape. On narrow widths, allow wrapping or collapse less-used actions into an attached menu. Keep targets adequately sized.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a labeled group of buttons; the split menu button exposes that it opens a menu and its expanded state.</li><li><strong>Keyboard</strong> — each button is individually focusable and operable; the menu is keyboard-navigable.</li><li><strong>Screen reader</strong> — announces the group and each action; icon-only members have accessible names.</li><li><strong>Focus</strong> — visible focus on each member.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep the group small and its actions related.</li><li>Make the split button's default action the most common one.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button, dropdown-menu (split menu), separator.</p>\n<p><strong>Used by:</strong> toolbars, data-table, card, dialog action rows.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Group only genuinely related actions.</li><li>Make each member independently operable.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Confuse a button group (actions) with a toggle-group (state).</li><li>Overfill the group; collapse extras into a menu.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Toolbar and Menu Button guidance.</li></ul>",
    api: None,
  },
  {
    id: "calendar",
    title: "Calendar",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A grid of dates for viewing a month and selecting one or more days.",
    tags: ["date", "selection", "grid", "input"],
    aliases: ["date-grid", "month-view"],
    composedOf: ["button"],
    usedBy: ["date-picker", "form", "dashboard"],
    related: ["date-picker", "popover", "input"],
    intent: "A calendar presents dates in a familiar month grid so users can orient by weekday and week, and select a day, a range, or several days. It is the visual core that a date-picker wraps in a popover, but it also stands alone for scheduling views.",
    spec: "<h2>Intent</h2>\n<p>A calendar presents dates in a familiar month grid so users can orient by weekday and week, and select a day, a range, or several days. It is the visual core that a date-picker wraps in a popover, but it also stands alone for scheduling views.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Selecting dates where weekday/adjacency context matters.</li><li>Displaying a month overview of events or availability.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>A single known date is easiest typed — offer a text <strong>input</strong> too.</li><li>Only month/year is needed — use dedicated month/year pickers.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Header</strong> (required) — current month/year with previous/next controls.</li><li><strong>Weekday row</strong> (required) — column headings.</li><li><strong>Day grid</strong> (required) — selectable day cells.</li><li><strong>Selection &amp; today markers</strong> (required) — highlight selected and current day.</li><li><strong>Range preview</strong> (conditional) — for range selection.</li><li><strong>Disabled/unavailable days</strong> (optional).</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Navigation</strong> — move between months/years.</li><li><strong>Selection</strong> — single, range (start→end), or multiple days.</li><li><strong>Disabled dates</strong> — min/max bounds or blocked days are not selectable.</li><li><strong>Hover preview</strong> — range endpoints preview on hover/focus.</li></ul>\n<p>Keyboard moves day-by-day and week-by-week; Page changes months.</p>\n<h2>Variants</h2>\n<ul><li><strong>Single / range / multiple</strong> selection.</li><li><strong>Single or multi-month</strong> display.</li><li><strong>With event indicators</strong> — dots or counts per day.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A seven-column week grid with a month header. Multi-month layouts collapse to one month on small screens. Day targets stay large enough to tap.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — arrows move by day, up/down by week, Page by month; Enter/Space selects; bounds respected.</li><li><strong>Semantics</strong> — a grid with the focused/selected date and month context exposed; disabled dates communicated.</li><li><strong>Screen reader</strong> — announces the full date, weekday, and selection/today state.</li><li><strong>Focus</strong> — a single roving focus within the grid.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Respect locale: first day of week, month/day names, and date format.</li><li>Explain why dates are unavailable when it matters.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button (day cells and navigation).</p>\n<p><strong>Used by:</strong> date-picker, form, dashboard/scheduling views.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Support full keyboard date navigation.</li><li>Localize weekdays, months, and week start.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Force calendar picking when typing a date is faster.</li><li>Make day targets too small to tap.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Date Picker Dialog (grid) pattern.</li></ul>",
    api: None,
  },
  {
    id: "card",
    title: "Card",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A self-contained surface that groups related content and actions into a single unit.",
    tags: ["container", "surface", "layout", "grouping"],
    aliases: ["tile", "panel"],
    composedOf: ["typography", "button", "badge", "avatar", "aspect-ratio", "separator"],
    usedBy: ["dashboard", "landing-page", "data-table", "settings"],
    related: ["alert", "table", "sheet"],
    intent: "A card groups related content and actions about a single subject into one bounded surface, making it scannable and manipulable as a unit. Cards turn a page of disparate information into a tidy set of digestible objects, and tile naturally into grids and lists.",
    spec: "<h2>Intent</h2>\n<p>A card groups related content and actions about a single subject into one bounded surface, making it scannable and manipulable as a unit. Cards turn a page of disparate information into a tidy set of digestible objects, and tile naturally into grids and lists.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Presenting a collection of like items (products, projects, stats) as units.</li><li>Grouping a subject's summary and actions on a surface.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Content is a dense, comparable dataset — a <strong>table</strong> reads better.</li><li>The grouping adds a box without meaning — plain sections may suffice.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Container / surface</strong> (required) — the bounded region.</li><li><strong>Media</strong> (optional) — image or visual, often in a fixed ratio.</li><li><strong>Header</strong> (optional) — title, subtitle, and metadata.</li><li><strong>Body</strong> (required) — the primary content.</li><li><strong>Actions / footer</strong> (optional) — buttons or links.</li><li><strong>Overlays</strong> (optional) — badges, avatars, menus.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static</strong> — a display container.</li><li><strong>Interactive</strong> (optional) — the whole card acts as a link/button with hover and focus states and a single clear primary target.</li><li><strong>Selected</strong> (optional) — in selectable grids.</li><li><strong>Loading</strong> — a skeleton mirrors the card's shape.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Content card</strong> — media + text + actions.</li><li><strong>Stat/metric card</strong> — a KPI with supporting detail.</li><li><strong>Interactive/link card</strong> — the surface navigates or selects.</li><li><strong>Elevated vs. outlined</strong> — emphasis via shadow or border.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Cards tile into responsive grids or stack in a column, reflowing column count with width. Internally, media, header, body, and footer stack vertically with consistent padding. Keep footers/actions aligned across a set for a clean grid.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Interactive cards</strong> — expose a single clear control with an accessible name; avoid nesting multiple independent links that fragment the target.</li><li><strong>Structure</strong> — card titles participate in the page's heading order where appropriate.</li><li><strong>Keyboard</strong> — interactive cards are focusable and operable; focus is visible.</li><li><strong>Media</strong> — images carry text alternatives.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>One subject per card; lead with the most identifying information.</li><li>Keep actions few and prioritize one primary action.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> typography, button, badge, avatar, aspect-ratio, separator.</p>\n<p><strong>Used by:</strong> dashboard, landing-page, data-table (as tiles), settings.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep one subject and a clear primary action per card.</li><li>Align content zones consistently across a set.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Nest many competing links inside an interactive card.</li><li>Use cards for dense tabular comparison.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — cards and content grouping.</li></ul>",
    api: None,
  },
  {
    id: "carousel",
    title: "Carousel",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A scrollable, paged set of items revealed a few at a time with navigation controls.",
    tags: ["media", "gallery", "slider", "content"],
    aliases: ["slider", "gallery", "slideshow"],
    composedOf: ["button", "card", "aspect-ratio"],
    usedBy: ["landing-page", "dashboard"],
    related: ["tabs", "scroll-area", "pagination"],
    intent: "A carousel presents a sequence of items — images, cards, testimonials — in a horizontally scrollable track, showing a subset at a time with controls to move through them. It suits browsable, non-essential content where saving vertical space matters more than seeing everything at once.",
    spec: "<h2>Intent</h2>\n<p>A carousel presents a sequence of items — images, cards, testimonials — in a horizontally scrollable track, showing a subset at a time with controls to move through them. It suits browsable, non-essential content where saving vertical space matters more than seeing everything at once.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Showcasing a browsable set (gallery, featured items) where order is loose.</li><li>Vertical space is constrained and users can page through content.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Content is important and must not be missed — carousels hide items and lower engagement with later slides. Prefer a grid or list.</li><li>Items are navigational sections — use <strong>tabs</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Viewport &amp; track</strong> (required) — the clipped area and the moving row of slides.</li><li><strong>Slides</strong> (required) — the items, one or several visible.</li><li><strong>Previous/next controls</strong> (required) — move by item or page.</li><li><strong>Pagination indicators</strong> (optional) — position dots.</li><li><strong>Autoplay controls</strong> (conditional) — pause/play when autoplay exists.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>At start / middle / end</strong> — controls disable or loop at bounds.</li><li><strong>Dragging / swiping</strong> — pointer and touch scrolling with snapping.</li><li><strong>Autoplay</strong> (optional) — advances on a timer; must be pausable and stop on interaction/hover/focus.</li></ul>\n<p>Keyboard moves between slides; focus stays visible as slides change.</p>\n<h2>Variants</h2>\n<ul><li><strong>Single-item</strong> — one slide at a time.</li><li><strong>Multi-item</strong> — several visible, paging by group.</li><li><strong>With/without autoplay</strong>; <strong>looping vs. bounded</strong>.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Slides size to the viewport, showing fewer per view as width shrinks. Snap points keep slides aligned. Ensure controls remain reachable and don't obscure content.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a labeled group/region conveying it's a carousel with multiple slides and current position.</li><li><strong>Keyboard</strong> — controls are focusable and operable; off-screen slides don't trap focus.</li><li><strong>Autoplay</strong> — provide an accessible pause; never autoplay motion without one, and respect reduced-motion.</li><li><strong>Screen reader</strong> — announces slide position (\"3 of 8\") and updates on change.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Don't bury critical content on later slides.</li><li>Label controls clearly (\"Previous slide\"/\"Next slide\").</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button (controls), card / aspect-ratio (slides).</p>\n<p><strong>Used by:</strong> landing-page, dashboard.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Provide manual controls and pause autoplay on interaction.</li><li>Indicate position and total.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Put essential content or primary CTAs only inside a carousel.</li><li>Autoplay motion without a way to stop it.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Carousel pattern; NN/g carousel usability.</li></ul>",
    api: None,
  },
  {
    id: "chart",
    title: "Chart",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A visual encoding of data — bars, lines, areas, and more — that reveals patterns and comparisons.",
    tags: ["data", "visualization", "analytics", "dashboard"],
    aliases: ["graph", "plot", "data-viz"],
    composedOf: ["tooltip", "legend", "card"],
    usedBy: ["dashboard", "card", "data-table"],
    related: ["data-table", "progress", "card"],
    intent: "A chart encodes data visually so people can perceive trends, comparisons, and distributions faster than reading numbers. It turns a dataset into a shape the eye can interpret, supporting decisions on dashboards and reports.",
    spec: "<h2>Intent</h2>\n<p>A chart encodes data visually so people can perceive trends, comparisons, and distributions faster than reading numbers. It turns a dataset into a shape the eye can interpret, supporting decisions on dashboards and reports.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Patterns, trends, or comparisons matter more than exact values.</li><li>A dataset is easier to grasp visually than as a table.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Users need precise, lookup-style values — pair with or use a <strong>data-table</strong>.</li><li>There are too few data points to justify a chart.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Plot area</strong> (required) — where marks are drawn.</li><li><strong>Marks</strong> (required) — bars, lines, points, areas, arcs encoding values.</li><li><strong>Axes &amp; gridlines</strong> (conditional) — scales and reference lines.</li><li><strong>Legend</strong> (conditional) — maps color/shape to series.</li><li><strong>Tooltip</strong> (optional) — precise values on hover/focus.</li><li><strong>Title &amp; caption</strong> (optional) — what the chart shows and its source.</li><li><strong>Empty/loading/error states</strong> (required) — when data is missing or pending.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Loading / empty / error</strong> — clearly distinguished from \"zero values\".</li><li><strong>Interactive</strong> — hover/focus reveals values; series can be toggled.</li><li><strong>Responsive redraw</strong> — adapts marks and labels to available size.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Comparison</strong> — bar/column.</li><li><strong>Trend</strong> — line/area, sparkline.</li><li><strong>Part-to-whole</strong> — stacked bar, pie/donut (sparingly).</li><li><strong>Distribution / relationship</strong> — histogram, scatter.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Charts resize to their container; labels thin out or rotate as space shrinks, and small variants (sparklines) drop axes entirely. Keep a readable data-to-ink ratio and avoid clutter at small sizes.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Non-visual access</strong> — provide the underlying data as an accessible table or summary; don't rely on the visual alone.</li><li><strong>Color</strong> — distinguish series by more than color (labels, patterns, direct labeling); meet contrast.</li><li><strong>Keyboard</strong> — interactive elements (legend toggles, focusable points) are operable; tooltips are reachable without hover.</li><li><strong>Description</strong> — a text summary conveys the key takeaway.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Title states the insight, not just the metric.</li><li>Label units and time ranges; start axes honestly.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> legend, tooltip, card (as a frame).</p>\n<p><strong>Used by:</strong> dashboard, card (metric cards), data-table (inline trends).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Offer an accessible data alternative and a plain-language summary.</li><li>Choose the chart type from the question being asked.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Encode meaning by color alone.</li><li>Mislead with truncated or distorted scales.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — non-text content and use of color.</li><li>Data visualization best practices (encoding, honesty, clarity).</li></ul>",
    api: None,
  },
  {
    id: "collapsible",
    title: "Collapsible",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A single region that expands and collapses to show or hide its content.",
    tags: ["disclosure", "content", "progressive-disclosure"],
    aliases: ["disclosure", "expandable", "show-more"],
    composedOf: ["button"],
    usedBy: ["accordion", "sidebar", "card", "form"],
    related: ["accordion", "popover"],
    intent: "A collapsible is a single show/hide region controlled by a trigger. It defers secondary detail until the user asks for it — a \"show more\", an optional form section, an expandable row of detail — keeping the default view clean.",
    spec: "<h2>Intent</h2>\n<p>A collapsible is a single show/hide region controlled by a trigger. It defers secondary detail until the user asks for it — a \"show more\", an optional form section, an expandable row of detail — keeping the default view clean.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>One region of content is optional or secondary and can be hidden by default.</li><li>You want a lightweight \"show more / show less\" toggle.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>There are several sibling sections to expand — use an <strong>accordion</strong>.</li><li>Hidden content overlays rather than pushes — use a <strong>popover</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Trigger</strong> (required) — toggles the region and shows expanded/collapsed state.</li><li><strong>Content region</strong> (required) — revealed or hidden.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Collapsed / expanded</strong> — toggled by the trigger.</li><li><strong>Disabled</strong> — cannot be toggled.</li></ul>\n<p>Expansion animates height and pushes surrounding content rather than overlaying.</p>\n<h2>Variants</h2>\n<ul><li><strong>Inline \"show more\"</strong> — for truncated text.</li><li><strong>Sectioned</strong> — an optional block within a form or panel.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Content flows in the document, moving what follows. Keep the trigger's target adequate and the expand/collapse motion smooth to avoid jumps.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — trigger is focusable and toggles on <code>Enter</code>/<code>Space</code>.</li><li><strong>Semantics</strong> — the trigger exposes expanded/collapsed and controls the region.</li><li><strong>Screen reader</strong> — announces state and the relationship to the content.</li><li><strong>Focus</strong> — visible focus on the trigger.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Label the trigger by what it reveals (\"Show advanced options\").</li><li>Keep the collapsed state informative enough to decide whether to expand.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button (trigger).</p>\n<p><strong>Used by:</strong> accordion (as items), sidebar, card, form.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Make the trigger's state and target obvious.</li><li>Use it for genuinely secondary content.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Hide essential content everyone needs.</li><li>Use a single collapsible where an accordion of peers is meant.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Disclosure pattern.</li></ul>",
    api: None,
  },
  {
    id: "combobox",
    title: "Combobox",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A text input paired with a filtered list of suggestions for selecting a value.",
    tags: ["form", "selection", "search", "autocomplete"],
    aliases: ["autocomplete", "typeahead", "searchable-select"],
    composedOf: ["input", "popover", "scroll-area", "badge"],
    usedBy: ["form", "field", "command", "data-table"],
    related: ["select", "command", "input", "dropdown-menu"],
    intent: "A combobox combines a text input with a dynamically filtered list, letting users either type to narrow options or browse and pick one. It scales selection to large option sets where a plain select would be unwieldy, and supports free-form entry when allowed.",
    spec: "<h2>Intent</h2>\n<p>A combobox combines a text input with a dynamically filtered list, letting users either type to narrow options or browse and pick one. It scales selection to large option sets where a plain select would be unwieldy, and supports free-form entry when allowed.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Choosing from many options where typing to filter helps.</li><li>You want search + selection in one control, optionally allowing new values.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The option set is small and fixed — a <strong>select</strong> or <strong>radio-group</strong> is simpler.</li><li>You're issuing commands, not picking a value — use <strong>command</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Text input</strong> (required) — accepts the query.</li><li><strong>Toggle / trigger</strong> (optional) — opens the list without typing.</li><li><strong>Popover list</strong> (required) — filtered options in a scrollable region.</li><li><strong>Options</strong> (required) — selectable items, possibly grouped, with highlight of the query match.</li><li><strong>Selected indicator / tokens</strong> (conditional) — check for single, chips for multi.</li><li><strong>Empty &amp; loading states</strong> (required) — \"no results\" and async loading.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Closed / open</strong> — list toggled by typing, click, or keyboard.</li><li><strong>Filtering</strong> — options narrow to the query; async sources show loading.</li><li><strong>Highlighted option</strong> — moves with arrow keys independent of focus.</li><li><strong>Selection</strong> — single (fills the input) or multiple (chips), clearable.</li><li><strong>No results</strong> — explicit empty state, optionally offering to create the value.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Single / multi-select</strong>.</li><li><strong>Free-solo</strong> — allows values not in the list.</li><li><strong>Async</strong> — options fetched as the user types.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The input anchors a popover list sized to the trigger, flipping when space is tight. On small screens the list may present as a full-height sheet. The list scrolls; the selected/highlighted item stays in view.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — type to filter; arrows move the highlight; Enter selects; Esc closes; Home/End jump.</li><li><strong>Semantics</strong> — a combobox exposing expanded state, the controlled listbox, and the active option; selection state per option.</li><li><strong>Screen reader</strong> — announces the active option, result counts, and selection.</li><li><strong>Focus</strong> — focus stays in the input; visible highlight tracks the active option.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Placeholder hints the action (\"Search framework…\").</li><li>Provide a helpful empty state and, if allowed, a \"create\" affordance.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> input, popover, scroll-area, badge (multi-select tokens).</p>\n<p><strong>Used by:</strong> form, field, command, data-table (filters).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Support keyboard filtering and selection fully.</li><li>Show clear loading and empty states.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a combobox for a handful of fixed options.</li><li>Lose the user's query or selection on reopen.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Combobox pattern.</li></ul>",
    api: None,
  },
  {
    id: "command",
    title: "Command Palette",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A searchable list of commands and destinations for fast, keyboard-driven navigation and action.",
    tags: ["navigation", "search", "productivity", "keyboard"],
    aliases: ["command-palette", "command-menu", "quick-search", "spotlight"],
    composedOf: ["input", "scroll-area", "dialog", "kbd", "separator"],
    usedBy: ["dashboard", "navbar"],
    related: ["combobox", "dialog", "dropdown-menu"],
    intent: "A command palette gives power users a single, keyboard-first surface to search across commands, navigation targets, and entities, then execute or jump with a keystroke. It collapses deep menus into one fast query box, usually summoned by a shortcut from anywhere in the app.",
    spec: "<h2>Intent</h2>\n<p>A command palette gives power users a single, keyboard-first surface to search across commands, navigation targets, and entities, then execute or jump with a keystroke. It collapses deep menus into one fast query box, usually summoned by a shortcut from anywhere in the app.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>An app has many actions/destinations and users benefit from fast keyboard access.</li><li>You want a global \"do anything\" entry point.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The app is simple with few actions — a menu suffices.</li><li>You're selecting a form value — use a <strong>combobox</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Trigger</strong> (required) — a shortcut (and optional button) to open it.</li><li><strong>Search input</strong> (required) — filters items as the user types.</li><li><strong>Grouped results</strong> (required) — commands, pages, and entities in labeled groups.</li><li><strong>Item</strong> (required) — icon, label, optional shortcut hint (kbd), and description.</li><li><strong>Empty state</strong> (required) — when nothing matches.</li><li><strong>Footer hints</strong> (optional) — keyboard legend.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Open / closed</strong> — typically in a centered modal overlay.</li><li><strong>Filtering</strong> — fuzzy search across items; recent/suggested shown when empty.</li><li><strong>Highlighted item</strong> — moves with arrows; Enter executes.</li><li><strong>Nested pages</strong> (optional) — a selection can drill into a sub-command list.</li><li><strong>Async</strong> — remote results show loading.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Flat command list</strong>.</li><li><strong>Multi-group</strong> — commands, navigation, search results together.</li><li><strong>Nested</strong> — sub-menus for parameterized commands.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A centered overlay with the input pinned at top and a scrolling results region below. On small screens it can fill more of the viewport. Keep the highlighted item scrolled into view.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — fully operable: open via shortcut, arrows to move, Enter to run, Esc to close; focus trapped while open and restored on close.</li><li><strong>Semantics</strong> — a modal dialog containing a combobox/listbox with active-option and expanded state exposed.</li><li><strong>Screen reader</strong> — announces result counts, the active item, and group context.</li><li><strong>Focus</strong> — starts in the input; highlight tracks the active item.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Name commands as verbs (\"Create project\", \"Go to settings\").</li><li>Show the shortcut for commands that have one.</li><li>Provide useful defaults (recent/most-used) before any query.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> input, scroll-area, dialog (overlay/modality), kbd, separator.</p>\n<p><strong>Used by:</strong> dashboard, navbar (global search/actions).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Make it reachable by a discoverable shortcut and restore focus on close.</li><li>Group and label results; show shortcuts.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Hide essential actions only behind the palette.</li><li>Neglect the empty and loading states.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Combobox and Dialog (modal) patterns.</li></ul>",
    api: None,
  },
  {
    id: "comment",
    title: "Comment",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A single unit of user-authored discussion, with author, content, and actions.",
    tags: ["social", "discussion", "content", "thread"],
    aliases: ["comment-item", "reply", "post"],
    composedOf: ["avatar", "badge", "button", "icon-button", "textarea", "link"],
    usedBy: [],
    related: ["list", "card", "avatar"],
    intent: "A comment is one contribution to a discussion — who said it, what they said, when, and what you can do about it (reply, react, edit). It's the repeating unit that composes threads and activity feeds, so its clarity about authorship and timing, and its handling of nesting and moderation, shape the whole conversation.",
    spec: "<h2>Intent</h2>\n<p>A comment is one contribution to a discussion — who said it, what they said, when, and what you can do about it (reply, react, edit). It's the repeating unit that composes threads and activity feeds, so its clarity about authorship and timing, and its handling of nesting and moderation, shape the whole conversation.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Displaying user-authored discussion, feedback, reviews, or activity entries.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The content is system-generated status — use an activity/list item.</li><li>You need a full rich-text document — that's an editor, not a comment.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Author</strong> (required) — avatar and name, with optional role/badge.</li><li><strong>Timestamp</strong> (required) — when it was posted (relative and absolute).</li><li><strong>Body</strong> (required) — the comment text (and any attachments).</li><li><strong>Actions</strong> (optional) — reply, react/like, edit, delete, report.</li><li><strong>Replies</strong> (optional) — nested child comments or a reply count.</li><li><strong>Composer</strong> (conditional) — an inline field to reply.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default</strong> — author, body, meta, and actions.</li><li><strong>Own comment</strong> — offers edit/delete.</li><li><strong>Editing</strong> — the body becomes an editable field with save/cancel.</li><li><strong>Pending / failed</strong> — optimistic post shows a sending or retry state.</li><li><strong>Deleted / hidden / flagged</strong> — moderated states are indicated clearly.</li><li><strong>Nested</strong> — replies indent or collapse under a parent.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Flat</strong> — a simple list of comments.</li><li><strong>Threaded</strong> — nested replies.</li><li><strong>Review</strong> — includes a rating.</li><li><strong>Compact activity</strong> — terse feed entries.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Author and timestamp head the item; the body follows; actions sit beneath or on hover. Nested replies indent, capping depth on small screens (collapsing or flattening) to preserve readability.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — author, time, and body are associated; timestamps expose the full date.</li><li><strong>Actions</strong> — reply/edit/etc. are keyboard operable with accessible names; icon-only actions are labeled.</li><li><strong>Live updates</strong> — newly arriving comments are announced politely.</li><li><strong>Nesting</strong> — reply relationships are conveyed, not implied by indentation alone.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Show relative time with the exact time available on hover/focus.</li><li>Make moderation states (edited, deleted) explicit and honest.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> avatar, badge (role), button / icon-button (actions), textarea (reply/edit), link.</p>\n<p><strong>Used by:</strong> discussion threads, feeds, review lists.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Make authorship and timing unmistakable.</li><li>Label icon-only actions and support keyboard use.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Convey nesting by indentation alone.</li><li>Hide moderation state or fake authorship.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — online discussion and comment usability.</li></ul>",
    api: None,
  },
  {
    id: "context-menu",
    title: "Context Menu",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A menu of actions relevant to a specific element, opened by right-click or long-press.",
    tags: ["menu", "actions", "contextual", "overlay"],
    aliases: ["right-click-menu", "contextual-menu"],
    composedOf: ["popover", "separator", "kbd"],
    usedBy: ["data-table", "sidebar", "card"],
    related: ["dropdown-menu", "menubar", "popover"],
    intent: "A context menu offers actions specific to the element the user targeted, summoned in place by right-click (or long-press on touch). It surfaces object-specific commands exactly where the user is working, without permanently occupying screen space.",
    spec: "<h2>Intent</h2>\n<p>A context menu offers actions specific to the element the user targeted, summoned in place by right-click (or long-press on touch). It surfaces object-specific commands exactly where the user is working, without permanently occupying screen space.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Providing power-user shortcuts to actions on a specific object (rows, files, canvas items).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The actions must be discoverable by everyone — also expose them via visible controls or a <strong>dropdown-menu</strong>, since context menus are easy to miss.</li><li>The menu isn't tied to a specific target — use a <strong>dropdown-menu</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Trigger gesture</strong> (required) — right-click / long-press on the target.</li><li><strong>Menu surface</strong> (required) — a floating list at the pointer.</li><li><strong>Items</strong> (required) — actions, with optional icons and shortcut hints.</li><li><strong>Submenus</strong> (optional) — nested actions.</li><li><strong>Separators &amp; section labels</strong> (optional) — grouping.</li><li><strong>Checkable items</strong> (optional) — toggles and single-choice groups.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Closed / open at pointer</strong> — positioned near the cursor, flipping to stay on-screen.</li><li><strong>Highlighted item</strong> — follows keyboard/pointer.</li><li><strong>Submenu open</strong> — on hover/arrow.</li><li><strong>Dismiss</strong> — on selection, <code>Escape</code>, or outside interaction.</li></ul>\n<p>Opening moves keyboard focus into the menu.</p>\n<h2>Variants</h2>\n<ul><li><strong>Flat actions</strong>; <strong>grouped</strong>; <strong>with submenus</strong>; <strong>with checkable items</strong>.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Anchored to the pointer with collision handling to keep it in view. On touch, a long-press opens it; consider a larger, tap-friendly presentation. Keep menus short; deep nesting is hard to use.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — a keyboard equivalent to open (e.g. the context-menu key) is provided; arrows navigate, Enter activates, Esc closes; focus is trapped and restored.</li><li><strong>Semantics</strong> — a menu with menuitem roles, including checked state and submenu relationships.</li><li><strong>Screen reader</strong> — announces items, groups, submenus, and checked state.</li><li><strong>Discoverability</strong> — don't make an action <em>only</em> reachable by right-click.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Order by frequency; group related actions; keep labels verb-led.</li><li>Mark destructive actions distinctly.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> popover (surface/positioning), separator, kbd (shortcut hints).</p>\n<p><strong>Used by:</strong> data-table (row actions), sidebar, card.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Provide a keyboard way to open and operate it.</li><li>Mirror key actions in visible controls too.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Bury essential actions behind right-click only.</li><li>Nest submenus deeply.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Menu and Menu Button patterns.</li></ul>",
    api: None,
  },
  {
    id: "cta-section",
    title: "CTA Section",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A focused section that restates the primary action to drive conversion.",
    tags: ["marketing", "landing", "section", "conversion"],
    aliases: ["call-to-action", "cta-banner", "conversion-section"],
    composedOf: ["button", "input", "link", "typography"],
    usedBy: ["landing-page", "pricing"],
    related: ["hero", "footer"],
    intent: "A CTA section is a focused band, usually near the end of a page, that restates the one action you want the visitor to take. After they've scrolled the argument, it gives momentum a place to land — a clear prompt and a single button (or a short lead-capture) — without the distraction of surrounding content.",
    spec: "<h2>Intent</h2>\n<p>A CTA section is a focused band, usually near the end of a page, that restates the one action you want the visitor to take. After they've scrolled the argument, it gives momentum a place to land — a clear prompt and a single button (or a short lead-capture) — without the distraction of surrounding content.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Re-asserting the page's primary action after the supporting content.</li><li>Capturing a lead with a short prompt (email + submit).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>It would introduce a second, competing primary action.</li><li>The page already ends with an equivalent conversion point right above.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Headline</strong> (required) — a short, action-oriented prompt.</li><li><strong>Supporting line</strong> (optional) — one sentence of reassurance.</li><li><strong>Primary action</strong> (required) — the button, or a short inline form.</li><li><strong>Secondary action</strong> (optional) — a lower-emphasis link.</li><li><strong>Reassurance</strong> (optional) — \"no credit card\", \"cancel anytime\".</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static prompt</strong> — a headline and button.</li><li><strong>Lead capture</strong> — an inline input submits interest, with loading and success/error feedback.</li><li><strong>Emphasis</strong> — often a contrasting band to stand out from the page.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Button-only</strong> — headline plus a single CTA.</li><li><strong>Inline form</strong> — email field plus submit.</li><li><strong>Split</strong> — copy on one side, action on the other.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A full-width band with centered (or split) content and the action prominent. Inline forms stack the field and button on small screens. Keep the single action visually dominant.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — a clear heading; the section is a delimited region.</li><li><strong>Form</strong> — any lead-capture field follows the input/field accessibility contract.</li><li><strong>Keyboard</strong> — the action and form are fully operable.</li><li><strong>Contrast</strong> — text and controls meet contrast against the band's background.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>One prompt, one primary action, a specific verb.</li><li>Keep any form to the minimum (usually just email).</li><li>Add brief reassurance to lower the perceived cost of acting.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> typography, button, input (lead capture), link.</p>\n<p><strong>Used by:</strong> landing-page, pricing.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep a single, unmistakable action.</li><li>Reduce friction with minimal fields and reassurance.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Introduce competing calls to action.</li><li>Ask for more than you need to capture interest.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — calls to action and conversion.</li></ul>",
    api: None,
  },
  {
    id: "data-table",
    title: "Data Table",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "An interactive table for exploring rows of structured data with sorting, filtering, and paging.",
    tags: ["data", "table", "list", "crud", "enterprise"],
    aliases: ["datagrid", "grid", "table-view"],
    composedOf: ["table", "checkbox", "input", "combobox", "pagination", "dropdown-menu", "button", "badge", "skeleton"],
    usedBy: ["dashboard", "settings"],
    related: ["table", "pagination", "chart", "card"],
    intent: "A data table lets users explore and act on collections of structured records: sort, filter, paginate, select, and trigger row actions. It extends the static table into a working surface for datasets too large to read at once, common in admin and analytics contexts.",
    spec: "<h2>Intent</h2>\n<p>A data table lets users explore and act on collections of structured records: sort, filter, paginate, select, and trigger row actions. It extends the static table into a working surface for datasets too large to read at once, common in admin and analytics contexts.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Presenting many records with columns users compare, sort, filter, and act on.</li><li>Bulk actions or row-level operations are needed.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Data is small and simple — a static <strong>table</strong> or list is enough.</li><li>Content is object-like and better browsed as <strong>cards</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Table core</strong> (required) — header and body rows/cells (the table archetype).</li><li><strong>Column headers with sort</strong> (required) — sortable indicators.</li><li><strong>Toolbar</strong> (optional) — search, filters, column visibility, view switches.</li><li><strong>Row selection</strong> (optional) — checkboxes and a bulk-action bar.</li><li><strong>Row actions</strong> (optional) — per-row menu.</li><li><strong>Pagination or virtualized scroll</strong> (required for large sets).</li><li><strong>States</strong> (required) — loading (skeleton rows), empty, and error.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Sorting</strong> — by column, single or multi, with clear direction indicators.</li><li><strong>Filtering / search</strong> — narrows rows; reflects active filters.</li><li><strong>Selection</strong> — per-row and select-all (with indeterminate parent).</li><li><strong>Paging / virtualization</strong> — moves through or streams large sets.</li><li><strong>Loading / empty / error</strong> — distinct, informative states.</li><li><strong>Density / column config</strong> (optional) — user-adjustable.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Client-side</strong> — sort/filter/page in the browser for modest sets.</li><li><strong>Server-side</strong> — operations fetched per page for large sets.</li><li><strong>Editable</strong> — inline cell editing.</li><li><strong>Expandable rows</strong> — detail panels per row.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A toolbar above, the table in a horizontally scrollable region, and pagination below. On narrow screens, allow horizontal scroll, pin key columns, or collapse rows into stacked cards. Keep headers visible while scrolling.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a proper table with header associations; sortable headers expose sort state; selection state is exposed.</li><li><strong>Keyboard</strong> — sorting, filtering, selection, paging, and row actions are all keyboard-operable; consider grid keyboard navigation for editable tables.</li><li><strong>Screen reader</strong> — announces sort changes, result/selection counts, and loading/empty states via live regions.</li><li><strong>States</strong> — empty and error states are conveyed as text, not just visuals.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Right-align numeric columns; keep headers concise nouns.</li><li>Write empty states that explain and offer a next step.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> table, checkbox, input/combobox (filters), pagination, dropdown-menu (row/column actions), button, badge, skeleton.</p>\n<p><strong>Used by:</strong> dashboard, settings/admin.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Provide clear loading, empty, and error states.</li><li>Keep sort/filter/selection state visible and accessible.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Paginate away context users need to compare.</li><li>Hide bulk actions once rows are selected.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Table and Grid patterns.</li></ul>",
    api: None,
  },
  {
    id: "date-picker",
    title: "Date Picker",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A field that lets users enter or choose a date (or range) via typing and a calendar.",
    tags: ["form", "date", "selection", "input"],
    aliases: ["date-input", "date-field", "datetime-picker"],
    composedOf: ["input", "calendar", "popover", "button"],
    usedBy: ["form", "field", "data-table"],
    related: ["calendar", "input", "popover", "combobox"],
    intent: "A date picker helps users provide a date (or range) accurately by combining a typed field with a calendar for browsing. It balances speed for those who know the date with visual context for those who need to see the month, and it enforces valid ranges and formats.",
    spec: "<h2>Intent</h2>\n<p>A date picker helps users provide a date (or range) accurately by combining a typed field with a calendar for browsing. It balances speed for those who know the date with visual context for those who need to see the month, and it enforces valid ranges and formats.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Collecting a date, date range, or date-time in a form.</li><li>Both fast typing and calendar browsing are valuable.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Only a month or year is needed — use a dedicated month/year picker.</li><li>The full calendar view is the primary surface — use <strong>calendar</strong> directly.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Field / input</strong> (required) — accepts typed dates with a clear format.</li><li><strong>Trigger</strong> (required) — opens the calendar (icon button).</li><li><strong>Popover calendar</strong> (required) — for browsing and selecting.</li><li><strong>Presets</strong> (optional) — quick ranges (\"Last 7 days\").</li><li><strong>Validation message</strong> (conditional) — for invalid or out-of-range dates.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Typed entry</strong> — parses and formats as the user types; tolerant of formats.</li><li><strong>Calendar selection</strong> — opens a popover; selecting fills the field.</li><li><strong>Range</strong> — start then end, with a hover/focus preview.</li><li><strong>Constraints</strong> — min/max and disabled dates enforced in both entry paths.</li><li><strong>Invalid</strong> — clear message; input preserved.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Single date / range / date-time</strong>.</li><li><strong>With presets</strong> — common relative ranges.</li><li><strong>Inline</strong> — calendar always visible (no popover).</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The field sits with a trigger; the calendar opens in a popover that flips to stay in view, or as a sheet on small screens. Presets sit beside or above the calendar.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — type a date directly; open and operate the calendar by keyboard; Esc closes and returns focus to the field.</li><li><strong>Semantics</strong> — the field describes the expected format; the calendar exposes grid semantics and selection/bounds.</li><li><strong>Screen reader</strong> — announces selected date(s), constraints, and validation.</li><li><strong>Focus</strong> — managed between field, popover, and back on close.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Show the expected format in helper text and honor locale.</li><li>Explain disabled dates and range rules.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> input, calendar, popover, button.</p>\n<p><strong>Used by:</strong> form, field, data-table (date filters).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Allow both typing and picking.</li><li>Enforce and explain min/max and disabled dates in both paths.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Force calendar navigation for a date the user can type.</li><li>Reset the field on an invalid parse.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Date Picker (Dialog) and combobox guidance.</li></ul>",
    api: None,
  },
  {
    id: "dialog",
    title: "Dialog",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A modal overlay that focuses the user on a self-contained task or piece of content.",
    tags: ["overlay", "modal", "focus", "task"],
    aliases: ["modal", "modal-dialog", "lightbox"],
    composedOf: ["button", "scroll-area", "separator"],
    usedBy: ["form", "data-table", "dashboard", "command"],
    related: ["alert-dialog", "sheet", "drawer", "popover"],
    intent: "A dialog overlays the page to give a focused, self-contained space for a task or content — a form, details, a confirmation with body content — while dimming and disabling the rest of the interface. It concentrates attention without navigating away, then returns the user exactly where they were.",
    spec: "<h2>Intent</h2>\n<p>A dialog overlays the page to give a focused, self-contained space for a task or content — a form, details, a confirmation with body content — while dimming and disabling the rest of the interface. It concentrates attention without navigating away, then returns the user exactly where they were.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A focused subtask benefits from isolating the user from the page (edit, create, view details).</li><li>The user should complete or dismiss before returning to context.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>It merely confirms a risky action — use an <strong>alert-dialog</strong>.</li><li>The content is large or navigational — consider a <strong>sheet</strong>/<strong>drawer</strong> or a page.</li><li>Only a small contextual overlay is needed — use a <strong>popover</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Overlay / scrim</strong> (required) — dims and blocks the background.</li><li><strong>Container / panel</strong> (required) — the focused surface.</li><li><strong>Header</strong> (required) — title and close control.</li><li><strong>Body</strong> (required) — content, scrollable if long.</li><li><strong>Footer</strong> (optional) — primary and secondary actions.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Open / closed</strong> — modal; the background is inert while open.</li><li><strong>Scrolling</strong> — long bodies scroll within the panel, keeping header/footer visible.</li><li><strong>Pending</strong> — actions can show progress.</li><li><strong>Dismiss</strong> — via close, <code>Escape</code>, or scrim click (unless work would be lost).</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Modal</strong> — blocks interaction (default).</li><li><strong>Non-modal</strong> (rare) — background remains usable.</li><li><strong>Scrollable / fixed-footer</strong> — for long content.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A centered, size-constrained panel over a scrim. On small screens it may go full-screen or become a sheet. Long content scrolls in the body while the title and actions stay reachable.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a dialog with its title as accessible name (and body described where useful).</li><li><strong>Keyboard</strong> — focus moves into the dialog on open, is trapped while open, and returns to the trigger on close; <code>Escape</code> closes.</li><li><strong>Screen reader</strong> — announces the dialog and its purpose on open.</li><li><strong>Background</strong> — content behind is inert and hidden from assistive tech.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Title names the task; keep the body focused on one job.</li><li>Use specific action verbs; place the primary action consistently.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button (actions/close), scroll-area, separator.</p>\n<p><strong>Used by:</strong> form, data-table, dashboard, command (as its overlay base).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Trap and restore focus; support <code>Escape</code>.</li><li>Keep dialogs focused on a single task.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Stack dialogs on dialogs.</li><li>Discard unsaved work on an accidental scrim click without warning.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Dialog (Modal) pattern.</li></ul>",
    api: None,
  },
  {
    id: "drawer",
    title: "Drawer",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A panel that slides in from a screen edge, often swipe-driven, for content or actions.",
    tags: ["overlay", "panel", "mobile", "gesture"],
    aliases: ["bottom-sheet", "slide-over"],
    composedOf: ["scroll-area", "button", "separator"],
    usedBy: ["navbar", "dashboard", "data-table"],
    related: ["sheet", "dialog", "sidebar", "popover"],
    intent: "A drawer slides content in from an edge of the screen — commonly the bottom on touch — keeping partial context visible and inviting gesture-driven dismissal. It suits mobile-first flows and secondary content that should feel attached to the current screen rather than replacing it.",
    spec: "<h2>Intent</h2>\n<p>A drawer slides content in from an edge of the screen — commonly the bottom on touch — keeping partial context visible and inviting gesture-driven dismissal. It suits mobile-first flows and secondary content that should feel attached to the current screen rather than replacing it.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Presenting secondary content/actions on touch, especially from the bottom.</li><li>A swipe-to-dismiss, partially-overlaying panel fits the interaction.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>On desktop for a focused task — a <strong>dialog</strong> or <strong>sheet</strong> may read better.</li><li>A confirmation is needed — use an <strong>alert-dialog</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Overlay / scrim</strong> (optional) — dims the background.</li><li><strong>Panel</strong> (required) — slides from an edge (usually bottom).</li><li><strong>Drag handle</strong> (optional) — affordance for swipe and snap points.</li><li><strong>Header / content / footer</strong> (as needed) — with scrollable content.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Open / closed</strong> — animated slide with optional snap points (peek, half, full).</li><li><strong>Dragging</strong> — follows the gesture; releases to the nearest snap or dismisses.</li><li><strong>Dismiss</strong> — swipe, scrim tap, close control, or <code>Escape</code>.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Bottom drawer</strong> — the common mobile pattern.</li><li><strong>Side drawer</strong> — from left/right (e.g. mobile navigation).</li><li><strong>With snap points</strong> — multi-height.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Anchored to an edge, sized to content or snap points, with the content region scrolling. On larger screens a drawer may become a sheet or dialog. Preserve safe areas and keep the handle/close reachable.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a dialog (modal when it blocks) with an accessible name.</li><li><strong>Keyboard</strong> — focus moves in on open, is trapped while modal, and returns on close; <code>Escape</code> closes. Gesture actions have keyboard/button equivalents.</li><li><strong>Screen reader</strong> — announces open/close and purpose.</li><li><strong>Motion</strong> — respect reduced-motion for the slide.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Give the drawer a clear title/purpose.</li><li>Provide an explicit close in addition to gestures.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> scroll-area, button, separator.</p>\n<p><strong>Used by:</strong> navbar (mobile menu), dashboard, data-table (filters on mobile).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Offer both gesture and explicit controls to dismiss.</li><li>Use snap points for content with natural heights.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Rely on swipe alone for dismissal.</li><li>Cover essential context when partial visibility is the point.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Dialog (Modal) pattern; touch gesture guidance.</li></ul>",
    api: None,
  },
  {
    id: "dropdown-menu",
    title: "Dropdown Menu",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A button-triggered menu of actions, revealed in a floating list.",
    tags: ["menu", "actions", "overlay", "navigation"],
    aliases: ["action-menu", "menu-button", "overflow-menu"],
    composedOf: ["popover", "separator", "kbd", "checkbox"],
    usedBy: ["navbar", "data-table", "card", "sidebar", "toolbar"],
    related: ["context-menu", "menubar", "popover", "select"],
    intent: "A dropdown menu attaches a list of actions to a button, revealing them on demand so a surface can offer many operations without clutter. It's the workhorse for \"more actions\", account menus, and overflow — an explicitly triggered, keyboard- navigable menu of commands.",
    spec: "<h2>Intent</h2>\n<p>A dropdown menu attaches a list of actions to a button, revealing them on demand so a surface can offer many operations without clutter. It's the workhorse for \"more actions\", account menus, and overflow — an explicitly triggered, keyboard- navigable menu of commands.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Grouping several actions behind one trigger (overflow, account, row actions).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Selecting a form value — use a <strong>select</strong> or <strong>combobox</strong>.</li><li>Actions are tied to right-clicking an object — use a <strong>context-menu</strong>.</li><li>There's a single action — use a plain <strong>button</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Trigger</strong> (required) — a button showing it opens a menu.</li><li><strong>Menu surface</strong> (required) — a floating, positioned list.</li><li><strong>Items</strong> (required) — actions with optional icons and shortcut hints.</li><li><strong>Submenus</strong> (optional) — nested action groups.</li><li><strong>Separators &amp; labels</strong> (optional) — grouping.</li><li><strong>Checkable items</strong> (optional) — toggles or single-choice groups.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Closed / open</strong> — anchored to the trigger, flipping to stay on-screen.</li><li><strong>Highlighted item</strong> — via keyboard/pointer.</li><li><strong>Submenu open</strong> — on hover/arrow.</li><li><strong>Dismiss</strong> — on selection, <code>Escape</code>, or outside click; focus returns to trigger.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Actions menu</strong> — commands.</li><li><strong>With checkable items</strong> — toggles / single-choice.</li><li><strong>With submenus</strong> — nested.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The menu aligns to the trigger and collision-flips within the viewport; long menus scroll. On small screens it may present as a sheet/drawer for larger targets.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — open with Enter/Space/Down; arrows navigate; Enter activates; Esc closes; typeahead optional. Focus moves into the menu and returns to the trigger.</li><li><strong>Semantics</strong> — a menu button controlling a menu of menuitems, with checked and submenu relationships exposed.</li><li><strong>Screen reader</strong> — announces items, groups, checked state, and submenus.</li><li><strong>Focus</strong> — visible highlight; trap while open.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Verb-led item labels; group related actions; mark destructive items.</li><li>Show shortcuts where they exist.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> popover (surface/positioning), separator, kbd, checkbox (checkable items).</p>\n<p><strong>Used by:</strong> navbar, data-table, card, sidebar, toolbars.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Support full keyboard operation and focus return.</li><li>Group and label; keep menus reasonably short.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a menu to pick form values.</li><li>Nest submenus deeply.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Menu Button pattern.</li></ul>",
    api: None,
  },
  {
    id: "empty-state",
    title: "Empty State",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A placeholder shown when there is no content yet, guiding the user toward a first action.",
    tags: ["feedback", "onboarding", "guidance", "zero-data"],
    aliases: ["zero-state", "blank-slate", "no-results"],
    composedOf: ["button", "typography", "aspect-ratio"],
    usedBy: ["data-table", "dashboard", "card", "search"],
    related: ["skeleton", "alert", "card"],
    intent: "An empty state fills the void when there's no content — a new list, no search results, a cleared inbox — turning a dead end into guidance. It explains why the space is empty and points to the next useful action, so absence of data doesn't feel like a broken screen.",
    spec: "<h2>Intent</h2>\n<p>An empty state fills the void when there's no content — a new list, no search results, a cleared inbox — turning a dead end into guidance. It explains why the space is empty and points to the next useful action, so absence of data doesn't feel like a broken screen.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A region has no items yet (first-run), no matches (filtered), or has been cleared.</li><li>Users benefit from context and a clear next step.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Content is still loading — use a <strong>skeleton</strong> or spinner.</li><li>An error occurred — use an error state/<strong>alert</strong> that explains the failure.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Visual</strong> (optional) — illustration or icon setting the tone.</li><li><strong>Headline</strong> (required) — names the situation.</li><li><strong>Description</strong> (required) — why it's empty and what to do.</li><li><strong>Primary action</strong> (conditional) — the main next step (create, invite, clear filters).</li><li><strong>Secondary action / help</strong> (optional) — learn more, import, contact.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>First-run / onboarding</strong> — encourages creating the first item.</li><li><strong>No results</strong> — after search/filter; offers to adjust or clear filters.</li><li><strong>Cleared / all-done</strong> — a positive, completed message.</li><li><strong>Restricted</strong> — content exists but the user lacks access (explain, don't dead-end).</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Onboarding empty state</strong> — guidance to get started.</li><li><strong>No-results empty state</strong> — recovery from a query/filter.</li><li><strong>Success/all-clear</strong> — nothing left to do.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Centered within the empty region with a comfortable vertical stack: visual, headline, description, action. Scales down gracefully, keeping the action visible on small screens. Occupies the space the content would, avoiding a jarring void.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — the headline is a proper heading; the region is announced when it replaces loading content.</li><li><strong>Keyboard</strong> — actions are focusable and operable.</li><li><strong>Screen reader</strong> — conveys that the area is empty and what to do next, not just a decorative image.</li><li><strong>Imagery</strong> — illustrations are decorative (hidden) unless they carry meaning.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Be encouraging and specific; explain the cause and the fix.</li><li>Distinguish \"no results\" (adjust query) from \"nothing yet\" (create something).</li><li>One clear primary action.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> typography, button (actions), aspect-ratio (illustration).</p>\n<p><strong>Used by:</strong> data-table, dashboard, card lists, search results.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Explain the cause and offer a concrete next step.</li><li>Differentiate empty from loading and from error.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Leave a blank area with no guidance.</li><li>Show an empty state while data is still loading.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — empty states and first-time user experience.</li></ul>",
    api: None,
  },
  {
    id: "faq",
    title: "FAQ",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A list of common questions with expandable answers that removes last-mile objections.",
    tags: ["marketing", "landing", "content", "support", "disclosure"],
    aliases: ["faqs", "questions", "help-list"],
    composedOf: ["accordion", "link"],
    usedBy: ["landing-page", "pricing"],
    related: ["accordion", "empty-state"],
    intent: "An FAQ answers the questions that block a decision — pricing details, limits, security, cancellation — in one scannable place. By surfacing common objections and answering them plainly, it removes friction late in the funnel and reduces support load, letting users self-serve the reassurance they need.",
    spec: "<h2>Intent</h2>\n<p>An FAQ answers the questions that block a decision — pricing details, limits, security, cancellation — in one scannable place. By surfacing common objections and answering them plainly, it removes friction late in the funnel and reduces support load, letting users self-serve the reassurance they need.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A handful of recurring questions predictably stand between users and acting.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The content is really documentation — link to proper docs instead.</li><li>\"Questions\" are marketing copy in disguise — answer honestly or omit.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Section heading</strong> (optional) — introduces the FAQ.</li><li><strong>Question items</strong> (required) — each a question header and an expandable answer.</li><li><strong>Answers</strong> (required) — concise, with links to detail where useful.</li><li><strong>Escape hatch</strong> (optional) — a link to contact support or full docs.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Collapsed / expanded</strong> — per item, toggled by its header (an accordion).</li><li><strong>Single vs. multiple open</strong> — one at a time, or several.</li><li><strong>Deep-linkable</strong> (optional) — a specific question can be linked and opened.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Accordion</strong> — collapsible Q&amp;A (the common default).</li><li><strong>Two-column list</strong> — questions and answers shown fully in columns.</li><li><strong>Categorized</strong> — grouped by topic.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A single readable column of question rows; answers expand in place, pushing content below. Two-column variants collapse to one column on small screens. Keep questions short and scannable.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Disclosure semantics</strong> — each question is a button controlling its answer, exposing expanded/collapsed state (the accordion pattern).</li><li><strong>Keyboard</strong> — headers are focusable and toggle on <code>Enter</code>/<code>Space</code>.</li><li><strong>Screen reader</strong> — announces the question, state, and answer relationship.</li><li><strong>Structure</strong> — questions participate in a sensible heading order.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Write real questions in the user's words; answer directly and briefly.</li><li>Link to detail rather than expanding into documentation.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> accordion, link.</p>\n<p><strong>Used by:</strong> landing-page, pricing.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Answer the questions that actually block decisions.</li><li>Keep answers concise with links to more.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use the FAQ as disguised marketing copy.</li><li>Bury essential information only inside collapsed answers.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Accordion/Disclosure patterns.</li></ul>",
    api: None,
  },
  {
    id: "feature-grid",
    title: "Feature Grid",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A grid of concise, icon-led items that communicate a product's key features or benefits.",
    tags: ["marketing", "landing", "section", "features"],
    aliases: ["feature-list", "benefits-grid", "feature-cards"],
    composedOf: ["icon", "card", "typography"],
    usedBy: ["landing-page"],
    related: ["card", "list"],
    intent: "A feature grid presents a product's key features or benefits as a scannable set of short, parallel items — each an icon, a title, and a sentence. It lets a visitor grasp the breadth of what's offered at a glance, framed as outcomes for the user rather than a raw list of capabilities.",
    spec: "<h2>Intent</h2>\n<p>A feature grid presents a product's key features or benefits as a scannable set of short, parallel items — each an icon, a title, and a sentence. It lets a visitor grasp the breadth of what's offered at a glance, framed as outcomes for the user rather than a raw list of capabilities.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Summarizing several comparable features or benefits on a marketing page.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Features need deep, sequential explanation — use alternating feature sections.</li><li>The items are a detailed spec comparison — use a table.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Grid container</strong> (required) — arranges items in columns.</li><li><strong>Feature items</strong> (required) — each with an icon, a short title, and a one-line description.</li><li><strong>Item action</strong> (optional) — a \"learn more\" link per feature.</li><li><strong>Section heading</strong> (optional) — introduces the set.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static</strong> — presentational; items may link to detail.</li><li><strong>Responsive reflow</strong> — column count adapts to width.</li><li><strong>Hover</strong> (optional) — subtle emphasis on interactive items.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Icon + text</strong> — the compact default.</li><li><strong>Cards</strong> — each feature in a bordered card.</li><li><strong>Two / three / four columns</strong> — by content volume.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Items lay out in an even grid that reduces columns as width shrinks, down to a single column on small screens. Keep titles and descriptions parallel in length so rows align and the set scans cleanly.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — a list of items with real headings for each title.</li><li><strong>Icons</strong> — decorative icons are hidden from assistive tech; meaningful ones have text alternatives.</li><li><strong>Keyboard</strong> — any per-item links are reachable and operable.</li><li><strong>Contrast</strong> — text and icons meet contrast requirements.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Frame each item as a user outcome; keep titles short and descriptions to a line.</li><li>Use a consistent icon style across items.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> icon, card (optional per-item), typography.</p>\n<p><strong>Used by:</strong> landing-page.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep items parallel and scannable.</li><li>Lead with benefits, not internal feature names.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Overload each item with a paragraph.</li><li>Mix icon styles or wildly uneven item lengths.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — feature communication and scannability.</li></ul>",
    api: None,
  },
  {
    id: "field",
    title: "Field",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A wrapper that pairs a form control with its label, help text, and validation message.",
    tags: ["form", "control", "layout", "validation"],
    aliases: ["form-field", "form-control", "form-group"],
    composedOf: ["label", "input", "textarea", "select", "checkbox", "radio-group", "switch"],
    usedBy: ["form", "sign-in", "settings"],
    related: ["form", "label", "input"],
    intent: "A field is the connective wrapper that turns a bare control into a complete, accessible form element: label, the control itself, optional helper text, and validation feedback — all wired together. It standardizes structure, spacing, and the label/description/error associations so every control behaves consistently.",
    spec: "<h2>Intent</h2>\n<p>A field is the connective wrapper that turns a bare control into a complete, accessible form element: label, the control itself, optional helper text, and validation feedback — all wired together. It standardizes structure, spacing, and the label/description/error associations so every control behaves consistently.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Presenting any form control that needs a label and possibly help or error text.</li><li>Enforcing consistent structure and associations across a form.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The control is standalone with no label/help/validation needs (rare).</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Label</strong> (required) — names the control (label element).</li><li><strong>Control</strong> (required) — the input, select, checkbox, etc.</li><li><strong>Helper/description</strong> (optional) — guidance about the value.</li><li><strong>Validation message</strong> (conditional) — error/success feedback.</li><li><strong>Requirement indicator</strong> (optional) — required/optional marker.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default / focus-within</strong> — reflects the control's focus.</li><li><strong>Invalid</strong> — shows the message and associates it with the control.</li><li><strong>Disabled / read-only</strong> — reflected on label and control together.</li><li><strong>Required</strong> — communicated in text/semantics.</li></ul>\n<p>The wrapper wires label→control and description/error→control associations.</p>\n<h2>Variants</h2>\n<ul><li><strong>Stacked</strong> — label above the control (default).</li><li><strong>Inline</strong> — label beside the control (e.g. checkbox/switch rows).</li><li><strong>With counter / adornments</strong> — passed through to the control.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Vertical stack of label, control, and message with consistent spacing and a shared vertical rhythm across a form. Controls fill the column width; inline layouts align label and control on a baseline. Messages reserve space to avoid layout jumps.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Associations</strong> — label, description, and error are programmatically bound to the control so name/description/validity are complete.</li><li><strong>Validation</strong> — errors are announced and linked; required and invalid states are exposed, not just colored.</li><li><strong>Focus</strong> — the label activates the control; focus-within is visible.</li><li><strong>Screen reader</strong> — announces label, description, requirement, and error together.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep labels short; put format guidance in helper text, not placeholders.</li><li>Write actionable error messages; mark requirement consistently across the form.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> label plus one control (input, textarea, select, checkbox, radio-group, switch, …).</p>\n<p><strong>Used by:</strong> form, sign-in, settings.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Wire label/description/error associations for every field.</li><li>Reserve space for messages to prevent layout shift.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Rely on placeholders as labels.</li><li>Convey errors with color alone.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Labels or Instructions; Info and Relationships; Error Identification.</li></ul>",
    api: None,
  },
  {
    id: "footer",
    title: "Footer",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A page-closing region with secondary navigation, legal, and trust information.",
    tags: ["navigation", "layout", "wayfinding", "legal"],
    aliases: ["site-footer", "page-footer", "colophon"],
    composedOf: ["link", "separator", "logo", "input", "button"],
    usedBy: ["landing-page", "dashboard", "pricing"],
    related: ["navbar", "sidebar"],
    intent: "A footer closes the page with the content users look for at the end: secondary navigation, legal and policy links, contact and social, and trust signals. It is the conventional home for everything that doesn't belong in primary navigation but should be reliably findable, anchoring the bottom of nearly every marketing and content page.",
    spec: "<h2>Intent</h2>\n<p>A footer closes the page with the content users look for at the end: secondary navigation, legal and policy links, contact and social, and trust signals. It is the conventional home for everything that doesn't belong in primary navigation but should be reliably findable, anchoring the bottom of nearly every marketing and content page.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A page needs a consistent closing region for secondary links, legal, and contact.</li><li>Users expect a familiar place to find policies, sitemap, or social links.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The screen is a focused app view or flow where a footer adds noise — omit it.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Container</strong> (required) — the page-closing region (a contentinfo landmark).</li><li><strong>Link groups</strong> (optional) — columns of categorized secondary links.</li><li><strong>Brand / identity</strong> (optional) — logo and a short descriptor.</li><li><strong>Legal</strong> (required) — copyright, terms, and privacy.</li><li><strong>Utility</strong> (optional) — newsletter signup, locale/currency, social links.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static</strong> — primarily a navigation and information region.</li><li><strong>Interactive utilities</strong> — newsletter input, locale switcher, and links carry their own states.</li><li><strong>Responsive collapse</strong> — multi-column groups stack on small screens.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Minimal</strong> — logo, a few links, and legal.</li><li><strong>Fat footer</strong> — multiple grouped link columns plus utilities.</li><li><strong>App footer</strong> — a slim bar with version, status, and legal.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>On wide viewports, link groups sit in columns with brand and legal spanning the width; on small screens the columns stack into a single readable column. Legal and copyright typically anchor the very bottom.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a contentinfo landmark; link groups use headings and lists so the structure is navigable.</li><li><strong>Keyboard</strong> — all links and utilities reachable in a logical order.</li><li><strong>Screen reader</strong> — announces the landmark and grouped navigation; utility controls follow their own patterns (input, button).</li><li><strong>Contrast</strong> — secondary text still meets contrast requirements.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Group links under clear category headings.</li><li>Keep legal links (privacy, terms) easy to find.</li><li>Don't bury genuinely primary navigation only in the footer.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> link, separator, logo, input (newsletter), button.</p>\n<p><strong>Used by:</strong> landing-page, dashboard, pricing.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Group and label secondary links; keep legal reliably placed.</li><li>Stack gracefully on small screens.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Rely on the footer as the only path to important destinations.</li><li>Overstuff it into an unscannable wall of links.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Info and Relationships; landmark guidance for contentinfo.</li></ul>",
    api: None,
  },
  {
    id: "form",
    title: "Form",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A structured set of fields that collects, validates, and submits user input toward a goal.",
    tags: ["form", "input", "validation", "data-entry"],
    aliases: ["data-entry", "input-form"],
    composedOf: ["field", "input", "textarea", "select", "checkbox", "radio-group", "switch", "button", "alert"],
    usedBy: ["sign-in", "settings", "dialog", "landing-page"],
    related: ["field", "input", "dialog"],
    intent: "A form gathers structured input from a user, validates it, and submits it to accomplish a goal — signing in, creating a record, updating settings. It orchestrates fields, guidance, validation, and submission into one coherent, accessible flow, minimizing effort and error.",
    spec: "<h2>Intent</h2>\n<p>A form gathers structured input from a user, validates it, and submits it to accomplish a goal — signing in, creating a record, updating settings. It orchestrates fields, guidance, validation, and submission into one coherent, accessible flow, minimizing effort and error.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Collecting one or more related values the user reviews and submits.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>A single setting applies instantly — a lone <strong>switch</strong> may be enough.</li><li>The interaction is a search box — that's a single input, not a form flow.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Fields</strong> (required) — label + control + help/error (the field archetype).</li><li><strong>Groups / sections / fieldsets</strong> (optional) — related fields with a legend.</li><li><strong>Guidance</strong> (optional) — instructions and required-field conventions.</li><li><strong>Validation feedback</strong> (required) — inline per-field and a summary for errors.</li><li><strong>Actions</strong> (required) — submit plus optional cancel/reset.</li><li><strong>Status</strong> (required) — submitting, success, and error states.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Editing</strong> — users enter values; validate at sensible times (blur/submit).</li><li><strong>Invalid</strong> — inline messages plus an error summary; focus moves to the first error on submit.</li><li><strong>Submitting</strong> — controls disabled or the submit shows progress; prevent double submit.</li><li><strong>Success / error</strong> — clear outcome, preserving input on failure.</li><li><strong>Dirty</strong> — warn before discarding unsaved changes.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Single-column</strong> — the recommended default for scannability.</li><li><strong>Sectioned / multi-step (wizard)</strong> — long forms split into steps with progress.</li><li><strong>Inline / compact</strong> — short forms (login, search-adjacent).</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Prefer a single column with a comfortable measure and consistent vertical rhythm; labels above controls. Group related fields; keep the primary action prominent and predictably placed. On small screens, fields go full-width.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a form landmark; each control labeled; groups use fieldset/legend.</li><li><strong>Validation</strong> — errors are programmatically associated with fields, announced, and summarized; focus moves to the first error on submit.</li><li><strong>Keyboard</strong> — logical tab order; submit reachable; Enter submits where expected.</li><li><strong>Status</strong> — submitting/success/error conveyed via live regions, not color alone.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Ask only for what's needed; explain why for sensitive fields.</li><li>Write specific, actionable error messages; mark required consistently.</li><li>Label the submit with its outcome (\"Create account\"), not \"Submit\".</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> field, input, textarea, select, checkbox, radio-group, switch, button, alert (error summary).</p>\n<p><strong>Used by:</strong> sign-in, settings, dialog, landing-page (lead capture).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Validate helpfully and preserve input on error.</li><li>Prevent duplicate submissions and confirm success.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Clear the form on a failed submit.</li><li>Rely on placeholders as labels or color alone for errors.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Labels or Instructions; Error Identification, Suggestion, and Prevention.</li></ul>",
    api: None,
  },
  {
    id: "hero",
    title: "Hero",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A prominent opening section that states the value proposition and the primary call to action.",
    tags: ["marketing", "landing", "section", "conversion"],
    aliases: ["hero-section", "banner", "masthead"],
    composedOf: ["button", "badge", "link", "typography"],
    usedBy: ["landing-page", "pricing"],
    related: ["cta-section", "navbar"],
    intent: "A hero is the first thing a visitor sees: a large opening section that states, in a few words, what the product is and why it matters, and offers the single most important next step. It has one job — communicate the value proposition and get the user to act — so everything in it earns its place toward that goal.",
    spec: "<h2>Intent</h2>\n<p>A hero is the first thing a visitor sees: a large opening section that states, in a few words, what the product is and why it matters, and offers the single most important next step. It has one job — communicate the value proposition and get the user to act — so everything in it earns its place toward that goal.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Opening a marketing or landing page with a clear value proposition and a primary action.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The screen is a working app view — a hero wastes prime space.</li><li>There is no single primary action to feature.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Eyebrow</strong> (optional) — a short tag or announcement above the headline.</li><li><strong>Headline</strong> (required) — the concise value statement.</li><li><strong>Subhead</strong> (optional) — a supporting sentence adding specificity.</li><li><strong>Primary action</strong> (required) — the main call to action; optional secondary.</li><li><strong>Supporting visual</strong> (optional) — product shot, illustration, or media.</li><li><strong>Social proof</strong> (optional) — logos, ratings, or a trust line.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static</strong> — primarily presentational.</li><li><strong>Media states</strong> — visuals reserve space to avoid layout shift; motion respects reduced-motion preferences.</li><li><strong>Variant/personalized</strong> (optional) — copy may differ by campaign or test while the single primary action stays constant.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Centered</strong> — stacked, centered copy and actions.</li><li><strong>Split</strong> — copy on one side, visual on the other.</li><li><strong>Media background</strong> — text over an image/video with sufficient contrast.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The headline leads with generous space; actions sit directly beneath. Split layouts collapse to stacked on small screens with the copy first. Keep the primary action above the fold and the measure readable.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — the headline is the page's top heading; the section is a landmark or clearly delimited region.</li><li><strong>Contrast</strong> — text over media meets contrast via overlays/scrims.</li><li><strong>Keyboard</strong> — actions are fully operable; visuals carry text alternatives.</li><li><strong>Motion</strong> — background/auto motion respects reduced-motion.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Lead with the benefit to the user, concretely, not adjectives.</li><li>One primary action with a specific verb; keep the subhead to one sentence.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> typography, button, badge (eyebrow), link (secondary action).</p>\n<p><strong>Used by:</strong> landing-page, pricing.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep one clear message and one primary action.</li><li>Front-load the value proposition.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Stack multiple competing calls to action.</li><li>Bury the message under heavy decorative media.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — value proposition and above-the-fold guidance.</li></ul>",
    api: None,
  },
  {
    id: "hover-card",
    title: "Hover Card",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A rich preview surface revealed when a pointer rests on a trigger.",
    tags: ["overlay", "preview", "contextual", "hover"],
    aliases: ["preview-card", "rich-tooltip"],
    composedOf: ["popover", "avatar", "button", "typography"],
    usedBy: ["navbar", "card", "data-table"],
    related: ["tooltip", "popover", "card"],
    intent: "A hover card shows a richer preview — a person's profile, a link's summary, an item's details — when the pointer rests on a trigger. It offers a peek of related information without a click or navigation, richer than a plain tooltip but lighter than opening a page.",
    spec: "<h2>Intent</h2>\n<p>A hover card shows a richer preview — a person's profile, a link's summary, an item's details — when the pointer rests on a trigger. It offers a peek of related information without a click or navigation, richer than a plain tooltip but lighter than opening a page.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Offering an optional, supplementary preview on hover for pointer users (profile cards, link previews).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The information is essential — hover is not available on touch or keyboard-only; don't hide required content here.</li><li>A short text hint suffices — use a <strong>tooltip</strong>.</li><li>The content is interactive and primary — use a <strong>popover</strong> opened by click.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Trigger</strong> (required) — the element hovered.</li><li><strong>Preview surface</strong> (required) — a floating card with structured content.</li><li><strong>Content</strong> (required) — media, text, and optional light actions.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Closed / open</strong> — appears after a short hover delay, dismisses on leave with a small grace period so the pointer can travel to it.</li><li><strong>Positioned</strong> — anchored to the trigger, flipping to stay in view.</li></ul>\n<p>Because it's hover-driven, it must be treated as supplementary, not required.</p>\n<h2>Variants</h2>\n<ul><li><strong>Profile preview</strong> — avatar, name, meta, follow action.</li><li><strong>Link preview</strong> — title, description, image.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Anchored near the trigger with collision handling. On touch, provide an alternative access path (tap to open a popover or navigate), since hover doesn't exist there.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Non-essential</strong> — never place required information only in a hover card.</li><li><strong>Keyboard</strong> — if the trigger is focusable and the content matters, expose it on focus too, or provide a click alternative.</li><li><strong>Screen reader</strong> — associate the preview with the trigger when it conveys meaningful, persistent info.</li><li><strong>Dismissal</strong> — dismissible and not obstructive; respects reduced motion.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep previews concise and scannable.</li><li>Don't gate primary actions behind hover.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> popover (surface/positioning), avatar, typography, button.</p>\n<p><strong>Used by:</strong> navbar, card, data-table (entity previews).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Add a small open/close delay and grace area.</li><li>Provide touch/keyboard alternatives.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Hide essential content or actions behind hover.</li><li>Trigger on every element, creating noise.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Content on Hover or Focus.</li></ul>",
    api: None,
  },
  {
    id: "input-group",
    title: "Input Group",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "An input combined with adjacent add-ons or controls that act as one composite control.",
    tags: ["form", "input", "layout", "control"],
    aliases: ["input-addon", "combined-input"],
    composedOf: ["input", "button", "select", "badge"],
    usedBy: ["form", "field", "navbar", "data-table"],
    related: ["input", "field", "button-group"],
    intent: "An input group binds an input with adjacent add-ons — a prefix/suffix, an icon, a unit, a select, or a button — into one composite control. It gives the value context (currency symbol, protocol, unit) or an attached action (search, copy, apply) while reading as a single, cohesive field.",
    spec: "<h2>Intent</h2>\n<p>An input group binds an input with adjacent add-ons — a prefix/suffix, an icon, a unit, a select, or a button — into one composite control. It gives the value context (currency symbol, protocol, unit) or an attached action (search, copy, apply) while reading as a single, cohesive field.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>An input needs a fixed affix (unit, symbol, domain) or an attached action/select.</li><li>Combining input + button (search, apply) into one visual control.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The affix is guidance, not part of the value — use helper text instead.</li><li>Multiple independent fields are involved — use separate fields.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Input</strong> (required) — the editable value.</li><li><strong>Leading add-on</strong> (optional) — icon, symbol, prefix text, or select.</li><li><strong>Trailing add-on</strong> (optional) — unit, action button, or select.</li><li><strong>Validation message</strong> (conditional) — for the composite value.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Focus-within</strong> — the whole group reflects focus on the input.</li><li><strong>Add-on actions</strong> — attached buttons trigger actions (search, clear, apply, copy); attached selects change a related parameter (unit, protocol).</li><li><strong>Disabled / invalid</strong> — applied consistently across the group.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Prefix/suffix text</strong> — symbols or units.</li><li><strong>With icon</strong> — leading/trailing icon.</li><li><strong>With button</strong> — attached action.</li><li><strong>With select</strong> — an attached choice affecting the input's meaning.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Add-ons sit flush against the input with shared borders and rounded outer corners, forming one control. On narrow widths, ensure attached controls stay tappable; consider stacking only if the seam would break. Keep the input the dominant target.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Associations</strong> — the group has one accessible name for the value; affixes that change meaning (e.g. unit select) are exposed, not just visual.</li><li><strong>Keyboard</strong> — the input and any attached controls are each focusable and operable.</li><li><strong>Screen reader</strong> — announces the value with meaningful affixes and the purpose of attached actions.</li><li><strong>Focus</strong> — visible focus on the focused member; adequate target sizes.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Use affixes that are genuinely part of the value; keep them short.</li><li>Label attached buttons/selects by their action or parameter.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> input, button (attached action), select (attached choice), badge.</p>\n<p><strong>Used by:</strong> form, field, navbar (search), data-table (filters).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep the composite readable as one control with one value name.</li><li>Ensure attached controls are individually operable.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Stuff unrelated fields into one group.</li><li>Hide meaningful affixes from assistive tech.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Labels or Instructions; Info and Relationships.</li></ul>",
    api: None,
  },
  {
    id: "list",
    title: "List",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A vertical sequence of related items, each a compact row of content and actions.",
    tags: ["collection", "rows", "content", "navigation"],
    aliases: ["list-view", "item-list", "feed"],
    composedOf: ["avatar", "badge", "button", "icon-button", "separator", "checkbox"],
    usedBy: ["dashboard", "settings", "sidebar"],
    related: ["table", "data-table", "card"],
    intent: "A list presents a set of related items as a vertical stack of rows, each showing a compact summary and optional actions. It suits object-like items scanned top to bottom — messages, files, people, notifications — where a single primary attribute leads and a table's columns would be overkill.",
    spec: "<h2>Intent</h2>\n<p>A list presents a set of related items as a vertical stack of rows, each showing a compact summary and optional actions. It suits object-like items scanned top to bottom — messages, files, people, notifications — where a single primary attribute leads and a table's columns would be overkill.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Presenting a homogeneous collection scanned vertically, one item per row.</li><li>Each item has a clear primary label plus light metadata and actions.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Items are compared across many attributes — use a <strong>table</strong> / <strong>data-table</strong>.</li><li>Items are rich, media-forward tiles — use <strong>cards</strong> in a grid.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Container</strong> (required) — the list region.</li><li><strong>Items / rows</strong> (required) — each with a primary label and optional supporting text, leading media (avatar/icon), trailing metadata, and actions.</li><li><strong>Dividers</strong> (optional) — separate rows.</li><li><strong>Selection</strong> (optional) — checkboxes for multi-select and bulk actions.</li><li><strong>Section headers</strong> (optional) — group items.</li><li><strong>States</strong> (required) — loading (skeleton rows), empty, and error.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static / interactive</strong> — rows may be inert, navigable (whole row a link), or selectable.</li><li><strong>Selected</strong> — in selectable lists, with a bulk-action affordance.</li><li><strong>Reorderable</strong> (optional) — drag to reorder.</li><li><strong>Loading / empty / error</strong> — distinct, informative states.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Simple</strong> — label-only rows.</li><li><strong>With media/metadata</strong> — leading avatar/icon, trailing status or time.</li><li><strong>Interactive</strong> — navigable or selectable rows.</li><li><strong>Grouped</strong> — sectioned with headers.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Rows span the container width with consistent internal padding and alignment (leading media, flexible content, trailing meta/actions). On small screens, trailing content may wrap beneath the primary label. Long lists scroll or paginate.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a real list structure; interactive rows expose the correct link or button role and an accessible name.</li><li><strong>Keyboard</strong> — interactive and selectable rows are reachable and operable; a single clear target per row avoids fragmenting focus.</li><li><strong>Screen reader</strong> — announces item count and each row's meaningful content; loading/empty states are conveyed.</li><li><strong>Selection</strong> — selected state and bulk actions are announced.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Lead with the most identifying attribute; keep supporting text brief.</li><li>Provide a helpful empty state with a next step.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> avatar, badge, button, icon-button, separator, checkbox.</p>\n<p><strong>Used by:</strong> dashboard, settings, sidebar.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep one clear target per interactive row.</li><li>Provide loading, empty, and error states.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Force multi-attribute comparison into a list — use a table.</li><li>Nest many competing controls in a single row.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — List and Listbox guidance.</li></ul>",
    api: None,
  },
  {
    id: "menubar",
    title: "Menubar",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A horizontal bar of top-level menus exposing an application's commands.",
    tags: ["menu", "commands", "application", "navigation"],
    aliases: ["app-menu", "menu-bar"],
    composedOf: ["dropdown-menu", "separator", "kbd"],
    usedBy: ["dashboard"],
    related: ["dropdown-menu", "navigation-menu", "context-menu"],
    intent: "A menubar presents an application's commands as a persistent horizontal row of top-level menus (File, Edit, View…), each opening a list of grouped actions. It mirrors the desktop-application menu model, giving power users a comprehensive, predictable home for commands.",
    spec: "<h2>Intent</h2>\n<p>A menubar presents an application's commands as a persistent horizontal row of top-level menus (File, Edit, View…), each opening a list of grouped actions. It mirrors the desktop-application menu model, giving power users a comprehensive, predictable home for commands.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Building an application-like product with many grouped commands.</li><li>Users expect a desktop-style, always-available command surface.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The product is content/marketing-oriented — use a <strong>navbar</strong> or <strong>navigation-menu</strong>.</li><li>There are only a few actions — a <strong>dropdown-menu</strong> suffices.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Bar</strong> (required) — the horizontal container of menu triggers.</li><li><strong>Top-level menus</strong> (required) — each a labeled trigger opening a menu.</li><li><strong>Menu items</strong> (required) — actions, with icons, shortcuts, and submenus.</li><li><strong>Separators &amp; groups</strong> (optional).</li><li><strong>Checkable items</strong> (optional) — toggles and single-choice groups.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Idle</strong> — no menu open.</li><li><strong>Menu open</strong> — one menu at a time; moving across the bar switches menus.</li><li><strong>Submenu open</strong> — on hover/arrow.</li><li><strong>Dismiss</strong> — <code>Escape</code>, selection, or outside click.</li></ul>\n<p>Roving focus moves across the bar; arrows enter and traverse menus.</p>\n<h2>Variants</h2>\n<ul><li><strong>Full menubar</strong> — many top-level menus.</li><li><strong>Compact</strong> — a couple of menus for lighter apps.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A single horizontal row of triggers with menus opening below. On narrow widths, collapse into an overflow or a single menu button. Keep the bar's position stable.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — left/right move across the bar, down opens a menu, up/down within it, submenus via right, Esc closes; a single tab stop for the bar.</li><li><strong>Semantics</strong> — a menubar with menuitem/menu roles, exposing submenu, checked, and disabled states.</li><li><strong>Screen reader</strong> — announces the bar, menus, items, and state.</li><li><strong>Focus</strong> — roving focus with a visible indicator.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Use conventional menu names and ordering where they exist.</li><li>Show shortcuts; mark destructive items.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> dropdown-menu (each top-level menu), separator, kbd.</p>\n<p><strong>Used by:</strong> dashboard / application shells.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Follow established menubar keyboard conventions.</li><li>Keep top-level groupings meaningful.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a menubar for simple marketing navigation.</li><li>Overload menus with deep nesting.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Menubar pattern.</li></ul>",
    api: None,
  },
  {
    id: "navbar",
    title: "Navbar",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A persistent top-level bar that anchors identity and primary navigation across a product.",
    tags: ["navigation", "header", "layout", "wayfinding"],
    aliases: ["navbar", "top-nav", "header", "app-bar"],
    composedOf: ["button", "input", "link", "logo", "dropdown-menu", "avatar"],
    usedBy: ["landing-page", "dashboard", "settings"],
    related: ["sidebar", "tabs", "breadcrumb", "footer"],
    intent: "A navbar is the primary wayfinding surface of a product. It stays present across screens to answer three questions at a glance: where am I, where can I go, and what can I do from anywhere. It anchors brand identity and the top level of the information architecture, and it hosts the handful of global actions (search, account, primary call to action) that should be reachable from every page.",
    spec: "<h2>Intent</h2>\n<p>A navbar is the primary wayfinding surface of a product. It stays present across screens to answer three questions at a glance: <em>where am I</em>, <em>where can I go</em>, and <em>what can I do from anywhere</em>. It anchors brand identity and the top level of the information architecture, and it hosts the handful of global actions (search, account, primary call to action) that should be reachable from every page.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A product needs consistent, always-available top-level navigation and identity.</li><li>The primary sections number roughly two to seven and benefit from horizontal exposure.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Navigation is deep or has many peer sections — a <strong>sidebar</strong> scales better.</li><li>The screen is a focused, single-task flow (checkout, onboarding step) where a minimal header reduces distraction.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Brand / logo</strong> (required) — identity; typically links to home.</li><li><strong>Primary navigation</strong> (required) — the top-level destinations.</li><li><strong>Global actions</strong> (optional) — search, primary CTA, notifications.</li><li><strong>Account / user menu</strong> (optional) — avatar opening account and session actions.</li><li><strong>Menu toggle</strong> (conditional) — reveals collapsed navigation on small screens.</li><li><strong>Active indicator</strong> (required when nav is present) — shows the current section.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default</strong> — resting bar with current section indicated.</li><li><strong>Sticky / scrolled</strong> (optional) — remains pinned; may compact or gain a shadow to separate from content.</li><li><strong>Collapsed (mobile)</strong> — primary navigation is hidden behind a menu toggle that opens a drawer or overlay.</li><li><strong>Menu open</strong> — a disclosed menu (account, or the mobile drawer) with managed focus.</li><li><strong>Authenticated vs. anonymous</strong> — the action cluster differs (e.g. \"Sign in\" vs. account menu).</li></ul>\n<p>The current-location indicator updates as the user navigates. Opening a menu traps focus appropriately and closes on <code>Escape</code> and outside interaction.</p>\n<h2>Variants</h2>\n<ul><li><strong>Marketing navbar</strong> — logo, a few section links, and a prominent CTA; optimized for conversion.</li><li><strong>Application navbar</strong> — adds search, notifications, and an account menu for signed-in product use.</li><li><strong>Minimal header</strong> — logo and at most one action, for focused flows.</li><li><strong>Centered vs. leading</strong> — brand and navigation alignment variations.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>On wide viewports, parts lay out horizontally: brand at the leading edge, navigation adjacent or centered, actions at the trailing edge. As width decreases, secondary items collapse first, then primary navigation moves behind a menu toggle into a drawer or overlay. The bar keeps a consistent height and predictable landmark position. If sticky, it must not obscure content behind it (anchor targets and scroll offsets account for its height).</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — every destination and action is reachable and operable by keyboard; logical focus order from brand to navigation to actions.</li><li><strong>Semantics</strong> — exposed as a navigation landmark with an accessible name; the current item is marked as current.</li><li><strong>Screen reader</strong> — announces the landmark, item names, and current state; disclosed menus announce expanded/collapsed.</li><li><strong>Focus</strong> — opening the mobile drawer or a menu moves focus into it, traps it while open, and restores it on close; <code>Escape</code> closes.</li><li><strong>Target size</strong> — touch targets meet minimum size, especially the menu toggle.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Label sections with short, distinct nouns that match their destination titles.</li><li>Keep top-level items few; demote the long tail into menus or a sidebar.</li><li>Give the primary CTA a specific verb; don't compete with multiple primaries.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> logo, link (navigation items), button (CTA / menu toggle), input (search), avatar + menu (account).</p>\n<p><strong>Used by:</strong> landing-page, dashboard, settings, and most full-page archetypes.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep the current location obvious at all times.</li><li>Preserve landmark position and identity across pages.</li><li>Collapse gracefully to a menu on small screens with proper focus management.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Overload the bar with more top-level items than can be scanned at a glance.</li><li>Hide essential actions behind unlabeled icons.</li><li>Let a sticky bar cover the content it links to.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — landmark and menu/disclosure patterns.</li><li>Nielsen Norman Group — navigation and information architecture guidance.</li></ul>",
    api: None,
  },
  {
    id: "navigation-menu",
    title: "Navigation Menu",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A primary navigation control whose items can open rich flyout panels of links.",
    tags: ["navigation", "menu", "wayfinding", "flyout"],
    aliases: ["mega-menu", "nav-menu", "flyout-nav"],
    composedOf: ["link", "popover", "separator"],
    usedBy: ["navbar", "landing-page"],
    related: ["navbar", "menubar", "dropdown-menu", "tabs"],
    intent: "A navigation menu presents a product's primary destinations, where top-level items can reveal flyout panels grouping related links (a \"mega menu\"). It scales site navigation beyond a flat row, organizing many destinations into browsable categories while keeping the top level clean.",
    spec: "<h2>Intent</h2>\n<p>A navigation menu presents a product's primary destinations, where top-level items can reveal flyout panels grouping related links (a \"mega menu\"). It scales site navigation beyond a flat row, organizing many destinations into browsable categories while keeping the top level clean.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A site has several sections, each with multiple destinations worth previewing.</li><li>You want categorized flyouts rather than a flat link row.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Navigation is a handful of links — a simple <strong>navbar</strong> row is enough.</li><li>Items are application commands — use a <strong>menubar</strong>.</li><li>Items switch views in place — use <strong>tabs</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Menu list</strong> (required) — top-level items.</li><li><strong>Triggers</strong> (required) — items that open a panel vs. plain links.</li><li><strong>Flyout panels</strong> (conditional) — grouped links, optionally with descriptions and featured content.</li><li><strong>Active indicator</strong> (required) — current section.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Closed / open panel</strong> — hover or focus/click opens; only one panel at a time.</li><li><strong>Pointer intent</strong> — small delays prevent flicker as the pointer moves.</li><li><strong>Dismiss</strong> — <code>Escape</code>, outside click, or moving away.</li></ul>\n<p>Keyboard opens and traverses panels; the current section stays indicated.</p>\n<h2>Variants</h2>\n<ul><li><strong>Simple links</strong> — no flyouts.</li><li><strong>Mega menu</strong> — multi-column grouped panels with rich content.</li><li><strong>Mixed</strong> — some items link directly, others open panels.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Horizontal top-level items with panels dropping beneath, aligned and collision- aware. On small screens it collapses into a drawer/accordion navigation. Panels reflow columns to fit width.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — items and panel links are reachable and operable; open/close and traverse by keyboard; Esc closes and restores focus.</li><li><strong>Semantics</strong> — a navigation landmark; disclosure/menu semantics for panels with expanded state; current item marked.</li><li><strong>Screen reader</strong> — announces the landmark, items, panel state, and current location.</li><li><strong>Pointer intent</strong> — hover behavior has keyboard/focus equivalents.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Group destinations under clear category labels.</li><li>Keep top-level labels short; use panels for depth.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> link, popover (flyout positioning), separator.</p>\n<p><strong>Used by:</strong> navbar, landing-page.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Provide keyboard and focus parity with hover.</li><li>Keep one panel open at a time and indicate current section.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Overstuff mega menus into unscannable walls of links.</li><li>Rely on hover-only behavior.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Disclosure and menu navigation guidance.</li></ul>",
    api: None,
  },
  {
    id: "pagination",
    title: "Pagination",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "Controls for moving through content split across multiple pages.",
    tags: ["navigation", "data", "list", "paging"],
    aliases: ["pager", "page-nav"],
    composedOf: ["button", "link", "select"],
    usedBy: ["data-table", "table", "dashboard"],
    related: ["data-table", "table", "breadcrumb"],
    intent: "Pagination breaks a long set of results into pages and gives users controls to move among them. It bounds how much loads and renders at once, and lets users orient themselves (\"page 3 of 20\") within a large collection.",
    spec: "<h2>Intent</h2>\n<p>Pagination breaks a long set of results into pages and gives users controls to move among them. It bounds how much loads and renders at once, and lets users orient themselves (\"page 3 of 20\") within a large collection.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A result set is too large to show at once and discrete pages aid orientation.</li><li>Users benefit from jumping to specific pages or knowing totals.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Continuous browsing suits better — consider infinite scroll or \"load more\" (with their own trade-offs).</li><li>The list is short enough to show fully.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Previous / next</strong> (required) — step controls, disabled at bounds.</li><li><strong>Page numbers</strong> (optional) — direct jumps, with ellipses for large ranges.</li><li><strong>Current-page indicator</strong> (required) — the active page.</li><li><strong>Page size selector</strong> (optional) — items per page.</li><li><strong>Range/total summary</strong> (optional) — \"showing 21–40 of 320\".</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>At first / last page</strong> — boundary controls disabled.</li><li><strong>Active page</strong> — clearly marked and non-interactive as a jump.</li><li><strong>Truncation</strong> — middle pages collapse into ellipses for long ranges.</li><li><strong>Loading</strong> — the region indicates fetching on page change.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Numbered</strong> — full page links with truncation.</li><li><strong>Prev/next only</strong> — minimal stepping.</li><li><strong>Load more / infinite</strong> — progressive loading (related pattern).</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A horizontal control group, centered or trailing. On narrow screens, reduce to prev/next plus current indicator. Keep targets tappable and the current page obvious.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a navigation landmark labeled as pagination; the current page is marked; disabled controls are conveyed as such.</li><li><strong>Keyboard</strong> — all controls reachable and operable in order.</li><li><strong>Screen reader</strong> — announces page changes and current position (e.g. via a live region) so context isn't lost.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Label controls clearly (\"Previous page\"); show totals when helpful.</li><li>Keep page-size options sensible and few.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button/link (controls), select (page size).</p>\n<p><strong>Used by:</strong> data-table, table, dashboard lists.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Indicate the current page and disable boundary controls.</li><li>Announce page changes to assistive tech.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Hide totals when users need to gauge scope.</li><li>Make number targets too small on touch.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — navigation landmark guidance.</li></ul>",
    api: None,
  },
  {
    id: "popover",
    title: "Popover",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A floating surface anchored to a trigger, holding contextual content or controls.",
    tags: ["overlay", "contextual", "floating", "container"],
    aliases: ["flyout", "pop-up", "floating-panel"],
    composedOf: ["button", "scroll-area"],
    usedBy: ["combobox", "select", "date-picker", "dropdown-menu", "tooltip", "hover-card", "navigation-menu"],
    related: ["tooltip", "dropdown-menu", "dialog", "hover-card"],
    intent: "A popover is a general-purpose floating surface anchored to a trigger, used to present contextual content or lightweight controls near what they relate to. It is the shared foundation many overlays build on (menus, selects, date pickers), and on its own it holds small interactive content without the weight of a modal dialog.",
    spec: "<h2>Intent</h2>\n<p>A popover is a general-purpose floating surface anchored to a trigger, used to present contextual content or lightweight controls near what they relate to. It is the shared foundation many overlays build on (menus, selects, date pickers), and on its own it holds small interactive content without the weight of a modal dialog.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Showing contextual, possibly interactive content next to its trigger (filters, small forms, info with actions).</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The content is a plain text hint — use a <strong>tooltip</strong>.</li><li>It's a list of commands — use a <strong>dropdown-menu</strong>.</li><li>The task needs full focus/blocking — use a <strong>dialog</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Trigger</strong> (required) — opens the popover (usually on click).</li><li><strong>Surface / panel</strong> (required) — the floating container, optionally with an arrow.</li><li><strong>Content</strong> (required) — text and/or interactive controls.</li><li><strong>Optional close</strong> (optional) — explicit dismiss control.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Closed / open</strong> — anchored to the trigger, flipping and shifting to stay in view.</li><li><strong>Focus behavior</strong> — may be modal (traps focus) or non-modal depending on content.</li><li><strong>Dismiss</strong> — <code>Escape</code>, outside click, or a close control.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Non-modal</strong> — background stays interactive (default for light content).</li><li><strong>Modal</strong> — traps focus for small interactive tasks.</li><li><strong>With arrow</strong> — points to the anchor.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Positioned relative to the trigger with collision handling (flip/shift); content can scroll if it grows. On small screens, larger popovers may become sheets. Keep it clear of the trigger it points to.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — associated with its trigger; the trigger conveys it opens a popover and its expanded state.</li><li><strong>Keyboard</strong> — openable and dismissible by keyboard; <code>Escape</code> closes and returns focus; if modal, focus is trapped.</li><li><strong>Screen reader</strong> — content is discoverable and, where relevant, announced on open.</li><li><strong>Focus</strong> — managed into the popover for interactive content and restored on close.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep content focused and small; escalate to a dialog if it grows.</li><li>Give interactive popovers a clear way to dismiss.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button (trigger), scroll-area.</p>\n<p><strong>Used by:</strong> combobox, select, date-picker, dropdown-menu, tooltip, hover-card, navigation-menu, context-menu.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Manage focus and support <code>Escape</code>.</li><li>Keep positioning within the viewport.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Cram large tasks into a popover.</li><li>Use a popover where a simple tooltip is meant.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Dialog and Disclosure guidance for popovers.</li></ul>",
    api: None,
  },
  {
    id: "pricing-table",
    title: "Pricing Table",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A side-by-side comparison of plans and their features to help a visitor choose.",
    tags: ["marketing", "pricing", "comparison", "conversion"],
    aliases: ["plan-comparison", "pricing-cards", "tiers"],
    composedOf: ["card", "badge", "button", "switch", "toggle-group", "table"],
    usedBy: ["landing-page", "pricing"],
    related: ["pricing", "card", "table"],
    intent: "A pricing table lays out plans side by side so a visitor can compare what each costs and includes, then choose. It makes differences legible at a glance, highlights a recommended plan to guide the decision, and answers the practical questions — price, billing period, and what's in each tier — that gate a purchase.",
    spec: "<h2>Intent</h2>\n<p>A pricing table lays out plans side by side so a visitor can compare what each costs and includes, then choose. It makes differences legible at a glance, highlights a recommended plan to guide the decision, and answers the practical questions — price, billing period, and what's in each tier — that gate a purchase.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Presenting two or more plans a visitor compares before selecting.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>There is a single plan/price — state it plainly.</li><li>The full matrix would overwhelm before the summary — lead with plan cards.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Billing toggle</strong> (optional) — monthly/annual switch that updates all prices.</li><li><strong>Plan columns/cards</strong> (required) — name, price, short pitch, and a call to action per plan.</li><li><strong>Recommended highlight</strong> (optional) — a badge and emphasis on one plan.</li><li><strong>Feature rows</strong> (required) — a per-feature comparison (included, limited, or absent) across plans.</li><li><strong>Footnotes</strong> (optional) — caveats, limits, and definitions.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Billing switch</strong> — toggling monthly/annual updates prices and shows savings.</li><li><strong>Recommended</strong> — one plan emphasized to steer choice.</li><li><strong>Per-plan CTA</strong> — reflects state (start, upgrade, contact sales).</li><li><strong>Currency/locale</strong> (optional) — prices adapt.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Cards</strong> — plans as side-by-side cards with key features.</li><li><strong>Matrix</strong> — a full feature-by-plan table.</li><li><strong>Cards + matrix</strong> — summary cards above a detailed table.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Plans sit in a row on wide screens with the recommended plan emphasized; they stack into a single column on small screens, keeping the recommended plan prominent. A full matrix scrolls horizontally or restructures per plan on narrow widths.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a real table with header associations (plans as columns, features as rows), or clearly-structured cards.</li><li><strong>Billing toggle</strong> — its effect on prices is announced.</li><li><strong>Keyboard</strong> — the toggle and every plan CTA are operable.</li><li><strong>Non-color emphasis</strong> — the recommended plan and included/excluded cells are distinguished by more than color.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Make prices, billing period, and included features easy to compare.</li><li>Keep feature rows parallel; define limits clearly.</li><li>One specific CTA verb per plan.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> card, badge, button, switch / toggle-group (billing), table.</p>\n<p><strong>Used by:</strong> landing-page, pricing.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Highlight a recommended plan honestly.</li><li>Show billing period and real costs up front.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Hide limits or fees, or use dark patterns to force a plan.</li><li>Lead with an exhaustive matrix before a scannable summary.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — pricing and plan-comparison usability.</li></ul>",
    api: None,
  },
  {
    id: "resizable",
    title: "Resizable Panels",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "Adjacent panels separated by draggable handles that let users redistribute space.",
    tags: ["layout", "panels", "split", "workspace"],
    aliases: ["split-pane", "split-view", "resizable-panels"],
    composedOf: ["separator", "scroll-area"],
    usedBy: ["dashboard", "data-table"],
    related: ["sidebar", "scroll-area", "separator"],
    intent: "Resizable panels divide a region into two or more areas separated by draggable handles, letting users decide how to split available space — a file tree beside an editor, a list beside a detail pane. It hands layout control to the user for workspace-style interfaces where preferred proportions vary.",
    spec: "<h2>Intent</h2>\n<p>Resizable panels divide a region into two or more areas separated by draggable handles, letting users decide how to split available space — a file tree beside an editor, a list beside a detail pane. It hands layout control to the user for workspace-style interfaces where preferred proportions vary.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A workspace benefits from user-controlled proportions between adjacent regions.</li><li>Users want to expand one area at the expense of another.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>A fixed or simply responsive layout serves users better.</li><li>The secondary area is navigation that collapses — use a <strong>sidebar</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Panels</strong> (required) — two or more adjacent regions.</li><li><strong>Handles / dividers</strong> (required) — draggable separators between panels.</li><li><strong>Min/max constraints</strong> (required) — bounds each panel's size.</li><li><strong>Collapse affordance</strong> (optional) — snap a panel closed/open.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Dragging</strong> — the handle resizes neighboring panels within constraints.</li><li><strong>At min/max</strong> — resizing stops at bounds; a panel may collapse at its minimum.</li><li><strong>Persisted</strong> — remembers sizes across sessions where appropriate.</li></ul>\n<p>Keyboard focus on a handle allows resizing by arrow keys.</p>\n<h2>Variants</h2>\n<ul><li><strong>Horizontal / vertical</strong> splits.</li><li><strong>Nested</strong> — panels within panels (grid workspaces).</li><li><strong>Collapsible panels</strong> — snap to hidden.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Panels fill the container and share space per the handles. On small screens, splits often stack or one panel collapses to preserve usability. Respect minimum sizes so no panel becomes unusable.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — handles are focusable and resize via arrow keys; provide a way to reset or collapse.</li><li><strong>Semantics</strong> — each handle exposes a separator role with orientation and current value/bounds.</li><li><strong>Screen reader</strong> — announces the handle, orientation, and resulting sizes.</li><li><strong>Focus</strong> — visible focus on handles; adequate handle hit area.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Set sensible minimums so content stays usable.</li><li>Indicate collapsible handles clearly.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> separator (handles), scroll-area (panel content).</p>\n<p><strong>Used by:</strong> dashboard, data-table (list/detail workspaces).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Support keyboard resizing and reasonable minimums.</li><li>Persist user-chosen sizes when it helps.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Allow panels to shrink into uselessness.</li><li>Rely on drag alone with no keyboard path.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Window Splitter pattern.</li></ul>",
    api: None,
  },
  {
    id: "search",
    title: "Search",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "An input-led pattern for querying content, with suggestions, clearing, and results.",
    tags: ["search", "input", "query", "navigation", "filter"],
    aliases: ["search-box", "search-bar", "search-field"],
    composedOf: ["input", "icon", "icon-button", "button", "list", "empty-state"],
    usedBy: ["navbar", "data-table", "dashboard"],
    related: ["combobox", "command", "input"],
    intent: "Search lets a person find content by typing a query. Beyond the field itself, the pattern covers the affordances that make searching productive: a clear entry point, a way to clear the query, live suggestions or recent searches, and a predictable results and empty state. It's how users cut through volume to the one thing they want.",
    spec: "<h2>Intent</h2>\n<p>Search lets a person find content by typing a query. Beyond the field itself, the pattern covers the affordances that make searching productive: a clear entry point, a way to clear the query, live suggestions or recent searches, and a predictable results and empty state. It's how users cut through volume to the one thing they want.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Users need to find items in a large or unbounded set by keyword.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Choosing from a small, known set — a <strong>select</strong> or filters fit better.</li><li>Selecting a single value into a field — use a <strong>combobox</strong>.</li><li>Running commands/navigation — use a <strong>command</strong> palette.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Field</strong> (required) — the query input, usually with a leading search icon.</li><li><strong>Clear affordance</strong> (optional) — an icon-button to reset the query.</li><li><strong>Submit</strong> (conditional) — for submit-on-enter vs. live search.</li><li><strong>Suggestions</strong> (optional) — recent/typeahead results as you type.</li><li><strong>Results &amp; states</strong> (required in results contexts) — results list plus loading, empty (\"no results\"), and error states.</li><li><strong>Scope/filters</strong> (optional) — restrict what is searched.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Empty / typing</strong> — placeholder guidance; may show recent searches on focus.</li><li><strong>Live vs. on-submit</strong> — results update as you type, or on enter.</li><li><strong>Loading</strong> — while fetching, especially for remote search.</li><li><strong>No results</strong> — an explicit empty state offering to adjust or clear.</li><li><strong>Cleared</strong> — the clear control resets query and results.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Inline field</strong> — a simple search box.</li><li><strong>With suggestions</strong> — typeahead/recent dropdown.</li><li><strong>Scoped</strong> — a filter/scope selector attached.</li><li><strong>Full search experience</strong> — field plus results and facets.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The field is prominent where search is primary (navbar, top of a list). Suggestions open in a popover beneath it; results render below or on a dedicated view. On small screens the field may expand to full width or open from an icon.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a labeled search field within a search landmark where appropriate; suggestions follow combobox semantics.</li><li><strong>Keyboard</strong> — type to query, arrow through suggestions, enter to submit, escape to clear/close; the clear control is reachable.</li><li><strong>Screen reader</strong> — result counts and loading/empty states are announced via live regions.</li><li><strong>Clear control</strong> — has an accessible name.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Placeholder states what's searched (\"Search invoices…\"), not \"Search\".</li><li>Write a helpful \"no results\" state with a way forward.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> input, icon, icon-button (clear), button (submit), list (results), empty-state.</p>\n<p><strong>Used by:</strong> navbar, data-table, dashboard.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Provide clear, loading, and empty states.</li><li>Announce result counts to assistive tech.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Hide whether search is live or requires submitting.</li><li>Leave a \"no results\" dead end without a next step.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — search usability; WAI-ARIA combobox guidance.</li></ul>",
    api: None,
  },
  {
    id: "select",
    title: "Select",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A control that opens a list for choosing one option (or several) from a defined set.",
    tags: ["form", "selection", "dropdown", "control"],
    aliases: ["dropdown", "picker", "listbox"],
    composedOf: ["popover", "scroll-area", "separator", "checkbox"],
    usedBy: ["form", "field", "data-table", "pagination"],
    related: ["combobox", "dropdown-menu", "radio-group"],
    intent: "A select lets users choose from a defined set of options by opening a list and picking one (or several). It conserves space compared to showing all options and suits sets too long for radios but not large enough to need typed filtering.",
    spec: "<h2>Intent</h2>\n<p>A select lets users choose from a defined set of options by opening a list and picking one (or several). It conserves space compared to showing all options and suits sets too long for radios but not large enough to need typed filtering.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Choosing from a moderate, fixed set of options where space is limited.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The set is very large or benefits from typing to filter — use a <strong>combobox</strong>.</li><li>There are only a few options worth showing at once — use a <strong>radio-group</strong>.</li><li>The list contains actions, not values — use a <strong>dropdown-menu</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Trigger</strong> (required) — shows the current value and opens the list.</li><li><strong>Listbox</strong> (required) — the scrollable set of options.</li><li><strong>Options</strong> (required) — selectable items, possibly grouped, with a selected indicator.</li><li><strong>Placeholder</strong> (optional) — when nothing is selected.</li><li><strong>Multi-select indicators</strong> (conditional) — checkboxes/chips.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Closed / open</strong> — trigger shows value; list opens on click/keyboard.</li><li><strong>Highlighted option</strong> — moves with arrows; typeahead jumps to matches.</li><li><strong>Selection</strong> — single (closes on pick) or multiple (stays open, shows count).</li><li><strong>Disabled options</strong> — skipped in navigation.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Single-select</strong> — one value.</li><li><strong>Multi-select</strong> — several values with a summary/chips.</li><li><strong>Grouped</strong> — options under labeled groups.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The trigger sizes to its field; the list anchors to it, flips to stay in view, and scrolls when long. On small screens the list may present as a sheet for larger targets. The selected option is scrolled into view on open.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — open with Enter/Space/Down; arrows move; typeahead; Enter selects; Esc closes; Home/End jump.</li><li><strong>Semantics</strong> — a listbox with the expanded state on the trigger and selected state per option; group labels exposed.</li><li><strong>Screen reader</strong> — announces the current value, active option, and selection.</li><li><strong>Focus</strong> — returns to the trigger on close; highlight tracks the active option.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Order options predictably (logical or alphabetical); label groups clearly.</li><li>Use a placeholder that states the choice (\"Select a country\").</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> popover, scroll-area, separator, checkbox (multi-select).</p>\n<p><strong>Used by:</strong> form, field, data-table (filters), pagination (page size).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Support keyboard navigation and typeahead.</li><li>Show the selected value clearly on the trigger.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a select for a huge list better served by a combobox.</li><li>Put actions in a select.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Listbox and Select-Only Combobox patterns.</li></ul>",
    api: None,
  },
  {
    id: "sheet",
    title: "Sheet",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A panel that slides in from a screen edge for secondary content or tasks alongside the page.",
    tags: ["overlay", "panel", "side", "task"],
    aliases: ["side-panel", "slide-over", "off-canvas"],
    composedOf: ["scroll-area", "button", "separator"],
    usedBy: ["dashboard", "data-table", "settings"],
    related: ["dialog", "drawer", "sidebar", "popover"],
    intent: "A sheet slides a panel in from an edge of the screen (commonly the side) to host secondary content or a task while keeping the page context adjacent. It's roomier than a popover and less interruptive than a centered dialog — ideal for details, filters, or edit forms that relate to what's on screen.",
    spec: "<h2>Intent</h2>\n<p>A sheet slides a panel in from an edge of the screen (commonly the side) to host secondary content or a task while keeping the page context adjacent. It's roomier than a popover and less interruptive than a centered dialog — ideal for details, filters, or edit forms that relate to what's on screen.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Showing supplementary content or a task beside the current page (details, filters, quick edit).</li><li>The content is taller than wide and benefits from an edge panel.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The task demands full focus and centering — use a <strong>dialog</strong>.</li><li>It's a mobile bottom, gesture-first panel — use a <strong>drawer</strong>.</li><li>It's persistent primary navigation — use a <strong>sidebar</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Overlay / scrim</strong> (optional) — dims the page when modal.</li><li><strong>Panel</strong> (required) — slides from top/right/bottom/left.</li><li><strong>Header</strong> (required) — title and close.</li><li><strong>Content</strong> (required) — scrollable body.</li><li><strong>Footer</strong> (optional) — actions.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Open / closed</strong> — animated slide from the chosen edge.</li><li><strong>Modal vs. non-modal</strong> — blocks the page or leaves it usable.</li><li><strong>Scrolling</strong> — long content scrolls within the panel.</li><li><strong>Dismiss</strong> — close control, <code>Escape</code>, or scrim click.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Right / left / top / bottom</strong> placement.</li><li><strong>Modal / non-modal</strong>.</li><li><strong>Sizes</strong> — from a defined width/height scale.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Anchored to an edge with a constrained size; content scrolls while header/footer stay reachable. On small screens it may expand to full width or become a drawer.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a dialog (modal when it blocks) with an accessible name.</li><li><strong>Keyboard</strong> — focus moves in on open, is trapped when modal, and returns on close; <code>Escape</code> closes.</li><li><strong>Screen reader</strong> — announces open/close and purpose; background inert when modal.</li><li><strong>Motion</strong> — respects reduced-motion.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Give the sheet a clear title and a visible close.</li><li>Keep it focused on content related to the current context.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> scroll-area, button, separator.</p>\n<p><strong>Used by:</strong> dashboard, data-table (filters/details), settings.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Manage focus and provide an explicit close.</li><li>Choose the edge that fits the content shape.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use a modal sheet where the page must stay interactive.</li><li>Confuse a task sheet with persistent navigation.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Dialog (Modal) pattern.</li></ul>",
    api: None,
  },
  {
    id: "sidebar",
    title: "Sidebar",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A persistent vertical navigation region alongside the main content of an application.",
    tags: ["navigation", "layout", "application", "wayfinding"],
    aliases: ["side-nav", "nav-rail", "app-sidebar"],
    composedOf: ["link", "button", "collapsible", "avatar", "separator", "badge", "scroll-area"],
    usedBy: ["dashboard", "settings"],
    related: ["navbar", "navigation-menu", "sheet", "drawer"],
    intent: "A sidebar provides persistent, vertically-organized navigation beside an application's content, scaling to many sections and nested groups. It's the primary wayfinding surface for product/app shells where a horizontal navbar would run out of room, keeping destinations always visible or a click away.",
    spec: "<h2>Intent</h2>\n<p>A sidebar provides persistent, vertically-organized navigation beside an application's content, scaling to many sections and nested groups. It's the primary wayfinding surface for product/app shells where a horizontal navbar would run out of room, keeping destinations always visible or a click away.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>An application has many sections or nested navigation that needs to stay present.</li><li>Vertical space for grouped, scrollable navigation suits the product.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Navigation is a few marketing links — use a <strong>navbar</strong>.</li><li>The panel is a transient task surface — use a <strong>sheet</strong>/<strong>drawer</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Container / rail</strong> (required) — the persistent vertical region.</li><li><strong>Brand / header</strong> (optional) — logo or workspace switcher.</li><li><strong>Navigation groups</strong> (required) — sections with items, optionally collapsible.</li><li><strong>Nav items</strong> (required) — links with icons, labels, and optional badges.</li><li><strong>Active indicator</strong> (required) — current destination.</li><li><strong>Footer</strong> (optional) — account, settings, collapse control.</li><li><strong>Collapse toggle</strong> (optional) — expand/collapse to an icon rail.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Expanded / collapsed (rail)</strong> — full labels vs. icons only, with tooltips when collapsed.</li><li><strong>Group expand/collapse</strong> — nested sections toggle.</li><li><strong>Active item</strong> — current location marked.</li><li><strong>Mobile</strong> — becomes an off-canvas drawer behind a toggle.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Full sidebar</strong> — icons + labels.</li><li><strong>Icon rail</strong> — compact, icon-only with tooltips.</li><li><strong>Multi-level</strong> — nested collapsible groups.</li><li><strong>Dual (rail + panel)</strong> — a slim rail plus a contextual panel.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Fixed to a side, full height, with a scrollable navigation region and pinned header/footer. It collapses to a rail on medium widths and to an off-canvas drawer on small screens. Content area adjusts to the sidebar's width.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a navigation landmark with an accessible name; current item marked; collapsible groups expose expanded state.</li><li><strong>Keyboard</strong> — all items and toggles reachable and operable; logical order.</li><li><strong>Screen reader</strong> — announces the landmark, groups, items, and current location; collapsed-rail items retain accessible names.</li><li><strong>Focus</strong> — visible focus; when it becomes a drawer, focus is managed and trapped.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Group destinations meaningfully with short, distinct labels.</li><li>Keep icons paired with labels; ensure collapsed icons have names.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> link, button, collapsible, avatar, separator, badge, scroll-area.</p>\n<p><strong>Used by:</strong> dashboard, settings / application shells.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep the current location obvious and navigation scannable.</li><li>Provide accessible names for collapsed icon items.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Cram every action into the sidebar.</li><li>Lose current-location context when collapsed.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — landmark and disclosure guidance.</li></ul>",
    api: None,
  },
  {
    id: "stat",
    title: "Stat",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A compact display of a single key metric with its label and optional trend.",
    tags: ["data", "metric", "kpi", "dashboard"],
    aliases: ["kpi", "metric", "stat-card", "metric-card"],
    composedOf: ["badge", "icon", "chart", "typography"],
    usedBy: ["dashboard"],
    related: ["card", "chart"],
    intent: "A stat surfaces one number that matters — revenue, active users, conversion rate — with just enough context to interpret it: a label, and often a change indicator or a tiny trend. It turns a single figure into a glanceable signal, the building block that tiles into a dashboard's metric row.",
    spec: "<h2>Intent</h2>\n<p>A stat surfaces one number that matters — revenue, active users, conversion rate — with just enough context to interpret it: a label, and often a change indicator or a tiny trend. It turns a single figure into a glanceable signal, the building block that tiles into a dashboard's metric row.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Highlighting a single headline metric with light context.</li><li>Building a row/grid of KPIs on a dashboard or summary.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Many values must be compared in detail — use a <strong>table</strong> or <strong>chart</strong>.</li><li>The number needs heavy explanation to be meaningful.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Value</strong> (required) — the metric, formatted with units.</li><li><strong>Label</strong> (required) — what the number measures.</li><li><strong>Change indicator</strong> (optional) — delta vs. a prior period, with direction.</li><li><strong>Trend</strong> (optional) — a sparkline or tiny chart.</li><li><strong>Icon</strong> (optional) — reinforces the metric's meaning.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Loading</strong> — a skeleton in the value's shape.</li><li><strong>Populated</strong> — value with optional delta/trend.</li><li><strong>Positive / negative change</strong> — direction conveyed by more than color (arrow, sign).</li><li><strong>Interactive</strong> (optional) — the whole tile links to detail.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Value only</strong> — number and label.</li><li><strong>With change</strong> — delta vs. previous period.</li><li><strong>With sparkline</strong> — a compact trend.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A stat is compact: label and prominent value stacked, with the change/trend nearby. Tiles align to a consistent height and tile into a responsive row that reflows to fewer columns on small screens.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — the value and its label are associated so the number is meaningful to assistive tech.</li><li><strong>Change</strong> — direction conveyed by icon/sign and text, not color alone.</li><li><strong>Trend</strong> — sparklines provide a text summary or accessible value.</li><li><strong>Interactive</strong> — linked tiles expose a clear accessible name.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Format the value with units and sensible precision.</li><li>State the comparison period for any change (\"vs. last month\").</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> typography, badge (delta), icon, chart (sparkline).</p>\n<p><strong>Used by:</strong> dashboard.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Give every number a clear label and comparison context.</li><li>Convey change direction with more than color.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Crowd a tile with multiple competing numbers.</li><li>Show a delta without saying against what.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — dashboards and KPI presentation.</li></ul>",
    api: None,
  },
  {
    id: "table",
    title: "Table",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A grid of rows and columns for presenting structured, comparable data.",
    tags: ["data", "table", "structure", "comparison"],
    aliases: ["data-grid-static", "grid"],
    composedOf: ["typography", "badge", "checkbox"],
    usedBy: ["data-table", "dashboard", "card"],
    related: ["data-table", "card", "chart"],
    intent: "A table arranges structured data into rows and columns so values align for easy scanning and comparison across records and attributes. It is the clearest way to present tabular information where relationships between columns matter; the interactive data-table builds on it.",
    spec: "<h2>Intent</h2>\n<p>A table arranges structured data into rows and columns so values align for easy scanning and comparison across records and attributes. It is the clearest way to present tabular information where relationships between columns matter; the interactive <strong>data-table</strong> builds on it.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Data is inherently tabular and users compare values across rows and columns.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Items are object-like and better browsed as <strong>cards</strong>.</li><li>You only need a simple vertical list — use a list.</li><li>You need sorting/filtering/paging at scale — use a <strong>data-table</strong>.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Column headers</strong> (required) — name each column; may include units.</li><li><strong>Rows</strong> (required) — one record each.</li><li><strong>Cells</strong> (required) — values, aligned by data type.</li><li><strong>Caption / title</strong> (optional) — describes the table.</li><li><strong>Row/column groups</strong> (optional) — headers and footers, spanning cells.</li><li><strong>Footer</strong> (optional) — totals or summaries.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static display</strong> — the base behavior.</li><li><strong>Row emphasis</strong> (optional) — zebra striping or hover highlighting for tracking.</li><li><strong>Empty</strong> — a clear message when there are no rows.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Basic</strong> — headers and rows.</li><li><strong>With row/column groups</strong> — multi-level headers.</li><li><strong>Compact / comfortable</strong> density.</li><li><strong>With totals footer</strong>.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Columns size to content or a defined layout; numeric columns right-align for comparison. When too wide, allow horizontal scroll within a bounded region, pin key columns, or restructure rows into stacked blocks on small screens. Keep headers associated with data when scrolling.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — real table structure with header cells associated to data cells; a caption where helpful; scope for row/column headers.</li><li><strong>Screen reader</strong> — announces headers with cells so context is preserved.</li><li><strong>Reading order</strong> — logical and preserved across responsive transforms.</li><li><strong>Contrast</strong> — striping/highlighting meets contrast and doesn't obscure text.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Headers are concise nouns; align data types consistently.</li><li>Provide a meaningful empty state.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> typography, badge (status cells), checkbox (selection when extended).</p>\n<p><strong>Used by:</strong> data-table (as its core), dashboard, card.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Use genuine table semantics with header associations.</li><li>Right-align numbers for comparison.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Fake a table with generic containers that lose semantics.</li><li>Use a table purely for visual layout.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Table pattern; WCAG Info and Relationships.</li></ul>",
    api: None,
  },
  {
    id: "tabs",
    title: "Tabs",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A set of labeled sections where selecting a tab reveals its associated panel in place.",
    tags: ["navigation", "content", "sections", "in-page"],
    aliases: ["tab-group", "segmented-views"],
    composedOf: ["separator", "badge"],
    usedBy: ["dashboard", "settings", "card"],
    related: ["accordion", "toggle-group", "navigation-menu"],
    intent: "Tabs divide related content into peer sections and let users switch between them in place, showing one panel at a time. They organize a moderate amount of content without navigation or scrolling, keeping the user in the same context while they move between views of the same subject.",
    spec: "<h2>Intent</h2>\n<p>Tabs divide related content into peer sections and let users switch between them in place, showing one panel at a time. They organize a moderate amount of content without navigation or scrolling, keeping the user in the same context while they move between views of the same subject.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Content splits into a few peer sections viewed one at a time within the same context.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Users need to compare sections simultaneously — show them together.</li><li>Sections are steps in a sequence — use a stepper/wizard.</li><li>There are many sections or deep hierarchy — use navigation instead.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Tab list</strong> (required) — the row/column of tab triggers.</li><li><strong>Tabs</strong> (required) — labels, optional icons/badges, one selected.</li><li><strong>Active indicator</strong> (required) — marks the selected tab.</li><li><strong>Panels</strong> (required) — one content region per tab; only the active shows.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Selected / unselected</strong> — one active tab and panel.</li><li><strong>Activation model</strong> — automatic (on focus) or manual (on Enter/Space).</li><li><strong>Disabled</strong> — a tab that can't be selected.</li><li><strong>Overflow</strong> — scroll or collapse tabs that exceed the width.</li></ul>\n<p>Arrow keys move between tabs; the tab list is a single tab stop.</p>\n<h2>Variants</h2>\n<ul><li><strong>Horizontal / vertical</strong> orientation.</li><li><strong>Underline / pill / enclosed</strong> styles.</li><li><strong>Scrollable</strong> — for many tabs.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>The tab list sits above (or beside) the panels; the active indicator tracks selection. When tabs exceed the width, make the list scrollable with affordances, or collapse into a select on very small screens. Panels occupy the same space.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Keyboard</strong> — one tab stop; arrows move between tabs; Home/End jump; activation is automatic or manual consistently.</li><li><strong>Semantics</strong> — a tablist with tabs controlling their panels; selected state and tab/panel relationships exposed.</li><li><strong>Screen reader</strong> — announces the tab, its selected state, and the associated panel.</li><li><strong>Focus</strong> — visible focus on the active tab; panel is reachable.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Short, parallel tab labels; keep the number small.</li><li>Don't hide critical, always-needed content behind a non-default tab.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> separator, badge (counts on tabs).</p>\n<p><strong>Used by:</strong> dashboard, settings, card.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep labels short and the set small.</li><li>Follow tab keyboard conventions.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Use tabs to compare content that must be seen together.</li><li>Overflow into an unusable row without a fallback.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Tabs pattern.</li></ul>",
    api: None,
  },
  {
    id: "testimonial",
    title: "Testimonial",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A customer quote with attribution that builds trust through credible social proof.",
    tags: ["marketing", "landing", "section", "social-proof", "trust"],
    aliases: ["quote", "review", "social-proof"],
    composedOf: ["avatar", "card", "badge", "typography"],
    usedBy: ["landing-page", "pricing"],
    related: ["card", "hero"],
    intent: "A testimonial presents a real customer's words to build trust: a quote paired with who said it. Its power comes from credibility and specificity — a named person, their role and company, and a concrete outcome — which reassures prospects far more than the brand describing itself.",
    spec: "<h2>Intent</h2>\n<p>A testimonial presents a real customer's words to build trust: a quote paired with who said it. Its power comes from credibility and specificity — a named person, their role and company, and a concrete outcome — which reassures prospects far more than the brand describing itself.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Reinforcing claims with credible, attributable social proof.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>You have no genuine, attributable quote — fabricated proof erodes trust and can mislead.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Quote</strong> (required) — the customer's words.</li><li><strong>Attribution</strong> (required) — name, and ideally role and company.</li><li><strong>Avatar / logo</strong> (optional) — the person's photo or company mark.</li><li><strong>Rating / metric</strong> (optional) — a star rating or a quantified result.</li><li><strong>Source link</strong> (optional) — to the original review or case study.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Static</strong> — presentational.</li><li><strong>Group</strong> — several testimonials in a grid or a carousel.</li><li><strong>Featured</strong> — a single prominent quote.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Single featured quote</strong> — large and prominent.</li><li><strong>Grid of quotes</strong> — several compact cards.</li><li><strong>Logo wall + quote</strong> — proof breadth plus a highlighted quote.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A single quote centers with generous space; grids reflow to fewer columns and then a single column on small screens. Keep the attribution visually tied to its quote.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — marked up as a quotation with its attribution associated.</li><li><strong>Media</strong> — avatars/logos carry appropriate text alternatives or are marked decorative.</li><li><strong>Contrast</strong> — text meets contrast; ratings are conveyed by more than color.</li><li><strong>Carousel</strong> — if used, follows the carousel accessibility contract.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Use real, attributable quotes; keep them specific and concise.</li><li>Include enough attribution to be credible (name, role, company).</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> avatar, card, badge (rating), typography.</p>\n<p><strong>Used by:</strong> landing-page, pricing.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Attribute quotes to real, identifiable people.</li><li>Favor specific outcomes over generic praise.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Fabricate or anonymize to the point of meaninglessness.</li><li>Rely on color alone to convey ratings.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — testimonials and social proof.</li></ul>",
    api: None,
  },
  {
    id: "toast",
    title: "Toast",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A brief, self-dismissing message that confirms an action or reports a background event.",
    tags: ["feedback", "notification", "transient", "status"],
    aliases: ["snackbar", "notification", "flash-message"],
    composedOf: ["button", "progress"],
    usedBy: ["dashboard", "form", "data-table"],
    related: ["alert", "alert-dialog", "badge"],
    intent: "A toast briefly surfaces a low-friction message — \"Saved\", \"Copied\", \"Upload failed\" — then dismisses itself, confirming an action or reporting a background event without interrupting the user's flow. It's ephemeral and non-blocking, the opposite of a dialog.",
    spec: "<h2>Intent</h2>\n<p>A toast briefly surfaces a low-friction message — \"Saved\", \"Copied\", \"Upload failed\" — then dismisses itself, confirming an action or reporting a background event without interrupting the user's flow. It's ephemeral and non-blocking, the opposite of a dialog.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Confirming an action succeeded or a background event occurred.</li><li>Delivering a short, non-critical, transient message.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The message is critical or must be acknowledged — use an <strong>alert-dialog</strong> or a persistent <strong>alert</strong>.</li><li>The information must remain available — a toast disappears.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Message</strong> (required) — concise text; optional title.</li><li><strong>Icon</strong> (optional) — reinforces status.</li><li><strong>Action</strong> (optional) — a single quick action (e.g. \"Undo\").</li><li><strong>Dismiss</strong> (optional) — manual close.</li><li><strong>Timer / progress</strong> (optional) — indicates auto-dismiss.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Enter / visible / exit</strong> — appears, persists briefly, then auto-dismisses.</li><li><strong>Pause on interaction</strong> — hover/focus pauses the timer so it can be read/acted on.</li><li><strong>Stacking</strong> — multiple toasts queue or stack with sensible limits.</li><li><strong>Severity</strong> — success, info, warning, error (with non-color cues).</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Simple</strong> — text only.</li><li><strong>With action</strong> — e.g. undo.</li><li><strong>Promise-driven</strong> — loading → success/error for async tasks.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Anchored to a screen corner/edge, stacking a limited number and collapsing overflow. On small screens they typically span the width near an edge. They never block content beneath.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — announced via a live region: polite for routine confirmations, assertive for errors — without stealing focus.</li><li><strong>Timing</strong> — auto-dismiss is long enough to read; pauses on hover/focus; actionable toasts persist or are easily recoverable.</li><li><strong>Keyboard</strong> — actions and dismiss are keyboard-reachable while present.</li><li><strong>Non-color</strong> — severity conveyed by text/icon, not color alone.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>One short, clear sentence; lead with the outcome.</li><li>Offer at most one action; keep error toasts actionable.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button (action/dismiss), progress (timer indicator).</p>\n<p><strong>Used by:</strong> dashboard, form, data-table (operation feedback).</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Announce politely/assertively per severity without moving focus.</li><li>Pause auto-dismiss on hover/focus.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Put critical, must-act information in a transient toast.</li><li>Auto-dismiss so fast it can't be read or acted on.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Alert and status/live region guidance.</li></ul>",
    api: None,
  },
  {
    id: "toolbar",
    title: "Toolbar",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A grouped row of controls that act on the current view or selection.",
    tags: ["actions", "controls", "editor", "grouping"],
    aliases: ["action-bar", "command-bar", "tool-strip"],
    composedOf: ["button", "icon-button", "toggle", "toggle-group", "separator", "dropdown-menu", "input"],
    usedBy: ["data-table", "dashboard"],
    related: ["button-group", "menubar", "navbar"],
    intent: "A toolbar gathers the controls that act on the current context — a document, canvas, selection, or list — into one consistent strip. It keeps frequently used actions visible and within reach, grouped by relationship, so users can operate without hunting through menus.",
    spec: "<h2>Intent</h2>\n<p>A toolbar gathers the controls that act on the current context — a document, canvas, selection, or list — into one consistent strip. It keeps frequently used actions visible and within reach, grouped by relationship, so users can operate without hunting through menus.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A view has a set of recurring actions (format, filter, sort, add, view options).</li><li>Users benefit from persistent, at-a-glance access to those controls.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Actions are a product's global commands — use a <strong>menubar</strong>.</li><li>There is only a single action — a lone <strong>button</strong> is enough.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Container</strong> (required) — the horizontal (or vertical) strip.</li><li><strong>Groups</strong> (required) — related controls clustered together.</li><li><strong>Controls</strong> (required) — buttons, icon-buttons, toggles, toggle-groups, selects, and inputs.</li><li><strong>Separators</strong> (optional) — divide groups.</li><li><strong>Overflow menu</strong> (conditional) — collapses controls that don't fit.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Contextual enablement</strong> — controls enable/disable based on selection or state.</li><li><strong>Toggle state</strong> — view/format toggles reflect on/off or active option.</li><li><strong>Overflow</strong> — low-priority controls collapse into a menu on narrow widths.</li><li><strong>Sticky</strong> (optional) — remains visible while the content scrolls.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Editor toolbar</strong> — formatting/tool controls.</li><li><strong>View toolbar</strong> — search, filter, sort, and display options above a list/table.</li><li><strong>Compact/overflow</strong> — most controls behind an overflow menu.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Controls lay out in a row with grouping and separators; a common pattern places primary actions leading and view options trailing. As width shrinks, collapse groups into overflow menus rather than shrinking targets below usable size.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — exposed as a toolbar with an accessible name; groups are conveyed.</li><li><strong>Keyboard</strong> — a single tab stop for the toolbar with arrow-key navigation between controls (roving focus), per the toolbar pattern.</li><li><strong>Screen reader</strong> — announces the toolbar, control names, and states.</li><li><strong>Focus</strong> — visible focus; overflow contents remain reachable.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Group related controls; order by frequency and importance.</li><li>Give every icon-only control an accessible name and tooltip.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> button, icon-button, toggle, toggle-group, separator, dropdown-menu, input.</p>\n<p><strong>Used by:</strong> data-table, dashboard, editors.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Support arrow-key navigation across the toolbar.</li><li>Collapse gracefully into overflow when space is tight.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Overload the strip so nothing stands out.</li><li>Leave icon-only controls unlabeled.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Toolbar pattern.</li></ul>",
    api: None,
  },
  {
    id: "tooltip",
    title: "Tooltip",
    layer: "component",
    status: "stable",
    version: "1.0.0",
    summary: "A brief text label that appears on hover or focus to clarify an element.",
    tags: ["overlay", "hint", "help", "contextual"],
    aliases: ["hint", "tip"],
    composedOf: ["popover"],
    usedBy: ["button", "navbar", "sidebar", "toggle", "data-table"],
    related: ["hover-card", "popover", "kbd"],
    intent: "A tooltip shows a short, plain-text hint about an element when the user hovers or focuses it — the meaning of an icon button, a truncated label, an extra detail. It supplies supplementary clarification without permanently occupying space, and must never carry essential or interactive content.",
    spec: "<h2>Intent</h2>\n<p>A tooltip shows a short, plain-text hint about an element when the user hovers or focuses it — the meaning of an icon button, a truncated label, an extra detail. It supplies supplementary clarification without permanently occupying space, and must never carry essential or interactive content.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Clarifying an icon-only control or adding a brief, non-essential hint.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The content is essential or interactive — use a <strong>popover</strong> or visible text.</li><li>The preview is rich (media, structured info) — use a <strong>hover-card</strong>.</li><li>The element already has a clear visible label.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Trigger</strong> (required) — the element being described.</li><li><strong>Tip surface</strong> (required) — a small floating label, optionally with an arrow.</li><li><strong>Text</strong> (required) — a short phrase; no interactive controls.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Hidden / shown</strong> — appears after a short hover/focus delay, hides on leave/blur.</li><li><strong>Positioned</strong> — anchored to the trigger, flipping to stay in view.</li><li><strong>Dismissible</strong> — can be dismissed with <code>Escape</code> while the trigger stays focused.</li><li><strong>Persistence</strong> — stays visible while hovered so users can read it.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Plain</strong> — text only.</li><li><strong>With shortcut</strong> — includes a keyboard hint (kbd).</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Small and anchored near the trigger with collision handling. On touch there is no hover, so never depend on tooltips there — ensure the same info is otherwise available. Keep text short enough not to need scrolling.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — the tip provides the trigger's accessible description/name; triggered on both hover and keyboard focus.</li><li><strong>Content on hover/focus</strong> — dismissible, hoverable, and persistent per WCAG.</li><li><strong>Screen reader</strong> — the description is associated with the trigger, not orphaned.</li><li><strong>Non-essential</strong> — never place essential or interactive content in a tooltip.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep to a short phrase; don't restate a visible label.</li><li>No links or buttons inside — those belong in a popover.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> popover (positioning foundation).</p>\n<p><strong>Used by:</strong> button/toggle (icon controls), navbar, sidebar, data-table.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Trigger on focus as well as hover.</li><li>Keep it brief and dismissible.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Put essential or interactive content in a tooltip.</li><li>Rely on tooltips on touch devices.</li></ul>\n<h2>References</h2>\n<ul><li>WAI-ARIA Authoring Practices — Tooltip pattern; WCAG Content on Hover or Focus.</li></ul>",
    api: None,
  },
  {
    id: "dashboard",
    title: "Dashboard",
    layer: "page",
    status: "stable",
    version: "1.0.0",
    summary: "An at-a-glance overview page that surfaces key metrics, status, and entry points to act.",
    tags: ["analytics", "overview", "application", "data"],
    aliases: ["home", "overview", "console"],
    composedOf: ["navbar", "sidebar", "breadcrumb", "toolbar", "card", "stat", "chart", "data-table", "list", "empty-state"],
    usedBy: [],
    related: ["settings", "data-table", "chart"],
    intent: "A dashboard gives users an at-a-glance understanding of the state of a system and fast paths to act on it. It answers \"how are things, and what needs my attention?\" by arranging the most important metrics, trends, and recent activity into a scannable overview, with drill-downs to the detail behind each summary.",
    spec: "<h2>Intent</h2>\n<p>A dashboard gives users an at-a-glance understanding of the state of a system and fast paths to act on it. It answers \"how are things, and what needs my attention?\" by arranging the most important metrics, trends, and recent activity into a scannable overview, with drill-downs to the detail behind each summary.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Users return regularly to monitor status and jump into work.</li><li>Several related metrics and activity streams benefit from a single overview.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The user has one primary task — a focused task page serves better.</li><li>There isn't enough meaningful data to justify an overview (show an onboarding empty state instead).</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>App shell</strong> (required) — navbar and/or sidebar for global navigation, plus breadcrumbs or a page title for context.</li><li><strong>Toolbar / filters</strong> (optional) — date range, scope, and view controls.</li><li><strong>Metric summary</strong> (required) — a row of stat/KPI cards.</li><li><strong>Visualizations</strong> (optional) — charts for trends and comparisons.</li><li><strong>Detail regions</strong> (optional) — data-tables and lists of recent activity.</li><li><strong>States</strong> (required) — loading (skeletons), empty (first-run), and error.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Loading</strong> — regions show skeletons; the layout is reserved to avoid shift.</li><li><strong>Populated</strong> — metrics, charts, and tables render with live data.</li><li><strong>Empty / first-run</strong> — an onboarding empty state guides setup.</li><li><strong>Filtered</strong> — global controls (time range, scope) update all regions.</li><li><strong>Error</strong> — a region that fails shows a contained, retryable error.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Analytics dashboard</strong> — metric- and chart-heavy.</li><li><strong>Operational dashboard</strong> — status, queues, and recent activity.</li><li><strong>Personal home</strong> — tailored shortcuts and summaries.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A responsive grid of regions inside the app shell: a metric row up top, charts and tables below, reflowing from multiple columns to a single column on small screens. Prioritize the most important summary above the fold; let detail regions scroll.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — a logical heading outline and landmarks (nav, main) so regions are navigable.</li><li><strong>Data alternatives</strong> — charts provide accessible data/summaries; tables use proper semantics.</li><li><strong>Keyboard</strong> — filters, drill-downs, and region controls are fully operable.</li><li><strong>Live updates</strong> — auto-refreshing regions announce changes politely without stealing focus.</li><li><strong>States</strong> — loading/empty/error are conveyed as text, not visuals alone.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Lead with the metrics that drive decisions; label units and time ranges.</li><li>Make each summary drill down to its detail.</li><li>Write empty states that help the user get to first value.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> navbar, sidebar, breadcrumb, toolbar, card, stat, chart, data-table, list, empty-state.</p>\n<p><strong>Used by:</strong> Not applicable — a top-level experience.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Surface what needs attention first and link to the detail.</li><li>Keep every region's loading, empty, and error states honest.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Crowd the page with vanity metrics that don't inform action.</li><li>Block the whole page on one slow region.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — dashboard design and information hierarchy.</li></ul>",
    api: None,
  },
  {
    id: "landing-page",
    title: "Landing Page",
    layer: "page",
    status: "stable",
    version: "1.0.0",
    summary: "A standalone marketing page designed to convert a focused audience toward a single primary action.",
    tags: ["marketing", "conversion", "page", "funnel"],
    aliases: ["landing", "marketing-page", "splash"],
    composedOf: ["navbar", "hero", "feature-grid", "testimonial", "pricing-table", "faq", "cta-section", "footer", "button", "input"],
    usedBy: [],
    related: ["dashboard", "pricing", "sign-in"],
    intent: "A landing page exists to move a specific audience toward one primary action — sign up, request a demo, start a trial, buy. Unlike a general home page, it is purpose-built and self-contained: every section builds the argument for that one action, and distractions that don't serve it are removed. Success is measured by conversion, so clarity and momentum matter more than exhaustiveness.",
    spec: "<h2>Intent</h2>\n<p>A landing page exists to move a specific audience toward <strong>one primary action</strong> — sign up, request a demo, start a trial, buy. Unlike a general home page, it is purpose-built and self-contained: every section builds the argument for that one action, and distractions that don't serve it are removed. Success is measured by conversion, so clarity and momentum matter more than exhaustiveness.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>You are driving a defined audience (from an ad, campaign, or referral) toward a single conversion goal.</li><li>You need a focused narrative: problem → solution → proof → action.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The user is already inside the product and needs to accomplish work — use a <strong>dashboard</strong> or task page.</li><li>The goal is broad navigation to many destinations — that's a home page, not a landing page.</li></ul>\n<h2>Anatomy</h2>\n<p>Sections are ordered to build and re-assert the argument; most are optional but the hero and a primary call to action are required.</p>\n<ul><li><strong>Navbar</strong> (required) — minimal, conversion-oriented; often just logo + CTA.</li><li><strong>Hero</strong> (required) — headline, supporting subhead, and the primary CTA above the fold; optionally a visual or product shot.</li><li><strong>Social proof</strong> (optional) — logos, ratings, or user counts that build trust early.</li><li><strong>Feature / benefit sections</strong> (optional) — framed as user outcomes, not raw features.</li><li><strong>Testimonials / case studies</strong> (optional) — credible, specific proof.</li><li><strong>Pricing</strong> (conditional) — when price is part of the decision.</li><li><strong>FAQ</strong> (optional) — removes last-mile objections.</li><li><strong>Closing CTA section</strong> (required) — restates the primary action for users who scrolled.</li><li><strong>Footer</strong> (required) — legal, secondary links, and trust signals.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Default</strong> — static content optimized for scannability.</li><li><strong>Lead capture</strong> — inline form or CTA that submits interest; shows loading and success/error feedback.</li><li><strong>Empty/loading media</strong> — images and embeds reserve space to avoid layout shift.</li><li><strong>Personalized / variant (optional)</strong> — content may differ by campaign or A/B test; the primary action stays singular.</li></ul>\n<p>The primary CTA repeats at natural decision points (hero, after proof, closing) without introducing competing primary actions.</p>\n<h2>Variants</h2>\n<ul><li><strong>Lead-gen</strong> — optimized to capture contact details via a short form.</li><li><strong>Click-through</strong> — sends the user onward to a sign-up or checkout flow.</li><li><strong>Product launch</strong> — narrative-heavy, announcement-oriented.</li><li><strong>App/store redirect</strong> — funnels to an app store or download.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A single scrollable column of full-width sections with a constrained content measure for readability. The hero leads; supporting sections alternate emphasis to maintain rhythm. Multi-column sections (features, pricing) stack into a single column on small screens. Keep the primary CTA reachable — recurring in-flow and, optionally, persistent in the navbar. Design for fast load and no cumulative layout shift, since landing traffic is often first-touch and impatient.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — a single, logical heading outline (one page title, then sectioned subheadings) so the page is navigable by headings.</li><li><strong>Landmarks</strong> — header, main, and footer landmarks are present and correct.</li><li><strong>Keyboard</strong> — every interactive element, including the lead form, is fully operable by keyboard in a sensible order.</li><li><strong>Media</strong> — images have appropriate text alternatives; decorative media is marked as such; no essential meaning is carried by color alone.</li><li><strong>Motion</strong> — respect reduced-motion preferences for animated or autoplaying content.</li><li><strong>Forms</strong> — lead-capture fields follow the input archetype's accessibility contract.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Lead with the value to the user, not the product's internals.</li><li>One primary message and one primary action; everything else supports them.</li><li>Keep the hero headline concrete and benefit-led; keep CTAs specific (\"Start free trial\", not \"Submit\").</li><li>Use proof (numbers, names, quotes) rather than adjectives.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> navbar, hero, feature-grid, testimonial, pricing-table, faq, cta-section, footer — which in turn use elements like button and input.</p>\n<p><strong>Used by:</strong> Not applicable — a landing page is a top-level experience.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep a single, repeated primary action throughout the page.</li><li>Front-load the value proposition and the CTA above the fold.</li><li>Back claims with specific, credible proof.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Compete the primary goal with many equally-weighted actions.</li><li>Bury the CTA or make users hunt for the next step.</li><li>Sacrifice load performance and stability for heavy media.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — landing page usability guidance.</li><li>WCAG — headings, landmarks, alternatives, and reduced motion.</li></ul>",
    api: None,
  },
  {
    id: "pricing",
    title: "Pricing Page",
    layer: "page",
    status: "stable",
    version: "1.0.0",
    summary: "A page that presents plans and their value to help a visitor choose and convert.",
    tags: ["marketing", "conversion", "plans", "pricing"],
    aliases: ["plans", "pricing-plans"],
    composedOf: ["navbar", "card", "badge", "switch", "toggle-group", "button", "table", "accordion", "faq", "footer"],
    usedBy: [],
    related: ["landing-page", "card"],
    intent: "A pricing page helps a visitor compare plans and choose the right one, then act. It has to make value and differences legible at a glance, reduce the anxiety of committing, and guide most people toward the intended plan — all while answering the practical questions (billing period, limits, what's included) that block a decision.",
    spec: "<h2>Intent</h2>\n<p>A pricing page helps a visitor compare plans and choose the right one, then act. It has to make value and differences legible at a glance, reduce the anxiety of committing, and guide most people toward the intended plan — all while answering the practical questions (billing period, limits, what's included) that block a decision.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Presenting paid plans/tiers a visitor selects between to sign up or buy.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>There is a single plan or price — state it plainly without a comparison page.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Header</strong> (required) — a concise value statement and, often, a billing-period toggle (monthly/annual).</li><li><strong>Plan cards</strong> (required) — name, price, a short pitch, key features, and a call to action; a recommended plan is highlighted.</li><li><strong>Highlight</strong> (optional) — a \"most popular\"/recommended badge on one plan.</li><li><strong>Comparison table</strong> (optional) — a detailed feature-by-plan matrix.</li><li><strong>FAQ</strong> (optional) — removes last-mile objections.</li><li><strong>Footer</strong> (required) — trust signals, contact, and legal.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Billing toggle</strong> — switching monthly/annual updates all prices (and shows savings).</li><li><strong>Recommended plan</strong> — visually emphasized to guide choice.</li><li><strong>Currency/locale</strong> (optional) — prices adapt to region.</li><li><strong>CTA per plan</strong> — each plan's action reflects its state (start, upgrade, contact sales).</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Tiered cards</strong> — two to four plans side by side.</li><li><strong>Cards + comparison table</strong> — summary cards over a detailed matrix.</li><li><strong>Usage-based</strong> — a calculator/estimate for metered pricing.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Plan cards sit in a row on wide screens, emphasizing the recommended plan, and stack into a single column on small screens (keeping the recommended plan prominent). A comparison table scrolls horizontally or restructures on narrow widths.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — clear headings; the billing toggle's effect on prices is announced.</li><li><strong>Comparison table</strong> — real table semantics with header associations.</li><li><strong>Keyboard</strong> — the billing toggle, plan CTAs, and FAQ are fully operable.</li><li><strong>Non-color emphasis</strong> — the recommended plan is marked by more than color.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Lead with value, not just numbers; make what's included scannable.</li><li>Show billing period and any savings clearly; avoid hidden costs.</li><li>Use one specific CTA verb per plan.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> navbar, card, badge, switch/toggle-group (billing), button, table, accordion/faq, footer.</p>\n<p><strong>Used by:</strong> Not applicable — a top-level experience.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Make plan differences and what's included easy to compare.</li><li>Guide toward a recommended plan without dark patterns.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Hide the billing period, limits, or real costs.</li><li>Overwhelm the first view with an exhaustive matrix before the summary.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — pricing page and plan-comparison usability.</li></ul>",
    api: None,
  },
  {
    id: "settings",
    title: "Settings",
    layer: "page",
    status: "stable",
    version: "1.0.0",
    summary: "A page for viewing and changing a user's or system's configuration, organized into sections.",
    tags: ["configuration", "preferences", "forms", "account"],
    aliases: ["preferences", "account-settings", "options"],
    composedOf: ["sidebar", "tabs", "navigation-menu", "form", "field", "input", "switch", "select", "radio-group", "button", "separator", "alert"],
    usedBy: [],
    related: ["dashboard", "form"],
    intent: "A settings page lets users view and adjust configuration — account, profile, notifications, billing, security — organized so a specific option is easy to find and safe to change. It groups many controls into coherent sections and makes the state of each setting, and how changes are saved, unmistakable.",
    spec: "<h2>Intent</h2>\n<p>A settings page lets users view and adjust configuration — account, profile, notifications, billing, security — organized so a specific option is easy to find and safe to change. It groups many controls into coherent sections and makes the state of each setting, and how changes are saved, unmistakable.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Users need to manage a non-trivial set of preferences or account options.</li><li>Options divide into meaningful categories worth navigating between.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>There is a single toggle or option — present it inline where it applies.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Section navigation</strong> (required for larger sets) — a sidebar, tabs, or menu of categories.</li><li><strong>Section content</strong> (required) — grouped settings, each a field or control with a label and description.</li><li><strong>Controls</strong> (required) — inputs, switches, selects, radio-groups, and buttons.</li><li><strong>Save model</strong> (required) — either instant-apply per control or an explicit save/cancel per section.</li><li><strong>Feedback</strong> (required) — success, error, and unsaved-changes indication.</li><li><strong>Dangerous actions</strong> (optional) — a clearly separated destructive zone.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Instant-apply</strong> — switches/toggles take effect immediately with confirmation.</li><li><strong>Form-style</strong> — grouped fields saved on submit, with dirty-state tracking and a warning before discarding unsaved changes.</li><li><strong>Validation</strong> — invalid values are flagged inline and block save.</li><li><strong>Saving / saved / error</strong> — clearly communicated.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Sidebar sections</strong> — a left nav of categories with content on the right.</li><li><strong>Tabbed</strong> — categories as tabs for a smaller set.</li><li><strong>Single scroll</strong> — sections stacked with anchor navigation.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Category navigation alongside a single readable content column; on small screens the navigation collapses (into a menu or top tabs) and content goes full-width. Keep a consistent field rhythm and clear section headings.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Structure</strong> — section headings and landmarks make categories navigable.</li><li><strong>Controls</strong> — every setting has an associated label and description; validation and required states are exposed.</li><li><strong>Save feedback</strong> — saving/success/error conveyed via live regions, not color alone.</li><li><strong>Keyboard</strong> — navigation between sections and operation of every control is fully keyboard-accessible.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Group related settings; give each a clear label and a plain-language description.</li><li>Make the save model obvious (instant vs. explicit save).</li><li>Separate and confirm destructive actions.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> sidebar, tabs, navigation-menu, form, field, input, switch, select, radio-group, button, separator, alert.</p>\n<p><strong>Used by:</strong> Not applicable — a top-level experience.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Make each setting's meaning and current value clear.</li><li>Communicate exactly how and when changes are saved.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Mix instant-apply and save-required controls without making it obvious.</li><li>Place destructive actions where they're easy to trigger by accident.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — preferences and settings guidance.</li></ul>",
    api: None,
  },
  {
    id: "sign-in",
    title: "Sign In",
    layer: "page",
    status: "stable",
    version: "1.0.0",
    summary: "A focused page that authenticates a returning user with minimal friction.",
    tags: ["auth", "authentication", "form", "account", "security"],
    aliases: ["login", "log-in", "signin"],
    composedOf: ["form", "field", "input", "input-otp", "checkbox", "button", "link", "alert", "separator"],
    usedBy: [],
    related: ["form", "settings"],
    intent: "A sign-in page authenticates a returning user and gets them into the product with as little friction as possible. It is a focused, single-purpose screen: collect credentials (or start a passwordless/again-party flow), handle errors clearly, and provide the escape hatches — reset password, alternate methods, and the path to sign up.",
    spec: "<h2>Intent</h2>\n<p>A sign-in page authenticates a returning user and gets them into the product with as little friction as possible. It is a focused, single-purpose screen: collect credentials (or start a passwordless/again-party flow), handle errors clearly, and provide the escape hatches — reset password, alternate methods, and the path to sign up.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>A returning user must authenticate to access the product.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>Creating a new account — use a sign-up page (related, distinct goals).</li><li>A lightweight step-up re-auth suffices — a dialog may be enough.</li></ul>\n<h2>Anatomy</h2>\n<ul><li><strong>Identity / branding</strong> (optional) — logo and product name for trust.</li><li><strong>Credential form</strong> (required) — email/username and password, or a passwordless entry.</li><li><strong>Alternate methods</strong> (optional) — social/SSO providers, magic link, or a one-time code (input-otp).</li><li><strong>Options</strong> (optional) — \"remember me\", show-password toggle.</li><li><strong>Recovery</strong> (required) — forgot-password link.</li><li><strong>Cross-links</strong> (required) — path to sign up.</li><li><strong>Feedback</strong> (required) — inline validation and a clear authentication-error message.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Editing</strong> — validate format on blur/submit; preserve input on error.</li><li><strong>Submitting</strong> — the submit shows progress; prevent duplicate attempts.</li><li><strong>Auth error</strong> — a clear, non-leaky message (\"email or password is incorrect\"); focus is directed to it.</li><li><strong>Multi-step</strong> — passwordless or 2FA advances to a code step (input-otp).</li><li><strong>Locked / rate-limited</strong> — communicates the cause and next step.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Password</strong> — email + password.</li><li><strong>Passwordless</strong> — magic link or one-time code.</li><li><strong>SSO-first</strong> — provider buttons with an email fallback.</li><li><strong>Two-step</strong> — credentials then a verification code.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A narrow, centered card is the conventional layout, optionally beside a brand panel on wide screens that collapses on small ones. Keep the form short, the primary action prominent, and recovery/sign-up links visible.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Semantics</strong> — a form with associated labels; the error summary is announced and focus moves to it on failure.</li><li><strong>Keyboard</strong> — full keyboard operation; <code>Enter</code> submits; show-password and provider buttons are reachable.</li><li><strong>Autofill &amp; code entry</strong> — supports credential autofill and OS one-time-code autofill for the code step.</li><li><strong>Security messaging</strong> — errors avoid revealing which field was wrong beyond what's necessary, while remaining actionable.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Keep the form minimal; ask only for what's needed to authenticate.</li><li>Write a single, clear error for failed authentication.</li><li>Make \"forgot password\" and \"create account\" easy to find.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> form, field, input, input-otp, checkbox, button, link, alert, separator.</p>\n<p><strong>Used by:</strong> Not applicable — a top-level experience.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Preserve entered values on error and focus the message.</li><li>Offer recovery and sign-up paths.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Reveal whether an account exists through differential error messages.</li><li>Clear the form on a failed attempt.</li></ul>\n<h2>References</h2>\n<ul><li>WCAG — Error Identification; Labels or Instructions.</li><li>OWASP — authentication UX and messaging guidance.</li></ul>",
    api: None,
  },
  {
    id: "authentication",
    title: "Authentication",
    layer: "flow",
    status: "stable",
    version: "1.0.0",
    summary: "A multi-step journey that verifies a user's identity to grant access to the product.",
    tags: ["auth", "security", "flow", "account", "journey"],
    aliases: ["auth-flow", "login-flow", "sign-in-flow"],
    composedOf: ["sign-in", "form", "field", "input", "input-otp", "button", "alert", "link"],
    usedBy: [],
    related: ["sign-in", "onboarding", "checkout"],
    intent: "Authentication is the journey that establishes who the user is and lets them in. It spans more than one screen — enter credentials, verify a second factor, recover a forgotten password, or complete a passwordless step — and its job is to be secure without being hostile: minimal friction for legitimate users, clear recovery paths, and honest, non-leaky handling of failures.",
    spec: "<h2>Intent</h2>\n<p>Authentication is the journey that establishes who the user is and lets them in. It spans more than one screen — enter credentials, verify a second factor, recover a forgotten password, or complete a passwordless step — and its job is to be secure without being hostile: minimal friction for legitimate users, clear recovery paths, and honest, non-leaky handling of failures.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Users must prove identity to access protected functionality across one or more steps.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>A single screen fully covers it — that's the <strong>sign-in</strong> page archetype on its own.</li><li>Establishing a new account is the goal — that's a sign-up flow (related).</li></ul>\n<h2>Anatomy (steps)</h2>\n<ul><li><strong>Entry</strong> (required) — sign-in with credentials or a passwordless start.</li><li><strong>Verification</strong> (conditional) — a second factor or one-time code step.</li><li><strong>Recovery</strong> (conditional) — request reset, confirm via link/code, set a new password.</li><li><strong>Provider/SSO</strong> (optional) — hand-off to an identity provider and back.</li><li><strong>Success / redirect</strong> (required) — land the user where they intended to go.</li><li><strong>Feedback</strong> (required) — clear errors, lockout, and rate-limit messaging.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Step progression</strong> — the flow advances only when a step succeeds.</li><li><strong>Errors</strong> — invalid credentials, expired codes, and locked accounts are communicated clearly and safely.</li><li><strong>Interruptions</strong> — deep links resume to the right step; the intended destination is preserved and honored after success.</li><li><strong>Session</strong> — establishes the session and respects \"remember me\".</li><li><strong>Step-up</strong> — sensitive actions can re-trigger a lightweight re-auth.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Password + optional 2FA</strong>.</li><li><strong>Passwordless</strong> — magic link or one-time code.</li><li><strong>SSO-first</strong> — provider hand-off with an email fallback.</li><li><strong>Reset password</strong> — a self-contained recovery sub-flow.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Each step is a focused, centered screen (or dialog for step-up), minimizing distraction. Progress between steps is evident, and the flow is comfortable on small screens where auth most often happens.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Per-step semantics</strong> — each screen's form and errors follow the form/field contract; focus moves to the first error on failure.</li><li><strong>Code entry</strong> — one-time-code steps support paste and OS autofill.</li><li><strong>Announcements</strong> — step changes and errors are announced without stealing focus disruptively.</li><li><strong>Recovery</strong> — reset paths are fully keyboard-accessible and clearly labeled.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Ask only for what's needed at each step.</li><li>Use a single, non-revealing error for failed authentication.</li><li>Make recovery and provider options easy to find.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> sign-in, form, field, input, input-otp, button, alert, link.</p>\n<p><strong>Used by:</strong> Not applicable — a top-level journey; precedes protected experiences.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Preserve the intended destination and resume there after success.</li><li>Keep failures safe and actionable.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Leak whether an account exists via differential responses.</li><li>Force unnecessary steps for legitimate returning users.</li></ul>\n<h2>References</h2>\n<ul><li>OWASP — authentication and session management guidance.</li><li>WCAG — Error Identification; Labels or Instructions.</li></ul>",
    api: None,
  },
  {
    id: "checkout",
    title: "Checkout",
    layer: "flow",
    status: "stable",
    version: "1.0.0",
    summary: "A step-by-step flow that collects the details needed to complete a purchase.",
    tags: ["commerce", "payment", "flow", "conversion", "journey"],
    aliases: ["checkout-flow", "purchase-flow", "payment-flow"],
    composedOf: ["form", "field", "input", "radio-group", "list", "card", "button", "alert", "progress"],
    usedBy: [],
    related: ["pricing", "onboarding"],
    intent: "Checkout is the journey from \"I want this\" to a completed purchase. It collects the necessary details — contact, shipping, and payment — confirms the order, and processes it, all while protecting a fragile moment: every unnecessary field, surprise cost, or unclear error is a chance to abandon. Its job is to reduce friction and build confidence right up to \"place order.\"",
    spec: "<h2>Intent</h2>\n<p>Checkout is the journey from \"I want this\" to a completed purchase. It collects the necessary details — contact, shipping, and payment — confirms the order, and processes it, all while protecting a fragile moment: every unnecessary field, surprise cost, or unclear error is a chance to abandon. Its job is to reduce friction and build confidence right up to \"place order.\"</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>Completing a purchase that requires payment and (often) shipping details.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>A single tap completes the buy (stored payment, one-click) — collapse the flow.</li><li>No payment is involved — it's a form or onboarding, not checkout.</li></ul>\n<h2>Anatomy (steps)</h2>\n<ul><li><strong>Order summary</strong> (required) — items, quantities, and a transparent cost breakdown (subtotal, shipping, tax, total), present throughout.</li><li><strong>Contact / account</strong> (required) — email; guest checkout offered.</li><li><strong>Shipping</strong> (conditional) — address and method selection.</li><li><strong>Payment</strong> (required) — payment details, with security cues.</li><li><strong>Review</strong> (required) — confirm everything before charging.</li><li><strong>Confirmation</strong> (required) — success with an order reference and next steps.</li><li><strong>Progress</strong> (required for multi-step) — where the user is in the flow.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Step progression</strong> — one section at a time (or a single scroll) with a persistent order summary.</li><li><strong>Validation</strong> — address/payment validated inline before advancing.</li><li><strong>Cost transparency</strong> — totals update as shipping/discounts change; no surprise fees at the end.</li><li><strong>Processing</strong> — a clear in-progress state; prevent double-submission.</li><li><strong>Payment error</strong> — clear, recoverable messaging without losing entered data.</li><li><strong>Confirmation</strong> — order reference, receipt, and what happens next.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Multi-step</strong> — contact → shipping → payment → review.</li><li><strong>Single-page</strong> — all sections on one scroll with a sticky summary.</li><li><strong>Express</strong> — wallet/one-click that skips most steps.</li><li><strong>Guest vs. account</strong> — purchase without or with sign-in.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>A focused layout (often minimal chrome to reduce distraction) with the form leading and the order summary persistent (beside on wide screens, collapsible on small). Progress is visible in multi-step variants; the primary action is unmistakable.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Per-step forms</strong> — labels, validation, and error summaries follow the form/field contract; focus moves to errors.</li><li><strong>Progress</strong> — the current step is exposed and announced.</li><li><strong>Processing</strong> — busy/success states are announced without stealing focus.</li><li><strong>Cost changes</strong> — total updates are perceivable to assistive tech.</li><li><strong>Payment fields</strong> — support autofill and clear, secure labeling.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>Show the full cost breakdown early and keep it visible; no surprise fees.</li><li>Offer guest checkout; ask only for what's required.</li><li>Write payment errors as specific, recoverable next steps.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> form, field, input, radio-group, list (order items), card, button, alert, progress.</p>\n<p><strong>Used by:</strong> Not applicable — a top-level journey; follows plan/cart selection.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Keep costs transparent and the order summary visible.</li><li>Preserve entered data through validation and payment errors.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Introduce surprise costs at the last step.</li><li>Force account creation to complete a purchase.</li></ul>\n<h2>References</h2>\n<ul><li>Baymard Institute — checkout usability and cart abandonment research.</li></ul>",
    api: None,
  },
  {
    id: "onboarding",
    title: "Onboarding",
    layer: "flow",
    status: "stable",
    version: "1.0.0",
    summary: "A guided sequence that gets a new user from first login to first meaningful value.",
    tags: ["activation", "flow", "first-run", "journey"],
    aliases: ["onboarding-flow", "getting-started", "setup-wizard"],
    composedOf: ["progress", "form", "field", "button", "empty-state", "card", "dialog", "checkbox"],
    usedBy: [],
    related: ["authentication", "checkout"],
    intent: "Onboarding carries a brand-new user from their first login to the moment the product delivers value (\"aha\"). It gathers just enough setup to personalize the experience, teaches the few things needed to start, and removes the blank-slate paralysis of an empty account — all while letting motivated users skip ahead. Its success metric is activation, not completion for its own sake.",
    spec: "<h2>Intent</h2>\n<p>Onboarding carries a brand-new user from their first login to the moment the product delivers value (\"aha\"). It gathers just enough setup to personalize the experience, teaches the few things needed to start, and removes the blank-slate paralysis of an empty account — all while letting motivated users skip ahead. Its success metric is activation, not completion for its own sake.</p>\n<h2>When to use / When not to use</h2>\n<p><strong>Use when</strong></p>\n<ul><li>New users need setup, orientation, or their first data to get value.</li></ul>\n<p><strong>Avoid when</strong></p>\n<ul><li>The product is immediately useful with no setup — don't manufacture steps.</li><li>The \"onboarding\" is really a marketing tour that delays real use.</li></ul>\n<h2>Anatomy (steps)</h2>\n<ul><li><strong>Welcome</strong> (optional) — sets expectations and the payoff.</li><li><strong>Setup steps</strong> (required) — the minimum configuration/personalization, one idea per step, with clear progress.</li><li><strong>First action</strong> (required) — create/import the first item so the account isn't empty (an empty state that guides).</li><li><strong>Guidance</strong> (optional) — inline hints, checklists, or a short tour.</li><li><strong>Completion</strong> (required) — confirm readiness and hand off to the product.</li><li><strong>Skip / resume</strong> (required) — let users skip and pick up later.</li></ul>\n<h2>States &amp; behavior</h2>\n<ul><li><strong>Progress</strong> — steps show position and how many remain.</li><li><strong>Skippable / resumable</strong> — users can defer steps and return; progress persists.</li><li><strong>Validation</strong> — required setup is validated before advancing.</li><li><strong>Empty → filled</strong> — the first-run empty state converts into real content.</li><li><strong>Completion tracking</strong> — a checklist may persist post-flow until done.</li></ul>\n<h2>Variants</h2>\n<ul><li><strong>Wizard</strong> — sequential, full-screen steps with progress.</li><li><strong>Checklist</strong> — a persistent list of setup tasks completed at the user's pace.</li><li><strong>Inline coach marks</strong> — contextual hints layered on the real product.</li></ul>\n<h2>Layout &amp; responsiveness</h2>\n<p>Wizard steps are focused single screens with visible progress and one primary action; checklists live beside or within the product. Everything remains usable on small screens, and steps never trap the user without a way forward or out.</p>\n<h2>Accessibility</h2>\n<ul><li><strong>Progress</strong> — the step position is exposed and announced as it changes.</li><li><strong>Per-step forms</strong> — follow the form/field accessibility contract.</li><li><strong>Focus</strong> — moves sensibly between steps; dialogs trap and restore focus.</li><li><strong>Skippable</strong> — skip/resume controls are reachable and clearly labeled.</li></ul>\n<h2>Content guidelines</h2>\n<ul><li>One idea per step; ask only for setup that improves the first experience.</li><li>Make the payoff and remaining steps clear; always offer a skip.</li></ul>\n<h2>Composition</h2>\n<p><strong>Composed of:</strong> progress, form, field, button, empty-state, card, dialog, checkbox (checklist).</p>\n<p><strong>Used by:</strong> Not applicable — a top-level journey following first sign-in.</p>\n<h2>Do / Don't</h2>\n<p><strong>Do</strong></p>\n<ul><li>Get users to first value fast; let them skip and resume.</li><li>Replace the empty slate with a guided first action.</li></ul>\n<p><strong>Don't</strong></p>\n<ul><li>Gate the product behind long, skippable-feeling setup.</li><li>Confuse a feature tour with genuine activation.</li></ul>\n<h2>References</h2>\n<ul><li>Nielsen Norman Group — onboarding and first-time user experience.</li></ul>",
    api: None,
  },
]
